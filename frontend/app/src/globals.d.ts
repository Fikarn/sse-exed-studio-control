/// <reference types="vite/client" />

/**
 * Replaced at build time by `define` in vite.config.ts. Reads from
 * frontend/app/package.json so the version on the health bar bumps with
 * the package manifest rather than a manually-edited constant.
 */
declare const __APP_VERSION__: string;
