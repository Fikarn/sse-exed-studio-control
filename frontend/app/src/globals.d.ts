/// <reference types="vite/client" />

/**
 * Replaced at build time by `define` in vite.config.ts. Reads from
 * the root package.json so the version on the health bar follows the
 * release manifest rather than a manually edited constant.
 */
declare const __APP_VERSION__: string;
