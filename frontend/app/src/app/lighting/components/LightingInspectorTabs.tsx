// Visual overhaul A, Slice 5b took the plate's tab row away: the plate shows
// what is selected, every section at once. The name of each view stays — the
// plate, the session and the region read it — and so does this file, as its
// home. New pages program, Slice 3: the tab row's component, unused since, went
// with its arrow-key handler.
export type InspectorTab = "scene" | "fixture" | "group" | "palettes" | "patch";
