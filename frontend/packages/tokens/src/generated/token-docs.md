# Generated Token Reference

This file is the committed output target for the token source in `src/source/tokens.json`.

The initial scaffold preserves the current native console identity:

- dark-only studio grayscale
- green-primary operational accent
- IBM Plex Sans / IBM Plex Mono
- dense spacing and fixed-height controls
- explicit shell, surface, and stage token groups

Use `npm run build --workspace @sse-exed/tokens` after the workspace dependencies are installed to regenerate the CSS, TypeScript, and token docs outputs.
