# Production readiness remediation 2026-09

Status: Slice 12 landed 2026-09-18 (its release-evidence run and its CI run are recorded by its ledger follow-up, which moves it to `verified`; Slice 11 landed 2026-09-17, operator verification pending) on branch `production-readiness-2026-09` (cut from `ui-gold-standard-2026-09` @ `c03e1db`, so the whole 2026-09 stack is carried inside it; S0 = `d935aaa` + `cfa91e4`, S1 = `b9da9dc` + `38ffc0d`, S2 = `c2b7361` + `214bdcb`, S3 = `e4c283d` + `011fe44`, S4 = `f3db990` + `c126b10`, S5 = `f1dc897` + `c4d849f` + `eaa3cc5`, S6 = `bee067b` + `330c3fb`, S7 = `9bb1f8d` + `c74ca5f` + `1799e9b`, S8 = `b00ecaf` + `8b1892e`, S9 = `96ae4e9` + `c87565a` + `96939dc`, S10 = `957acc9` + `04b68d7`, S11 = `9c45d36` + `4b89191` + `d0ef0ff`, S12 = the slice commit + its ledger follow-up, all pushed). CI runs on every branch push; the first run on the stack (34459772849) is recorded under Slice 0, the S1 run (34472807896) was the first with `qualification` green, the S2 (34487837771), S3 (34507456992), S4 (34514042594), S5 (34574043349, after the lane follow-up), S6 (34593996173), S7 (34602095638, on the baselines commit), S8 (35201156819) and S9 runs (35208750697 on the slice commit, 35209951483 on the queue follow-up) kept that shape, and the S10 run (35230069147) kept it too, and so did the S11 run (35261830154, on the baselines commit); the S12 runs — the branch run, now with a ninth job, `supply-chain`, and the `release-evidence` run on the rehearsal tag — are recorded under Slice 12 — `frontend-e2e` stays red on the stale linux baselines and the S13 timing cases until Appendix B item 6 and S13. The workstation baseline is under Slice 0 (with an S1 correction on `scripts:test`). Open operator items: Appendix B item 1 (re-import the regenerated Stream Deck profile; S2 moves to `verified` when it is signed), Appendix B item 2 (end the engine process during a session and launch a second copy of the app; S5 moves to `verified` when it is signed), Appendix B item 3 (confirm live TotalMix meters and console echo with the loopback bind; S6 moves to `verified` when it is signed), Appendix B item 4 (verify and restore a database backup from Setup / Support and confirm fixtures, scenes, deck bindings return; S7 moves to `verified` when it is signed) and Appendix B item 5 (sACN to the real bridge starting with `SSE_SAFE_START=1`, arming from Setup / Support, the Stream Deck's lighting keys while previewing, and Recent actions showing who did what; S10 and S11 move to `verified` when it is signed). None is blocking. The packaged production build under `release\native\windows` is still the S2-era one (see the correction under Slice 9 and Slice 10's Operator hands); promoting a build from this branch is the operator's, and from Slice 11 on it upgrades the operator's database to schema 7, which the S2-era build refuses — the way back is then by hand (Slice 11's Operator hands). Appendix B item 7 (signing certificates, D2) now names the secrets that wake the dormant signing steps; it blocks nothing. Next: Slice 13 — Test posture (F25, F03 e2e blocking); it depends on S2 S10 S12. Nothing past S12 is implemented.

Tracking: one commit per slice `Readiness S<N>: <what landed>` (baseline refreshes as `Readiness S<N> (baselines): …`), with a What / Why / Tests / Verified body. The per-slice `Status:` lines below are the execution record; any divergence from a slice's written scope gets a bold **Rescope:** paragraph under that slice per the AGENTS.md rescope protocol — no silent substitution. Status vocabulary: `planned` → `in progress (next: <step>)` → `landed (operator verification pending)` (commit named; all lanes green; hardware not yet walked) → `verified (<date>, Appendix B item N)`. Slices with no hardware exposure (S0, S1, S4, S9, S12, S13, S14, S15) go straight to `verified` when their lanes are green. Every slice records, before it closes: what the existing gates asserted before the change, the tests added that would have caught the finding, and every test that had to change (old assertion → new assertion → reason).

Goal / Sources: close all 32 findings (F01–F32) of the 2026-09-10 production readiness audit of this program (verdict NOT PRODUCTION READY: 3 critical, 16 major, 13 minor; artifact "Studio Control Readiness Audit", https://claude.ai/code/artifact/06da97b6-852a-421f-901d-6541fa233978, memory note `production-readiness-audit-2026-09-10`). Design (authoritative for _what to build_): `~/.claude/plans/i-want-you-to-logical-quilt.md` on the operator's workstation — it is not in the repository; each slice below carries the scope it needs. If this ledger and the design disagree, these `Status:` lines win for what has happened and the design wins for what to build, unless a **Rescope:** paragraph here says otherwise. Outcome: every finding closed by a change **and** a named guard (test or lane) that fails if it regresses; CI running on every push; the operator hardware checklist enumerated for sign-off; verdict re-ratable to "ready with minor changes" once Appendix B is signed.

Session protocol: start with `git status && git log --oneline -10`; read `AGENTS.md`, this file's `Status:` / `Tracking:` paragraphs and the first slice not `verified`; re-read that slice in the design; confirm the production app is closed (port 38201 free, TotalMix untouched); run only that slice's lanes to re-establish state. End by updating the slice `Status:` with `next:`, recording lane results with dates under Validation, committing only complete slices, and describing any uncommitted work in `Status:` so the next session can resume or discard it — never leave a half-slice committed. Work sequentially in one session; no parallel subagents (they exhaust this workstation's usage limits).

Hardware safety (binding for every lane in this program): tests never write to TotalMix ports 7001–7010 (`SSE_ENGINE_TEST_ALLOW_CONSOLE_WRITES` stays unset; test sockets ≥ 7011); the production app is closed before any `tauri:*:qualify` or bridge lane (ports 38201 / 4173, single instance from S5 on); Playwright and a qualification lane never run at the same time (both bind 4173); sACN toward the real bridge only in Appendix B sessions with the operator present, from S11 on starting with `SSE_SAFE_START=1`; never load a TotalMix snapshot slot; `native:acceptance` stays simulated (`SSE_NATIVE_ACCEPTANCE_LIVE_CONSOLE` unset).

Mechanical drift guards: `scripts/check-slice-rescope.mjs` (a slice title change without a **Rescope:** paragraph is refused at commit), `npm run protocol:check`, `tests/contract.rs`, `EVENT_DOMAIN_REFRESH satisfies Record<EventName, …>` (S9 on), `scripts/file-health.mjs`, the ui-contract ratchets (a measure may fall, never rise), `scripts/check-operator-copy.mjs` (never "engine", "backend", "transport", "IPC", "snapshot" outside the Console — say "hardware link", "backup", "Stream Deck profile", "light outputs", "saved data"), coverage floors (S13 on).

**Rescope:** none at program level; slice-level paragraphs sit under Slice 1 (four deviations), Slice 2 (four), Slice 3 (two), Slice 4 (five), Slice 5 (three), Slice 6 (none in scope, two additions recorded), Slice 7 (three), Slice 8 (seven), Slice 9 (eight), Slice 10 (ten), Slice 11 (twelve) and Slice 12 (twelve), each old scope → new scope → why, with the slice titles unchanged because what landed is what the titles say. A slice whose premise changes on inspection gets its own **Rescope:** paragraph under its heading and a renamed title that matches what landed.

## Locked decisions (operator, 2026-09-10)

| #                   | Decision                                                                                                                                                                                                         |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1                  | Lighting output at launch keeps today's auto-stream; the fix adds an opt-in hold (`SSE_SAFE_START=1` env + persisted `app.lighting.output_armed`, default `true`) with a Support › Workstation switch.           |
| D2                  | Release chain: tag-triggered CI builds packaged bundles, SHA256 manifests and CycloneDX SBOMs; signing steps are wired but dormant until certificate secrets exist. No certificate purchase in this program.     |
| D3                  | F26 (splitting the three large orchestrators) is the last implementation slice, guarded by ui-contract, visual-review and Playwright.                                                                            |
| D4                  | Slice 0 changes the CI trigger only; the operator opens pull requests and sets branch protection themselves.                                                                                                     |
| D5 (design default) | Bridge token is generated per install, stored in app-data, and embedded in the exported Companion profile; the operator re-imports the profile once (Full Reset & Import).                                       |
| D6 (design default) | No new UI surfaces beyond: recovery copy for new failure codes, a Verify-backup action, an Outputs Armed/Held switch, a Recent-actions list in Support. Everything else is engine/shell/store work. No redesign. |

## Program shape

Path shorthands: `E/` = `native/rust-engine/src/`, `T/` = `native/tauri-shell/src/`, `FA/` = `frontend/app/src/`, `EC/` = `frontend/packages/engine-client/src/`. Slices run strictly in order unless the dependency column allows otherwise; S6 and S12 depend only on S0 and can be pulled forward if a session is blocked (for example waiting on network for a crate).

| Slice | Title                                      | Closes                   | Depends on | Hours |
| ----- | ------------------------------------------ | ------------------------ | ---------- | ----- |
| S0    | Branch, ledger, CI on every push, baseline | F03 (trigger)            | —          | 3     |
| S1    | Dev-fixture gate, default dirs/hosts       | F04 F22 F23 F24          | S0         | 5     |
| S2    | Bridge hardening                           | F01 F06 F28              | S0         | 12    |
| S3    | Storage integrity + database backups       | F02 F13 F21              | S0         | 12    |
| S4    | Shell hardening                            | F07 F08 F15              | S0         | 8     |
| S5    | Engine supervision + single instance       | F09 F19                  | S4         | 14    |
| S6    | OSC ingress                                | F05                      | S0         | 4     |
| S7    | Support backup/restore surface             | F20 F29                  | S3         | 12    |
| S8    | Engine health + logging                    | F11 (engine) F14 F16 F27 | S3 S6      | 10    |
| S9    | Front-end resilience                       | F10 F11 (front-end) F32  | S5 S8      | 12    |
| S10   | Lighting concurrency + sACN render path    | F12 F18                  | S2 S3      | 16    |
| S11   | Audit trail + safe start                   | F30 F31                  | S3 S10     | 14    |
| S12   | Supply chain + release CI                  | F17 F03 (release)        | S0         | 10    |
| S13   | Test posture                               | F25 F03 (e2e blocking)   | S2 S10 S12 | 10    |
| S14   | Front-end structure                        | F26                      | S9 S11     | 20    |
| S15   | Docs + closeout                            | —                        | all        | 6     |

## Traceability

Kept current every commit. `Commit` names the slice commit by subject until the hash is known; `Status` is `open` until the closing slice lands, then mirrors that slice's `Status:` line.

| Finding                                        | Slice        | Guard (fails on regression)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Commit                                                                                                                                         | Status                                                                                                                                                                                                                      |
| ---------------------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F01 bridge unauthenticated                     | S2           | `authorize_*` unit tests + `bridge_refuses_unauthenticated_requests_and_serves_authenticated_ones`; `companion_export_carries_the_bridge_token_on_every_request`; `native:bridge:win:verify` refusals (401 / 403 / 400)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `c2b7361`                                                                                                                                      | landed 2026-09-10 (operator verification pending, Appendix B item 1)                                                                                                                                                        |
| F02 no backups, integrity ignored              | S3           | `initialize_refuses_corrupt_database`, `snapshot_writes_verified_copy`, `prune_keeps_retention`, `bootstrap::tests::corrupt_db_yields_storage_corrupt`, `startupHelpers.test.ts` "formats STORAGE_CORRUPT", `tauriTransport.test.ts` listener-before-start, store test "keeps the engine-reported failure"; setup-support lane scenario `corrupt-db`                                                                                                                                                                                                                                                                                                                                                                                                                                          | `e4c283d`                                                                                                                                      | verified 2026-09-10 (S3)                                                                                                                                                                                                    |
| F03 CI never ran / e2e advisory                | S0, S12, S13 | CI run on branch push; `release-evidence.yml` on a `v*` tag (S12: bundle, SHA256 manifest checked against the archive, three SBOMs refused when empty or foreign, from clean Windows and macOS runners); required checks list in ledger                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `d935aaa` + `cfa91e4` (trigger); `Readiness S12: supply-chain job on every push, release evidence on a tag, SBOMs, dependency fixes` (release) | trigger verified 2026-09-10 (run 34459772849; `qualification` green from run 34472807896, S1); release chain landed 2026-09-18 (the rehearsal-tag run is recorded under Slice 12 by its follow-up); e2e blocking open (S13) |
| F04 dev method in prod                         | S1           | `parity_fixture_unavailable_without_feature` (release build), `parity_fixture_loads_with_feature`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `b9da9dc`                                                                                                                                      | verified 2026-09-10 (S1; CI run 34472807896)                                                                                                                                                                                |
| F05 OSC 0.0.0.0 ingress                        | S6           | `accept_source_rejects_foreign_ip`, `receive_policy_binds_loopback_for_a_loopback_console_and_every_interface_otherwise`, `receive_sockets_bind_loopback_for_loopback_console`, `foreign_datagrams_are_dropped_and_logged_once_per_minute`, `classic_slots_read_the_console_address_only`, `dropped_source_log_notes_a_source_once_a_minute`; the workspace lane's live audio probe on the workstation                                                                                                                                                                                                                                                                                                                                                                                        | `bee067b`                                                                                                                                      | landed 2026-09-11 (operator verification pending, Appendix B item 3)                                                                                                                                                        |
| F06 bridge DoS                                 | S2           | `read_http_request_rejects_body_over_cap`, `read_http_request_rejects_oversized_headers`, `read_http_request_times_out_on_short_body`, `worker_pool_returns_503_when_saturated`; lane 413 / 408 checks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `c2b7361`                                                                                                                                      | landed 2026-09-10 (operator verification pending, Appendix B item 1)                                                                                                                                                        |
| F07 sync commands block UI                     | S4           | `shell-stays-responsive-during-stalled-request` in the setup-support lane (the test bridge's status-file heartbeat keeps landing while the engine sits in a stalled lighting probe; the check fails on the sync-command shell)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `f3db990`                                                                                                                                      | verified 2026-09-10 (S4; CI run 34514042594)                                                                                                                                                                                |
| F08 request-id collision                       | S4           | `tauriTransport.test.ts` "ids unique within one millisecond" + "a second transport instance carries its own session nonce"; `engine::tests::request_refuses_duplicate_pending_id`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `f3db990`                                                                                                                                      | verified 2026-09-10 (S4; CI run 34514042594)                                                                                                                                                                                |
| F09 silent engine death                        | S5           | `exit_watcher_fails_pending_and_emits_event`, `stop_reports_a_graceful_exit_and_kills_a_lingering_engine`; store tests "engine.exited → recovery + bounded restart", "stale generation ignored", "keeps the last twenty background failures without leaving ready"; "resolves initialize with the launch the shell reported"; "formats ENGINE_EXITED"; lane `engine-crash`                                                                                                                                                                                                                                                                                                                                                                                                                    | `f1dc897` + `c4d849f`                                                                                                                          | landed 2026-09-11 (operator verification pending, Appendix B item 2)                                                                                                                                                        |
| F10 no error boundary                          | S9           | `ShellErrorBoundary.test.tsx` (the root page with no store at all, a malformed reply shown by name in a development build, `WorkspaceErrorBoundary` keeps a render error inside the area and adds no element, `reportUiFailure` records an error once, `BackgroundFailureBand`); `shell.spec.ts` "workspace crash keeps shell alive" and "the crash hook is absent unless the fixture URL asks for it"                                                                                                                                                                                                                                                                                                                                                                                        | `96ae4e9`                                                                                                                                      | verified 2026-09-17 (S9; no hardware exposure)                                                                                                                                                                              |
| F11 fan-out + whole-log read                   | S8, S9       | `read_log_tail_bounded`; store tests "event refreshes only mapped domains" (one `lighting.changed` costs 2 requests, was 10; all eleven events), "a request refreshes only what its method can change", "events raised together are fetched as one batch", "events that arrive while a batch is out share the next one", "a partial refresh writes only what it fetched", "unknown event full refresh", "catalog once per session", "a restart abandons a batch that never settles"; `domainRefresh.test.ts`; both qualification lanes (the development store on the live hardware link)                                                                                                                                                                                                      | `b00ecaf` (engine half); `96ae4e9` + `c87565a` (front-end half)                                                                                | verified 2026-09-17 (engine half with S8, front-end half with S9; no hardware exposure)                                                                                                                                     |
| F12 lighting blob race / deck bypasses preview | S10          | `deck_and_ipc_mutations_serialize_without_lost_updates` (`app::tests`: the screen's requests and the deck's keys through their real entry points, a reader beside them; fails 3 of 3 runs without the lock), `deck_preview_action_updates_preview_runtime` (the stored state and the frames the wire would carry do not move), `ipc_preview_mode_is_the_preview_the_deck_edits`, `deck_recall_writes_what_a_recall_writes_and_nothing_after_it` and `deck_save_scene_never_reuses_a_live_scene_id` (both fail at `96939dc`), `deck_relative_keys_store_through_the_fixture_update`, `deck_light_and_planning_actions_raise_their_events`; `lighting::tests_state_lock` (the generation, the values a key starts from, the lock order)                                                         | `957acc9`                                                                                                                                      | landed 2026-09-17 (operator verification pending, Appendix B item 5)                                                                                                                                                        |
| F13 in-place migrations                        | S3           | `migration_snapshots_before_upgrading_v5`, `blob_migration_failure_rolls_back`, `migrate_schema_refuses_malformed_editor_state_json_at_the_blob_step`, `bootstrap::tests::migration_failure_maps_to_its_own_code`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `e4c283d`                                                                                                                                      | verified 2026-09-10 (S3)                                                                                                                                                                                                    |
| F14 health never degrades                      | S8           | `status_error_on_integrity_failure`, `status_attention_on_bridge_port_fallback`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `b00ecaf`                                                                                                                                      | verified 2026-09-17 (S8; no hardware exposure)                                                                                                                                                                              |
| F15 no CSP / open paths                        | S4           | `open_path_rejects_outside_app_data`, `diagnostics_report_lands_with_a_utc_stamp_and_never_overwrites`; `verifyContentSecurityPolicy` in `tauri:smoke` (policy present and strict, `devCsp` present); lane `diagnostics-export-defaults-to-app-data-exports`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `f3db990`                                                                                                                                      | verified 2026-09-10 (S4; CI run 34514042594)                                                                                                                                                                                |
| F16 stderr discarded                           | S8           | `rg eprintln!` count ≤ 2 (documented); `stderr_lines_reach_shell_log`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `b00ecaf`                                                                                                                                      | verified 2026-09-17 (S8; the two documented sites are named under Slice 8)                                                                                                                                                  |
| F17 no scanning / unsigned                     | S12          | `supply-chain` CI job — `scripts/check-npm-audit.mjs` (every lockfile, expiring allowlist; `check-npm-audit.test.mjs` on recorded reports), `scripts/check-deny-ignores.mjs` (`check-deny-ignores.test.mjs`), `cargo deny check` against `native/deny.toml`; `release-evidence.yml` with `scripts/write-release-sboms.mjs` (`write-release-sboms.test.mjs`) and the dormant signing step (`native-sign.test.mjs`); unsigned stays the documented posture until Appendix B item 7 (D2)                                                                                                                                                                                                                                                                                                         | `Readiness S12: supply-chain job on every push, release evidence on a tag, SBOMs, dependency fixes`                                            | landed 2026-09-18 (the CI run and the rehearsal-tag run are recorded under Slice 12 by its follow-up, which moves it to `verified`)                                                                                         |
| F18 DB as bus                                  | S10          | `rerenders_settings_only_on_generation_change` (a counting reader, the generation and the clock handed in: one read per state change, one safety read per two seconds, a failed read retried), `render_frames_identical_before_and_after_cache` (golden frame), `thread_local_read_connection_reused`, `kept_read_connection_sees_later_writes_and_blocks_neither_backup_nor_checkpoint`, `pending_restore_folds_the_write_ahead_log_into_the_replaced_copy` (fails without the checkpoint); both qualification lanes and `native:bridge:win:verify` run the engine with the kept connections                                                                                                                                                                                                 | `957acc9`                                                                                                                                      | landed 2026-09-17 (operator verification pending, Appendix B item 5)                                                                                                                                                        |
| F19 no single instance                         | S5           | `second_lock_holder_is_refused`, `bootstrap_refuses_a_held_app_data_dir`, `second_engine_on_the_same_app_data_dir_is_refused` (end_to_end); "formats ENGINE_ALREADY_RUNNING"; lane `second-instance` (the plugin's refusal on Windows and macOS, the engine lock's on Linux)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `f1dc897` + `c4d849f`                                                                                                                          | landed 2026-09-11 (operator verification pending, Appendix B item 2)                                                                                                                                                        |
| F20 restore gaps                               | S7           | `restore_refuses_newer_format`, `restore_round_trips_all_prefixes`, `list_includes_sqlite3`, `verify_flags_junk_file`, `database_restore_stages_pending_file_and_keeps_rollback`, `pre_restore_archives_are_capped_at_five`, `exports_older_than_thirty_days_are_pruned`; `bootstrap::tests::pending_restore_applied_after_snapshot`, `pending_restore_that_fails_its_check_is_refused`; `storage_backups::tests::newest_snapshot_skips_replaced_files`; store tests "restore with requiresRestart restarts the link once and spends no restart budget", "fetches the backup list after a storage failure at start"; `SetupSupportPilot.test.tsx` "verify shows inline result"; lane checks `database-backup-verifies-and-restores-after-restart`, `corrupt-db-restores-from-database-backup` | `9bb1f8d` + `c74ca5f`                                                                                                                          | landed 2026-09-11 (operator verification pending, Appendix B item 4)                                                                                                                                                        |
| F21 synchronous NORMAL                         | S3           | `connection_uses_synchronous_full`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `e4c283d`                                                                                                                                      | verified 2026-09-10 (S3)                                                                                                                                                                                                    |
| F22 relative app-data default                  | S1           | `default_app_data_dir_is_absolute_platform_dir`, `runtime_paths_refuse_to_guess_without_a_platform_base`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `b9da9dc`                                                                                                                                      | verified 2026-09-10 (S1; CI run 34472807896)                                                                                                                                                                                |
| F23 CWD auto-import                            | S1           | `auto_import_ignores_cwd` (`tests/end_to_end.rs`) + `bootstrap::tests::legacy_import_source_*`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `b9da9dc`                                                                                                                                      | verified 2026-09-10 (S1; CI run 34472807896)                                                                                                                                                                                |
| F24 vite host 0.0.0.0                          | S1           | `scripts/frontend/dev-server-host.test.mjs` (Vite host assertion + `0.0.0.0` scan)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `b9da9dc`                                                                                                                                      | verified 2026-09-10 (S1; CI run 34472807896)                                                                                                                                                                                |
| F25 test posture                               | S13          | coverage floors; quarantine project; proptest suites                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | —                                                                                                                                              | open                                                                                                                                                                                                                        |
| F26 oversized modules                          | S14          | `file:health` with allowlist entries removed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | —                                                                                                                                              | open                                                                                                                                                                                                                        |
| F27 request logging                            | S8           | log-level test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `b00ecaf`                                                                                                                                      | verified 2026-09-17 (S8; `request_line_is_off_at_info_and_on_at_debug`, `handle_request_writes_no_request_line_at_the_default_level`)                                                                                       |
| F28 percent decoding                           | S2           | `query_parameter_decodes_percent`; lane `%2F` / `%5F` cases                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `c2b7361`                                                                                                                                      | landed 2026-09-10 (operator verification pending, Appendix B item 1)                                                                                                                                                        |
| F29 arbitrary restore paths                    | S7           | `restore_rejects_path_outside_backups`, `verify_flags_junk_file` (the `.txt` refusal); `SetupSupportPilot.test.tsx` "verify shows the refusal for a file outside the backups folder"; the fixture transport's `findFixtureBackup` (the same sentence)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `9bb1f8d` + `c74ca5f`                                                                                                                          | landed 2026-09-11 (operator verification pending, Appendix B item 4)                                                                                                                                                        |
| F30 no hardware audit trail                    | S11          | `control_surface::tests::deck_all_off_records_source_deck` and its counterparts at the other entry points — `app::tests::ui_requests_record_source_ui`, `audio::tests_console_link::console_changes_record_source_console`, `audio::talkback::tests::watchdog_release_records_source_watchdog`, `deck_audio_keys_record_source_deck`; `action_log::tests::record_list_prune`, `every_contract_method_is_classified` (a method added to the contract fails until it is classified as an action or not), `sentences_avoid_the_words_the_operator_never_reads`; `storage::tests::migrate_v6_to_v7_after_snapshot`; `app::tests::recovery_mode_support_snapshot_has_no_recent_events_and_opens_no_database`; workspaces lane `ui-actions-appear-in-recent-actions`                                | `9c45d36`                                                                                                                                      | landed 2026-09-17 (operator verification pending, Appendix B item 5)                                                                                                                                                        |
| F31 no safe start                              | S11          | `lighting_sacn_output::tests::held_outputs_send_nothing` (a loopback listener on its own port: nothing at all while held from the start, sequence 0 on arming, three stream-terminated packets on a hold), `a_hold_reaches_the_wire_on_the_next_tick`, `bootstrap::tests::safe_start_env_holds` (the hold outlives the launch; a default launch writes nothing), `database_restore_carries_the_armed_flag`, `support::tests::restore_never_changes_whether_the_light_outputs_are_armed`, `app::tests::set_armed_holds_and_arms_the_light_outputs`, `health::tests::held_light_outputs_are_not_a_fault`; `SetupSupportPilot.test.tsx` `arm switch calls setArmed`, `shellData.test.ts`; setup-support lane scenario `safe-start`                                                               | `9c45d36`                                                                                                                                      | landed 2026-09-17 (operator verification pending, Appendix B item 5)                                                                                                                                                        |
| F32 unvalidated snapshots                      | S9           | store tests "malformed snapshot rejected in dev", "a row without an id is refused by name", "a malformed snapshot in production is a failed refresh, never a throw", "every fixture scenario passes the guards"; `snapshotGuards.test.ts`; `check-fixtures.mjs` (`recentLogExcerpt` is a string); both qualification lanes run the development store, so a live reply the guards refuse fails them                                                                                                                                                                                                                                                                                                                                                                                            | `96ae4e9`                                                                                                                                      | verified 2026-09-17 (S9; no hardware exposure)                                                                                                                                                                              |

## Slice 0 — Branch, ledger, CI on every push, baseline

Status: verified 2026-09-10 (commit `d935aaa`, pushed; every `dev:check` step green on the workstation; the push produced the first CI run ever on this stack, recorded under "CI (result)" below; no hardware exposure). The CI record landed as the follow-up commit `Readiness S0 (ledger): first CI run on the branch recorded`, because a run cannot be recorded by the commit that triggers it.

Scope: branch `production-readiness-2026-09` cut from `ui-gold-standard-2026-09` @ `c03e1db`; this ledger; `.github/workflows/dev-checks.yml` `on.push.branches` `[main]` → `["**", "!dependabot/**"]` with `pull_request` and the `concurrency` block kept — the Dependabot exclusion is added to the design's `["**"]` because Dependabot branches already get a `pull_request` run and would otherwise run every job twice; `CHANGELOG.md` `[Unreleased]` carries the program placeholder paragraph and a Changed bullet for the trigger; `docs/HANDOFF.md` gets the pointer to this ledger and its two sentences that stated the old trigger are corrected (blocker 9 item 1 and the Validation Baseline paragraph), as is the one in `docs/DEVELOPMENT.md` §Pull Request CI. No source file changed.
Gates before: no CI job had ever run on `studio-bringup-sacn-globalosc`, `audit-remediation-2026-09` or `ui-gold-standard-2026-09` (64 commits ahead of `main`); every gate in the two 2026-09 ledgers was run on the studio workstation only (win32).
Tests added: none — a trigger has no unit test; its guard is the run itself (Appendix A, F03). Tests changed: none.
Validation (workstation, 2026-09-10): `npm run dev:check` green end to end (exit 0; every cache warm, so the whole bundle took about two minutes) — per-step results under Baselines. This file contains a **Rescope:** paragraph, so `scripts/check-slice-rescope.mjs` accepts its sixteen new slice titles at commit (lint-staged runs it on every staged `docs/plans/**.md`).
Baselines (workstation, 2026-09-10, tree = `c03e1db` + the S0 docs and trigger change): `format:check` clean; `lint` clean; `scripts:test` ran **0 tests** — no `scripts/**/*.test.mjs` exists yet, so the lane is empty rather than red (S12 adds the first one and must confirm the single-quoted glob in the `scripts:test` script expands under Windows `cmd`, which does not strip quotes) — corrected in S1: thirteen such files existed and CI had been running them; the workstation ran none because `cmd.exe` handed Node the single quotes literally, and the glob is double-quoted since S1 (85 tests run here); `file:health` passed with four allowed oversized sources (`frontend/app/src/app/lighting/LightingWorkspace.tsx` 3171 lines, `frontend/packages/engine-client/src/transports/fixtureTransport.ts` 6659, `native/rust-engine/src/lighting/tests.rs` 3481, `native/rust-engine/src/rme_totalmix_osc.rs` 2893 — S14 owns the first; the other three are not in this program's scope) and one allowed large artifact; `rust:fmt:check` clean; `rust:clippy` clean under `-D warnings`; `protocol:check` clean (it prints a Node `DEP0190` deprecation warning from a `shell: true` child spawn — noise, no owning slice); `frontend:typecheck` clean across the six workspaces (27 fixture scenarios validated; token artifacts carry the A families); `frontend:test` 253 passed (app 59 in 9 files, design-system 175 in 24, engine-client 4 in 2, tokens 15 in 1, shared-graphics has no tests); `native:check` clean; `native:test` 300 passed, 1 ignored (tauri-shell 12, engine 282 + 1 ignored hardware-bound test, `contract.rs` 4, `end_to_end.rs` 2, protocol crate 0). Visual baselines were not run (no UI change); the repository carries 93 win32 / 75 linux / 75 darwin captures as HANDOFF blocker 9 records.
CI (result, 2026-09-10): run 34459772849 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/34459772849), event `push`, sha `d935aaa`, created within five seconds of the push — the first run ever on the 65-commit stack; it finished in eleven minutes. Green: `lint`, `format-protocol`, `frontend-typecheck`, `frontend-test`, `rust` (rustfmt, clippy, check, test, `native:acceptance`), `tauri-foundation`. Red, expected: `frontend-e2e` — 92 failed / 323 passed in 8.5 min: 73 linux screenshot mismatches + 18 missing linux snapshots (the stale-baseline case F03 states; the run's `playwright-test-results` artifact is the refresh source, so no pull request is needed for it — the pull request itself and the required-checks list stay the operator's, D4, Appendix B item 6) + 1 timing case, `tests/audio-arm-countdown.spec.ts:45` (on the runner the second click landed after the 350 ms arm dwell, so the recall applied; a quarantine candidate for S13, not chased here). Red, not expected: `qualification` — the setup-support qualification passed; the workspace qualification failed at `scripts/tauri-workspace-qualification.mjs:386` (`updateCommissioning-3`) with `Publish refused: Control Surface Probe not run, Audio OSC Probe failed`: the script publishes `stage: "ready"` without `overrideProbes: true` while CI sets `SSE_TAURI_QUALIFICATION_SKIP_AUDIO_PROBE=1`; the setup-support script carries the override (line 301), the workspaces script never did, and the audit-remediation Slice 8 publish gate had never run on Linux (that ledger says "scripts carry `overrideProbes`" — only one does). Also logged there: the lighting probe server cannot bind 127.0.0.1:80 on the runner (EACCES; the script continues). Owning slice: **S1** — a script-only fix recorded as a **Rescope:** under Slice 1; the job stays red at that step on every push until it lands. Not fixed in S0 (D4: trigger only).
Operator hands: none for S0. Program-level operator items are Appendix B items 6 (the PR + required checks, after S13) and 7 (signing certificates, D2).
Workstation notes for later slices (recorded once, here): a stale `vite preview --host 127.0.0.1 --port 4173 --strictPort` from an earlier session (PID 213120) was found listening on 4173; it is not the production app and serves `frontend/app/dist` from disk. Stop it before the first lane that binds 4173 (S1's `tauri:setup-support:qualify`). TotalMix FX and Companion were running throughout and were not touched; nothing listened on 38201.

## Slice 1 — Dev-fixture gate, default dirs/hosts

Status: verified 2026-09-10 (commit `b9da9dc` `Readiness S1: dev-fixture gate, platform app-data default, staged auto-import, loopback dev server`, pushed; every S1 lane and `npm run dev:check` green on the workstation; CI run 34472807896 recorded under "CI (result)" below by the follow-up commit `Readiness S1 (ledger): CI run on the branch recorded`, as in S0; no hardware exposure). Closes F04 F22 F23 F24. Depends on S0.

Scope (as landed): `native/rust-engine/Cargo.toml` gains `[features] dev-fixtures = []`. `E/main.rs` compiles `mod parity_fixtures` only under the feature; `E/app.rs` keeps `"dev.parityFixture.load" =>` as two literal one-line arms (`#[cfg(feature = "dev-fixtures")]` → today's handler, `#[cfg(not(feature = "dev-fixtures"))]` → `METHOD_UNAVAILABLE`, no events, nothing written) and gates `dispatch_parity_fixture` / `reply_with_app_commissioning_and_planning_change` with it; the method stays in `v1.contract.json` (`tests/contract.rs` green in both builds). `E/parity_fixtures.rs::parse_parity_fixture_request` defaults `replaceExistingData` to `false` (merge; `import_legacy_db` refuses the merge over existing data as `INVALID_PARAMS`). `E/bootstrap.rs`: `resolve_runtime_paths()` returns `Result<RuntimePaths, String>`; with no `SSE_APP_DATA_DIR` it resolves the platform durable directory through a copy of the shell's `default_app_data_dir_for_platform` (`%APPDATA%` → `%LOCALAPPDATA%`; `~/Library/Application Support`; `$XDG_DATA_HOME` → `~/.local/share`; joined with `ExEd Studio Control Native`, the shell's name), and with no base it fails — `main.rs` emits `engine.startupFailed {stage: "bootstrap", code: "BOOTSTRAP_FAILED"}` instead of guessing (the shell always passes the directory, so only a bare engine launch reaches this); `resolve_legacy_import_source(app_data_dir)` honours `SSE_DISABLE_AUTO_IMPORT`, then `SSE_LEGACY_DB_PATH`, then `<app-data>/import/db.json` — the working-directory scan is gone. `frontend/app/vite.config.ts` `server.host: "127.0.0.1"`. `package.json`: new `native:engine:build:dev-fixtures` (`cd native && cargo build --package studio-control-engine --features dev-fixtures`; no lane needs it — nothing calls the method — and `docs/DEVELOPMENT.md` says so next to `tauri:cutover:candidate`); `scripts:test` glob double-quoted. New `scripts/frontend/dev-server-host.test.mjs` (the F24 guard). `scripts/tauri-workspace-qualification.mjs` runs the control-surface probe too and publishes with `overrideProbes: true` only when a probe cannot pass on the host. Docs: `native/protocol/v1.md` (feature, `replaceExistingData`, import sources, app-data default), `native/README.md`, `docs/DEVELOPMENT.md`, `scripts/README.md`; CHANGELOG four Changed and two Fixed bullets.
**Rescope:** four deviations from the written scope, none silent. (1) The workspace-qualification fix S0 queued here turned out wider than "add the override for CI": the lane never ran the control-surface probe, and the publish gate counts a probe that was not run as failing, so the publish was refused on every host since the gate landed (the audit-remediation Slice 8 entry says "the three publishing scripts pass `overrideProbes`" — this lane was the fourth). Landed: all three probes run, and the override is sent only when `SSE_TAURI_QUALIFICATION_SKIP_AUDIO_PROBE=1` (CI, no TotalMix) or the lighting probe server could not bind 127.0.0.1:80; on the workstation the publish therefore goes through the real gate — the evidence records `commissioning-probes-and-publish-complete.overrideProbes: false`. (2) The S0 baseline said `scripts:test` ran 0 tests because no `scripts/**/*.test.mjs` existed. Wrong: thirteen existed (eight beside their scripts, five under `scripts/release/`). The lane ran none on the workstation because npm runs scripts through `cmd.exe`, which hands Node the single-quoted glob literally (CI's bash strips the quotes, so CI ran them all along). The F24 guard lives in that lane, so the glob is double-quoted here (85 tests now run on the workstation, 81 passed, 4 skipped) and S12's "confirm the glob" note is closed. (3) The planned `bootstrap::tests::auto_import_ignores_cwd` is `tests/end_to_end.rs::auto_import_ignores_cwd`: the working directory is process-wide, so the honest test spawns the engine with a `data/db.json` under its working directory and asserts `planningProjectCount` stays 0; `bootstrap::tests` holds the pure resolver tests (`legacy_import_source_*`), and the positive twin `auto_import_reads_the_staged_app_data_file` proves the new `<app-data>/import/db.json` source imports (two projects from `fixtures/commissioning-sample-db.json`). (4) The feature gate had to extend to `E/lighting/legacy_import.rs` (`import_legacy_lighting_fixture` and its wire structs have no other caller) and to the `confidence_setting` / `ConsoleConfidence` re-export in `E/audio/mod.rs` (dead under `-D warnings` once `parity_fixtures` is compiled out; the audio writers use `helpers::` directly), and `resolve_runtime_paths` became fallible because the copied shell resolver is — the design did not spell either out; both sit inside the findings' intent.
Gates before: `tests/contract.rs` asserted only that a dispatch arm exists; nothing asserted the method's availability per build, the `replaceExistingData` default, the app-data default, the auto-import sources or the dev-server host; `scripts:test` was silently empty on the workstation.
Tests added: `parity_fixtures::tests::load_defaults_to_merge`, `load_honours_an_explicit_replace_flag` (feature build); `app::tests::parity_fixture_unavailable_without_feature` (release build — answers `METHOD_UNAVAILABLE`, no events, no fixture file written) and `parity_fixture_loads_with_feature` (feature build); `bootstrap::tests::default_app_data_dir_is_absolute_platform_dir`, `every_platform_default_mirrors_the_shell_layout`, `runtime_paths_refuse_to_guess_without_a_platform_base`, `runtime_paths_honour_explicit_overrides`, `legacy_import_source_prefers_the_explicit_path`, `legacy_import_source_reads_only_the_staged_app_data_file`, `legacy_import_source_is_disabled_by_env`; `tests/end_to_end.rs::auto_import_ignores_cwd`, `auto_import_reads_the_staged_app_data_file`; `scripts/frontend/dev-server-host.test.mjs` (three cases: Vite binds loopback; no server surface — Vite, Playwright, Storybook, Tauri dev, the two `package.json` — binds `0.0.0.0`; no helper script under `scripts/` does). Tests changed (old → new → why): `tests/end_to_end.rs::EngineProcess::spawn` → delegates to the new `spawn_with(label, configure)` with the same env and pipes (`SSE_DISABLE_AUTO_IMPORT=1` kept) → the two auto-import tests need a working directory and a staged file before the process starts; no assertion changed.
Lanes: `npm run rust:clippy`, `npm run native:test`, `cd native && cargo test -p studio-control-engine --features dev-fixtures`, `npm run protocol:check`, `npm run scripts:test`, `npm run tauri:setup-support:qualify` (proves `commissioning.seedPlanningDemo` still works), `npm run tauri:workspaces:qualify` (Rescope 1). Done when the release engine answers `METHOD_UNAVAILABLE`, the feature build passes, and `rg "0\.0\.0\.0" frontend scripts` hits only documented cases.
Validation (workstation, 2026-09-10): `cargo fmt` applied; `rust:clippy` clean under `-D warnings`, and `cargo clippy -p studio-control-engine --all-targets --features dev-fixtures -- -D warnings` clean; `native:test` 310 passed / 1 ignored (tauri-shell 12, engine 290 + 1 ignored hardware-bound, `contract.rs` 4, `end_to_end.rs` 4 — was 300 / 1); `cargo test -p studio-control-engine --features dev-fixtures` 292 + 4 + 4 passed; `protocol:check` clean (contract unchanged); `scripts:test` 85 tests, 81 passed, 4 skipped (0 ran on this workstation before); `lint`, `frontend:typecheck` (27 fixture scenarios), `format:check`, `file:health` clean; `node scripts/check-operator-copy.mjs` 0 hits; the stale `vite preview` on 4173 (PID 213120, started 2026-09-07) was stopped first; `tauri:setup-support:qualify` passed (31 s); `tauri:workspaces:qualify` passed (23 s) with the publish through the real gate (`overrideProbes: false` in the evidence; the audio probe verified against live TotalMix metering, the lighting probe server bound 127.0.0.1:80); `npm run dev:check` green end to end (65 s, warm caches). `rg "0\.0\.0\.0" frontend scripts` hits only `frontend/app/tests/setup.spec.ts` and the fixture transport's unreachable-bridge address (both documented in the guard) plus the guard itself. TotalMix FX and Companion untouched; nothing listened on 38201.
Baselines: no operator-visible change; the visual lanes were not run.
Operator hands: none.
CI (result, 2026-09-10): run 34472807896 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/34472807896), event `push`, sha `b9da9dc`, created within five seconds of the push; ten minutes end to end. Green: `lint` (0.4 min), `frontend-typecheck` (0.6), `frontend-test` (0.6), `format-protocol` (1.2 — `scripts:test` under bash, the double-quoted glob discovers all fourteen files as before (the thirteen existing plus the new guard)), `rust` (1.9 — rustfmt, clippy, check, test with the S1 tests, `native:acceptance`), `tauri-foundation` (2.5), and **`qualification` (6.8) — green for the first time on this stack**: the setup-support lane as before, and the workspace lane through Rescope 1 (all three probes run; on the runner the override is sent because `SSE_TAURI_QUALIFICATION_SKIP_AUDIO_PROBE=1` and port 80 is not bindable, and the evidence records it). Red, expected and unchanged from S0's run: `frontend-e2e` — 92 failed / 323 passed in 8.6 min, the same 73 stale + 18 missing linux snapshots and the same `tests/audio-arm-countdown.spec.ts:45` timing case (S13 quarantine candidate); S1 changed no rendered front-end source, so no new diff exists. The linux refresh stays Appendix B item 6 (the run's `playwright-test-results` artifact). The push also surfaced GitHub's Dependabot count for `main` (4 alerts: 1 high, 1 moderate, 2 low) — an input for S12's `npm audit` allowlist, not chased here.

## Slice 2 — Bridge hardening

Status: landed (operator verification pending) 2026-09-10 (commit `c2b7361` `Readiness S2: bridge token, request limits, worker pool, percent decoding, authenticated Stream Deck profile`, pushed; CI run 34487837771 recorded under \"CI (result)\" below by the follow-up commit `Readiness S2 (ledger): CI run on the branch recorded`, as in S0 and S1); every S2 lane and `npm run dev:check` green on the workstation; the hardware step is Appendix B item 1, which only the operator can walk. Closes F01 F06 F28. Depends on S0.

Scope (as landed): new `E/control_surface_http.rs` holds the bridge transport. `load_or_create_bridge_token(app_data_dir)` → `<app-data>/control-surface.token`, 64 hex characters from `getrandom` 0.3 (already in the lock file through the Tauri shell, so no crate was fetched), created on the first launch of an install and reused afterwards, `0600` on unix, a damaged file regenerated, `SSE_CONTROL_SURFACE_TOKEN` overriding it for lanes without touching the file; a token that cannot be written fails the bootstrap loudly instead of starting an unauthenticated bridge. `start_control_surface_bridge(db, log, port, token)`. `HttpRequest` carries lower-cased `headers`; `authorize(&request, token, port)` runs on every route before the body is parsed: any `Origin` → 403, `Host` outside {`127.0.0.1:<port>`, `localhost:<port>`} → 400, missing / wrong `Authorization: Bearer` (constant-time compare) → 401 with a `WWW-Authenticate: Bearer` challenge. `read_http_request` reads through a `RequestSource` (`TcpStream`; a `Cursor` in tests): 8 KiB header cap (431), 16 KiB body cap checked against the declared `Content-Length` before any body byte is read (413), POST without a usable `Content-Length` or any `Transfer-Encoding` → 411, conflicting `Content-Length` headers → 400, a 1 s deadline enforced through the socket read timeout and an `Instant` check → 408. `run_control_surface_bridge`: one acceptor, `sync_channel(16)`, four named workers; queue full → 503 written by the acceptor; a handler that panics is caught and logged, the worker carries on; refusals are logged at most once per status per minute; every response is followed by a write-shutdown and a bounded drain so a refused client reads its status instead of a reset. `query_parameter` percent-decodes names and values (`%XX`, `+`). Status texts 401 / 403 / 408 / 411 / 413 / 431 / 503. `E/control_surface.rs` keeps the deck handlers and `ControlSurfaceError` (seven new variants, `status_code` now `pub(crate)`) and shares its test fixtures as `test_support`. `E/bootstrap.rs`: `RuntimeContext.control_surface_token`, loaded before the bridge starts, never part of a snapshot. `E/exports.rs`: `generate_companion_config(base_url, surface, token)` walks the finished profile and sets the generic-http `header` option of every bridge action to `{"Authorization":"Bearer <token>"}` — keys, dials, per-action LCD refreshes and the 1 s LCD poll trigger. `scripts/native-control-surface-qualification.mjs`: reads the token file the packaged engine wrote, sends the header on every positive check, then proves the refusals — no token 401 with the challenge, wrong token 401, `Origin` 403 and foreign `Host` 400 over raw sockets, a 17 KiB body 413, `Content-Length: 99999999` 413, `Content-Length: 4000` with ten bytes held open → 408 with the first byte inside 1.5 s, `%2F` in `key` reaching the handler decoded, `%5F` resolving to the known key, and the refused `deleteProject` leaving the project count unchanged — and exports the profile through `exports.companion.export` to check that every bridge request in it carries the token. Docs: `docs/OPERATIONS.md` (the token paragraph under "Stream Deck Audio Surface", the 401 step under "bridge stops responding", the qualification section), `native/protocol/v1.md` (authentication, refusals, limits, decoding), `docs/RELEASE.md`, a CHANGELOG Changed bullet. `native/Cargo.toml`, `native/rust-engine/Cargo.toml`, `native/Cargo.lock`: the `getrandom` edge.
**Rescope:** four deviations, none silent. (1) The transport lives in a new module `E/control_surface_http.rs` instead of growing `E/control_surface.rs`: with its tests the single file would have passed the 2,000-line `file:health` guard — F26's own gate — and the split mirrors the existing `control_surface_audio.rs`; the deck handlers did not move. (2) The design put the header into `exports.rs::http_post` and `lcd_refreshes`; it landed as a walk over the finished profile (`apply_bridge_auth_header`), because those helpers have dozens of callers across `exports.rs` and `exports_audio.rs`, and the walk is what lets a test prove "every bridge request, and nothing else" — `companion_export_carries_the_bridge_token_on_every_request` counts the token once per bridge action in the serialized file. (3) The design's 408 lane case, `Content-Length: 99999999` with a short body, is a 413 under its own cap rule (a declared length over 16 KiB is refused before a body byte is read); the lane asserts exactly that, and drives the 408 with `Content-Length: 4000` held open — the stall the finding describes. (4) `worker_pool_returns_503_when_saturated` runs the real pool with one worker and one queue slot through `run_control_surface_bridge`'s parameters (production stays 4 / 16): saturating four workers and sixteen slots deterministically would need twenty idle sockets and a multi-second window in every test run.
Gates before: `native:bridge:win:verify` asserted that the bridge binds and answers the five routes; nothing asserted who may call it, how much it reads, or how many threads it starts; `query_parameter` replaced `%20` and nothing else; no test fed `read_http_request` a hand-built request, and `handle_control_surface_connection` spawned one thread per accepted connection with a 2 s read timeout and no body cap.
Tests added: `control_surface_http::tests` — `read_http_request_parses_headers_lower_cased_and_body`, `read_http_request_rejects_body_over_cap` (17 KiB and 99999999), `read_http_request_rejects_oversized_headers`, `read_http_request_rejects_post_without_content_length`, `read_http_request_rejects_chunked_transfer_encoding`, `read_http_request_times_out_on_short_body` (a stalling reader), `read_http_request_honours_the_deadline`, `read_http_request_rejects_a_truncated_body`, `authorize_rejects_missing_bearer`, `authorize_rejects_wrong_token` (wrong, truncated, extended, `Basic`, bare), `authorize_rejects_origin`, `authorize_rejects_foreign_host`, `authorize_accepts_loopback_hosts_with_the_token`, `authorization_precedes_body_parsing`, `query_parameter_decodes_percent`, `token_file_is_created_once_and_reused` (mode `0600` asserted on unix), `token_file_with_junk_is_regenerated`, `token_env_override_wins_and_leaves_the_file_alone`, `bridge_refuses_unauthenticated_requests_and_serves_authenticated_ones` (a real loopback bridge: 401 with the challenge, 401, 403, 400, 413, 408 on the deadline, 200 through `localhost`, `%2F` decoded, `%5F` served, no action stamped by any refused request), `worker_pool_returns_503_when_saturated` (503 from the acceptor while the pool is full, then a 200 once the idle connections time out); `exports::tests::companion_export_carries_the_bridge_token_on_every_request`. Tests changed (old → new → why): `exports::tests` — seven `generate_companion_config(base_url, surface)` calls → `generate_companion_config(base_url, surface, TEST_TOKEN)` → the export needs the token; no assertion changed. `control_surface::tests` — `TestDir` / `ready_audio_test_db` → `control_surface::test_support` (`pub(crate)`, plus a `path()` accessor) → shared with the new module's tests; no assertion changed. The four test `RuntimeContext` literals (`app.rs`, `app_state.rs`, `commissioning.rs`, `support.rs`) gain `control_surface_token`. The existing handler tests call the handlers directly and are unchanged.
Lanes: `npm run native:test`, `npm run rust:clippy`, `npm run scripts:test`, `npm run native:engine:build && npm run native:package:win:local && npm run native:bridge:win:verify`. Done when an unauthenticated POST → 401, the exported profile carries the header on every action and LCD poll, and the qualification is green including the refusals.
Validation (workstation, 2026-09-10): `cargo fmt` applied; `rust:clippy` clean under `-D warnings`; `native:test` 331 passed / 1 ignored (tauri-shell 12, engine 311 + 1 ignored hardware-bound, `contract.rs` 4, `end_to_end.rs` 4 — was 310 / 1; the twenty bridge tests take 2.6 s, the saturation case included); `scripts:test` 85 tests, 81 passed, 4 skipped (the `0.0.0.0` guard covers the new lane code); `format:check`, `lint`, `protocol:check` (contract unchanged) and `file:health` (`control_surface_http.rs` 1,346 lines, `control_surface.rs` 1,451) clean; `node scripts/check-operator-copy.mjs` 0 hits (the front-end did not change); `native:engine:build` → `native:package:win:local` (the packaged engine is the freshly built debug engine beside the 2026-09-04 shell exe; this lane does not exercise the shell) → `native:bridge:win:verify` passed in 1.7 s with the steps `packaged-engine-start`, `bridge-token-file`, `bridge-snapshot-contract`, `bridge-http-routes`, `bridge-request-refusals` (no token 401, wrong token 401, `Origin` 403, foreign `Host` 400, 17 KiB 413, 99999999 413, stalled body 408 with the first byte after 1,011 ms, `%2F` 400 naming the decoded key; project count 2 before and after the refused `deleteProject`) and `profile-carries-token` (272 bridge requests, 25 of them in the LCD poll trigger); `npm run dev:check` green end to end. The lane ran on the fixture data with no verified audio, so its audio branch was the gated-409 path, as before. Companion was running (the export bound the page-follow triggers to the physical deck); TotalMix FX untouched; nothing listened on 38201 (the lane reserves an ephemeral port).
Baselines: no operator-visible change; the visual lanes were not run.
Operator hands: **Appendix B item 1.** Companion still holds the profile exported before this slice, which carries no token; once this build runs on the workstation the app writes the token at its first start, and every request from the old profile is refused with 401 — the deck goes idle, the app itself is unaffected. Export the Stream Deck profile from Setup step 1, import it in Companion with Full Reset & Import, press one deck key and watch one LCD refresh, then sign item 1; S2 moves to `verified`. CHANGELOG and OPERATIONS say so in the operator's words ("Stream Deck profile"); the copy gate scans the front-end, which did not change, and holds at 0.
CI (result, 2026-09-10): run 34487837771 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/34487837771), event `push`, sha `c2b7361`, created within five seconds of the push; ten minutes end to end. Green: `lint` (0.5 min), `frontend-test` (0.5), `frontend-typecheck` (0.6), `format-protocol` (1.3), `rust` (2.2 — rustfmt, clippy, check, test with the twenty bridge tests and the export test on Linux, the two real-socket cases and the unix `0600` assertion included, plus `native:acceptance`), `tauri-foundation` (2.4), `qualification` (7.4 — the shell-started engines wrote their token files into the runners' temp app-data and both lanes passed as in S1). Red, expected: `frontend-e2e` — 94 failed / 321 passed in 8.7 min: the same 52 `visual-review` + 39 `storybook` linux snapshot failures as S0 and S1 (73 stale + 18 missing) and the same `tests/audio-arm-countdown.spec.ts:45` dwell case, plus two runner-timing cases in specs S2 did not touch: `tests/audio.spec.ts:1054` (the 48 V arm-then-apply — the second click landed after the dwell and `data-armed` read `false`; already on the S13 quarantine list) and `tests/lighting.spec.ts:721` (the rotate-handle drag landed at 0° instead of near 82°; a further S13 quarantine candidate, noted under Slice 13). S2 changed no rendered front-end source — `frontend-test` and `frontend-typecheck` are green and the failing titles differ from S1's run by exactly those two — so no new visual diff exists and the linux refresh stays Appendix B item 6.

## Slice 3 — Storage integrity + database backups

Status: verified 2026-09-10 (commit `e4c283d` `Readiness S3: integrity check at start, verified database backups, pre-migration copies, synchronous FULL, recovery copy`, pushed; CI run 34507456992 recorded under "CI (result)" below by the follow-up commit `Readiness S3 (ledger): CI run on the branch recorded`, as in S0–S2; every S3 lane and `npm run dev:check` green on the workstation; no hardware exposure — the backups accumulate on their own, and Appendix B item 4 is the operator's restore check once S7 lands the restore). Closes F02 F13 F21. Depends on S0.

Scope (as landed): `E/storage.rs` — `configure_connection` sets `synchronous = FULL`; a typed `StorageError { Corrupt { db_path, detail } | MigrationFailed { key, detail } }` travels boxed in `EngineResult` and is recovered by downcast; `initialize_database(db_path, backups_dir)` opens the file, runs `PRAGMA integrity_check(8)` and returns `StorageError::Corrupt` before any migration or default touches it when SQLite refuses the file (`SQLITE_NOTADB` / `SQLITE_CORRUPT` while opening or checking) or the check answers anything but `ok`. `E/storage_backups.rs` (new module) — `SnapshotReason { PreMigration, Daily, Shutdown, PreRestore }` (`as_str`, `retention` 5 / 14 / 3 / 5); `snapshot_database(db_path, backups_dir, reason)` → `VACUUM INTO backups/db-<YYYY-MM-DDTHH-MM-SS-mmmZ>-<reason>.sqlite3` on the live connection (a consistent, compacted copy; the UTC millisecond stamp is computed in-process so a corrupt database cannot stop the name, and the names sort in write order), then the copy is opened on its own, converted to `journal_mode = DELETE` (one self-contained file, no `-wal` / `-shm`), integrity-checked, flushed with `sync_all` (opened for write — `FlushFileBuffers` refuses a read-only handle on Windows), and deleted again if any of that fails; `prune_snapshots(backups_dir)` removes the oldest of each reason beyond its retention by name and leaves every other file alone (support archives included); `newest_snapshot(backups_dir)`. Back in `E/storage.rs`, `migrate_schema(connection, backups_dir)` takes the PreMigration copy on the same connection, after the idempotent base DDL and before the first migration step, whenever `0 < current_schema_version < STORAGE_SCHEMA_VERSION` — a brand-new database (version 0) has nothing to protect and gets none; `seed_lighting_palettes_v6` returns `StorageError::MigrationFailed { key: "app.lighting.editor.state" }` for unparsable JSON or a non-object value, so the v6 transaction rolls back and the schema version stays at 5. `E/bootstrap.rs` — `bootstrap_runtime` splits into `bootstrap_runtime_from_paths(RuntimePaths)` (testable without env or ports; a storage failure returns before the bridge starts); `StartupFailure { code: &'static str, message }` implements `Error`; `storage_startup_failure` maps `Corrupt` → `STORAGE_CORRUPT` and `MigrationFailed` → `STORAGE_MIGRATION_FAILED` (a code the design's error type implied but did not name) with a sentence that names the file, the newest database backup (or that none exists yet) and the way out ("Restore a backup from Setup / Support; the file itself was left untouched"), and writes an ERROR line to the engine log; `startup_failure_code(&dyn Error)` gives `main.rs` the code (`BOOTSTRAP_FAILED` for anything else). `E/main.rs` — the bootstrap failure event carries that code; `spawn_snapshot_scheduler` (named thread, first Daily copy 5 min after start, then every 24 h) and a Shutdown copy in the stdin-EOF path right after `app.shutdown()`; both log INFO / WARN and never fail the process. `FA/app/startup/startupHelpers.ts` — `getFailureTitle` answers "Saved data needs attention" and `formatFailureCode` "Saved data check failed" / "Saved data upgrade failed" for the two codes; the "Restore a backup from Setup / Support" sentence is the engine's, rendered verbatim as the display's sentence, so no other surface changed. `scripts/tauri-setup-support-qualification.mjs` — scenario `corrupt-db` as step 4/4: junk at `studio-control.sqlite3` in a fresh app-data → `lifecycle === "failed"` with `startupFailure.code === "STORAGE_CORRUPT"`, stage `bootstrap`, a sentence naming a backup, and the junk file byte-identical afterwards (evidence check `corrupt-db-reaches-recovery-surface`). Docs: CHANGELOG (Added), `docs/OPERATIONS.md` ("The app fails before the dashboard" with the manual database restore until S7; "Data Safety"), `native/protocol/v1.md` (the two codes in the error taxonomy). Test convenience: `storage::initialize_test_database(db_path)` (cfg(test)) derives the backups directory beside the database, as the runtime lays it out.
**Rescope:** two deviations, neither silent. (1) The design listed `E/storage.rs`, `E/bootstrap.rs`, `E/main.rs`, `FA/startup/startupHelpers.ts` and the lane script, on the premise that `main.rs` already emits `engine.startupFailed` with a code — true — and that the shell shows it. The first run of the new `corrupt-db` scenario showed the recovery surface reading `ENGINE_READY_TIMEOUT` while the engine's stderr carried the right sentence: `EC/transports/tauriTransport.ts` invoked `engine_start` without awaiting the `engine://event` listener registration, so an event the engine emits within milliseconds of its spawn was dropped (a Tauri event with no listener is not queued), and `EC/store/createShellStore.ts` would in any case have replaced a failure that arrived before the ready gate existed with a ready timeout ten seconds later. The existing recovery scenario never noticed because a blocked app-data directory fails inside the shell's `engine_start` itself; an engine-side `PROTOCOL_MISMATCH` would have been lost the same way. Both files are now in scope: the transport holds the registered listener before it starts the engine, and a failure the engine reported during the bootstrap short-circuits the ready gate and outranks whatever the bootstrap trips over afterwards. Guarded by `tauriTransport.test.ts` ("is listening for engine events before it starts the engine", "releases the listener when the shell refuses to start the engine") and the store test "keeps the engine-reported failure instead of a ready timeout"; the lane scenario proves the path end to end through the real shell. (2) `E/storage.rs` had reached 2,421 lines with the backup code and its tests — over the 2,000-line `file:health` guard, F26's own gate — so the backup code lives in a new module `E/storage_backups.rs` with its three tests, and the inline tests of `storage.rs` moved to `E/storage/tests.rs` (the `planning/tests.rs` / `audio/tests.rs` precedent); pure moves, no assertion changed: `storage.rs` 889 lines, `storage/tests.rs` 1,176, `storage_backups.rs` 410.
Gates before: `initialize_database` ran `PRAGMA integrity_check` after migrating and stored the answer in `StorageBootstrap.integrity_check` for the health summary; nothing acted on it, so a damaged database was opened, migrated in place and reported in a sentence. No test fed the storage layer a file that was not a database. `seed_lighting_palettes_v6` returned `Ok(())` on unparsable JSON and `migrate_schema_v3_to_v4_handles_malformed_editor_state_json` asserted exactly that. `configure_connection` set `synchronous = NORMAL` with no test on the pragma. No code path wrote a copy of the database; the only backups were the JSON support archives the operator exports by hand. The setup-support lane's recovery step covered a blocked app-data directory (`BOOTSTRAP_FAILED`, raised by the shell) only, and no test drove an engine-raised startup failure through the Tauri transport.
Tests added: `storage::tests` (`E/storage/tests.rs`) — `connection_uses_synchronous_full`, `initialize_refuses_corrupt_database` (a junk file and a database with a damaged second page, both left byte-identical), `migration_snapshots_before_upgrading_v5` (the copy holds version 5 without palettes; a second start writes no further copy), `blob_migration_failure_rolls_back` (`MigrationFailed` with the key, version stays 5, value untouched, the pre-migration copy present, the repaired database upgrades); `storage_backups::tests` — `snapshot_writes_verified_copy` (name, reason, `journal_mode = delete`, no `-wal` / `-shm`, integrity `ok`, the data and schema version present, the live database still in WAL, `newest_snapshot`), `prune_keeps_retention` (retention per reason, oldest removed by name, four bystander files untouched), `file_timestamp_is_utc_to_the_millisecond`; `bootstrap::tests` — `corrupt_db_yields_storage_corrupt` (code, the file and the newest backup in the sentence, the ERROR log line, no bridge started), `migration_failure_maps_to_its_own_code` (and a plain error stays `BOOTSTRAP_FAILED`); `frontend/app` `startupHelpers.test.ts` — `formats STORAGE_CORRUPT`, `formats STORAGE_MIGRATION_FAILED`, the generic title for other codes; `engine-client` `tauriTransport.test.ts` (2 cases) and `createShellStore.test.ts` "keeps the engine-reported failure instead of a ready timeout"; setup-support lane scenario `corrupt-db`. Tests changed (old → new → why): `migrate_schema_v3_to_v4_handles_malformed_editor_state_json` → `migrate_schema_refuses_malformed_editor_state_json_at_the_blob_step` — old: the whole migration succeeds despite the bad JSON and version 4 is recorded; new: versions 4 and 5 commit, the v6 blob step returns `MigrationFailed` for `app.lighting.editor.state`, the value is untouched and a pre-migration backup exists — because silently skipping an unreadable value is the F13 finding. `migrate_schema_is_idempotent` passes the backups directory (signature). `initialize_database_applies_planning_schema_and_defaults` gains two assertions (integrity `ok`; no backup for a brand-new database). The 72 test call sites of `initialize_database(db_path)` in eleven files became `initialize_test_database(db_path)`; no assertion changed.
Lanes: `npm run native:test`, `npm run frontend:test`, `npm run tauri:setup-support:qualify`. Done when a corrupt DB reaches the recovery surface, upgrading a v5 DB leaves `backups/db-*-pre-migration.sqlite3`, and `PRAGMA synchronous` reads 2. Operator copy says "backup", never "snapshot" (copy gate). Open risk (unchanged): if metering-thread writes stay frequent after S10, rescope F21 to `NORMAL` + more frequent backups.
Validation (workstation, 2026-09-10): `cargo fmt` applied; `rust:clippy` clean under `-D warnings`; `native:test` 340 passed / 1 ignored (tauri-shell 12, engine 320 + 1 ignored hardware-bound, `contract.rs` 4, `end_to_end.rs` 4 — was 331 / 1; the four end-to-end cases each exit through the new shutdown backup); `frontend:test` — `frontend/app` 62 tests in 10 files, `engine-client` 7 tests in 3 files (the transport file new); `frontend:typecheck` clean; `node scripts/check-operator-copy.mjs` 0 hits; prettier and eslint clean on every changed file. `tauri:setup-support:qualify` ran three times: the first run, on the design's file list alone, failed its new step 4 with `ENGINE_READY_TIMEOUT` while the engine's stderr carried the right sentence (the delivery gap recorded under **Rescope:**); after the transport and store fixes it passed all four steps, and it passed again on the final, split engine code — steps 1–3 as before (`clean-startup-routes-to-commissioning`, `support-section-toggle-persists`, `commissioning-publish-unlocks-dashboard`, `backup-export-creates-archive`, `diagnostics-export-writes-report`, `backup-restore-round-trips-native-support-backup`, `persisted-restart-restores-dashboard-state`, `bootstrap-failure-remains-operator-visible`) plus `corrupt-db-reaches-recovery-surface` (code `STORAGE_CORRUPT`, stage `bootstrap`, the sentence naming the file and that no backup exists yet, the junk file byte-identical afterwards); about ninety seconds per run, the JSON `backupCount` checks unaffected by the new `.sqlite3` files because the support listing counts `.json` archives only. `npm run dev:check` green end to end (format, lint, `scripts:test`, `file:health` — `storage.rs` 889 lines, `storage/tests.rs` 1,176, `storage_backups.rs` 410 — rustfmt, clippy, `protocol:check` with the contract unchanged, `frontend:typecheck`, `frontend:test`, `native:check`, `native:test`). TotalMix FX and Companion untouched; nothing listened on 38201 or 4173.
Baselines: no operator-visible layout change — the recovery display prints the new title in its existing word slot and the engine's sentence in the sentence slot; no fixture board carries a `STORAGE_CORRUPT` failure, so no ratchet or visual baseline moves; the visual lanes were not run.
Operator hands: none for this slice. The database backups accumulate under `<app-data>/backups` on their own; until S7 lands the Support surface's restore of a `.sqlite3` backup, `docs/OPERATIONS.md` ("The app fails before the dashboard") carries the manual restore, and Appendix B item 4 (after S7) is the operator's restore check.
CI (result, 2026-09-10): run 34507456992 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/34507456992), event `push`, sha `e4c283d`, created within five seconds of the push; ten minutes end to end. Green: `lint` (0.4 min), `frontend-test` (0.4 — the three new suites among them), `frontend-typecheck` (0.5), `format-protocol` (1.2 — `file:health` with `storage.rs` at 889 lines), `tauri-foundation` (2.0), `rust` (2.5 — rustfmt, clippy, check, the 340 tests on Linux with the corrupt-file, damaged-page, `VACUUM INTO`, write-handle `sync_all` and retention cases included, plus `native:acceptance`, whose engines now write shutdown backups), `qualification` (7.1 — the setup-support lane's four steps under xvfb, `corrupt-db` included, and the workspace lane). Red, expected: `frontend-e2e` — 94 failed / 321 passed in 8.8 min: the same 52 `visual-review` + 39 `storybook` linux snapshot failures as S0–S2 (73 stale + 18 missing), the same `tests/audio-arm-countdown.spec.ts:45` dwell case and the same `tests/audio.spec.ts:1054` 48 V arm-then-apply case (S13 quarantine list), plus one runner-timing case that differs from S2's run: `tests/audio-talkback.spec.ts:64` (`keyboard.down("t")` never read as pressed within 5 s under the headless runner — a focus/timing case in a spec S3 did not touch; noted under Slice 13 as a further quarantine candidate), while S2's `tests/lighting.spec.ts:721` rotate-handle case passed this time. S3 changed no rendered front-end source and no fixture path — the store change reacts only to an `engine.startupFailed` event, which no Playwright fixture emits — so no new visual diff exists and the linux refresh stays Appendix B item 6.

## Slice 4 — Shell hardening

Status: verified 2026-09-10 (commit `f3db990` `Readiness S4: async shell commands, duplicate-id refusal, content security policy, folder-bound open and export`, pushed; CI run 34514042594 recorded under "CI (result)" below by the follow-up commit `Readiness S4 (ledger): CI run on the branch recorded`, as in S0–S3; every S4 lane and `npm run dev:check` green on the workstation; no hardware exposure, so the slice goes straight to `verified` per the status vocabulary). Closes F07 F08 F15. Depends on S0.

Scope (as landed): `T/main.rs` — `EngineState.bridge` is an `Arc<EngineBridge>`; `off_main_thread(task)` awaits `tauri::async_runtime::spawn_blocking`, and the seven commands `engine_start`, `engine_request`, `engine_stop`, `engine_summary`, `shell_confirm_close`, `shell_open_path` and `shell_export_diagnostics` are `async fn` commands that hand their work to it (Tauri runs an `async fn` command on its async runtime, never on the thread that paints the window and dispatches the next IPC call; the blocking pool takes the wait). `shell_open_path`: `allowed_open_roots()` = the app-data directory (which holds `backups` and `exports`), the logs directory (it may sit elsewhere through `SSE_LOG_DIR`) and `SSE_UPDATE_REPOSITORY_PATH` when set; `resolve_open_path(target, roots)` canonicalises the target (symlinks, junctions, `..`) and requires it to sit under a canonicalised root — a root that does not exist admits nothing — answering `PATH_OUTSIDE_APP_DATA: <path> is outside the app data, logs and update folders, so it was not opened.` or `PATH_NOT_FOUND: …`; the original spelling goes to the platform opener because `canonicalize` yields a `\\?\` path on Windows that Explorer refuses. `shell_export_diagnostics(report)`: `write_diagnostics_report(<app-data>/exports, report)` names the file `diagnostics-<YYYY-MM-DDTHH-MM-SS-mmmZ>.json` (`file_timestamp` / `civil_from_days` mirrored from `storage_backups.rs`, so backups and exports sort together; a taken name waits 1 ms and retries, so two exports in one millisecond get two files) — no other directory, and no temp-directory fallback. `#[cfg(feature = "test-bridge")] shell_test_bridge_export_diagnostics_to(report, directory)` serves the lanes and is registered in the test-bridge handler list only. `T/engine.rs` — `EngineBridge::request` goes through the pending map's `entry`: an occupied id is answered with `error_response(id, "DUPLICATE_REQUEST_ID", …)` — a protocol error envelope, so the front-end throws its message — before anything is written, and the original waiter keeps its slot; `EXPORTS_DIR_NAME`, `exports_dir_for(app_data_dir)`; `start` creates `<app-data>/exports` beside the logs directory so the Diagnostics key can open it before the first export. `native/tauri-shell/tauri.conf.json` — `app.security.csp` exactly as designed; `app.security.devCsp` is the same policy widened for the Vite document (`http://127.0.0.1:4173` in `default-src` / `script-src` / `style-src`, `'unsafe-inline'` in `script-src` for the React refresh preamble, `http://127.0.0.1:4173` and `ws://127.0.0.1:4173` in `connect-src` for HMR). `scripts/tauri-smoke.mjs` — `verifyContentSecurityPolicy(config)`: `csp` present; `default-src 'self'`; `script-src 'self'` without `'unsafe-inline'`, `'unsafe-eval'`, `*`, `http:` or `https:`; `connect-src` with `ipc:` and `http://ipc.localhost`; no `*` in any directive; `devCsp` present with the IPC origins and the HMR socket. `EC/transports/tauriTransport.ts` — `createSessionNonce()` (12 hex characters from `crypto.randomUUID`, a base-36 fallback), a `sequence` per instance, `id = ${method}:${sequence}:${sessionNonce}`. `FA/app/shellCommands.ts` — `exportShellDiagnostics(report)` takes no directory (fixture path `diagnostics-fixture.json`); `openShellPath` and `exportShellDiagnostics` wrap a refused command's plain-string rejection into an `Error`, so the surfaces show the shell's sentence with its code instead of their generic line. `FA/app/tauriShellTestBridge.ts` — an `exportShellDiagnostics` command with a `directory` invokes `shell_test_bridge_export_diagnostics_to`, one without it the production command; `testBridge.heartbeat` counts up every 250 ms and rewrites the status file whatever the shell state does (every write is an IPC call the shell answers on its main thread); `testBridge.cspViolations` keeps the last twenty `securitypolicyviolation` events (blocked URI, directive, source file, line). `FA/app/setup/SetupSupportPilot.tsx`, `FA/app/setup/SetupRecoverySurface.tsx` — the export call drops its `logsDir` argument; the Diagnostics key opens `runtimePaths.exportsDir ?? appDataDir` (was `logsDir ?? appDataDir`). `E/bootstrap.rs` `EXPORTS_DIR_NAME`; `E/app_state.rs`, `E/app.rs`, `E/main.rs` — `runtime.paths.exportsDir` (`<app-data>/exports`) in the app snapshot, the health snapshot and both startup-failure payloads. `scripts/tauri-setup-support-qualification.mjs` — step 1 gains, before the publish, `pickUnroutedBridgeAddress()` (the first of `203.0.113.113`, `198.51.100.113`, `192.0.2.113`, `100.127.255.113` whose port-80 connect from Node is still pending after 1.2 s — RFC 5737 / RFC 6598 addresses no LAN or CI network hosts; if none stalls the lane fails with a sentence, it never skips) and `runStalledRequestCheck` (writes a `runCommissioningCheck` lighting probe against it, samples the status file every 50 ms until the command lands, then asserts the probe took ≥ 1 s and failed as unreachable, that ≥ 2 heartbeats landed while it was in flight, and that the longest gap between status updates — command write, each heartbeat change, command completion — stayed under 1 s), followed by a lighting probe against `127.0.0.1` (a refused connect counts as reachable) so the persisted bridge address points back at loopback; after the directory export a default export is asserted to land in `<app-data>/exports` as `diagnostics-<UTC ms>.json` (evidence checks `shell-stays-responsive-during-stalled-request` with `bridgeIp` / `probeMs` / `heartbeats` / `maxGapMs`, and `diagnostics-export-defaults-to-app-data-exports`). Docs: `native/tauri-shell/README.md` (a hardening paragraph), `native/protocol/v1.md` (`exportsDir`), `docs/OPERATIONS.md` (where the export lands), `docs/DEVELOPMENT.md` (the lane's coverage), a CHANGELOG Changed bullet.
**Rescope:** five deviations, none silent. (1) The seven commands are `async fn`s that await `spawn_blocking`, not sync functions under `#[tauri::command(async)]`: the macro runs a sync body marked `async` inside `async_runtime::spawn` (`sync_threadpool` in `tauri-macros`), i.e. on one of the runtime's few worker threads, so the 10 s wait in `EngineBridge::request` would pin a worker per stalled request — a store refresh fans several out (F11), enough to pin every worker and stall the remaining commands; `spawn_blocking` uses the blocking pool, which exists for exactly that. `State<'_, EngineState>` stays; the bridge sits in an `Arc` so the closure can own it. (2) The allowed roots include the update repository (`SSE_UPDATE_REPOSITORY_PATH`): both Support surfaces carry an "Update folder" key that opens it, which the design's list (app-data, logs, backups, exports) would have broken; backups and exports sit inside app-data, so the allowlist is app-data, logs and the update repository. (3) The Diagnostics keys had to follow the file: both surfaces opened `logsDir` as "Diagnostics" because the pilot passed `logsDir` as the export directory, and with exports moving to `<app-data>/exports` the engine now reports `runtime.paths.exportsDir` (four payload sites) and the keys open it, the shell creating the folder at engine start. Files the design did not list: `E/bootstrap.rs`, `E/app_state.rs`, `E/app.rs`, `E/main.rs`, `FA/app/setup/SetupSupportPilot.tsx`, `FA/app/setup/SetupRecoverySurface.tsx`, `scripts/tauri-smoke.mjs`, `scripts/tauri-setup-support-qualification.mjs`. The fixture transport was left alone — its recovery boards would otherwise gain a row and move a baseline — and can carry `exportsDir` when S7 lists exports. (4) `tauri:smoke` is not a webview: `scripts/tauri-smoke.mjs` spawns the engine over stdin/stdout and reads the config files; it never launches the shell and cannot see a blank webview. It now asserts the policy's presence and strictness, the durable guard the traceability row names; the behavioural proof for the packaged build was a one-off launch on the workstation (under Validation) of a release shell built with `--features test-bridge`, because only a release build serves the embedded assets under the policy: `is_dev()` is "no `custom-protocol` feature", `tauri dev` loads Vite's document from `devUrl`, and Tauri 2.11.4 injects a policy only through `get_asset` (the `tauri://` protocol) — `manager::csp()` is consulted nowhere else on desktop. The design's "one dev launch" therefore proves nothing about the policy; the qualification lane's dev launches prove the dev shell is unaffected, and `devCsp` is set so the dev document keeps a working policy should a Tauri update start injecting one. (5) The proof build and the production build share the release exe; the production `tauri:build` ran last, and a 12 s launch of that exe with the test-bridge environment wrote no status file, so the shipped shell carries no test bridge.
Gates before: nothing asserted which thread answered a command — `engine_request` sat in `recv_timeout(10 s)` on the main thread, so every IPC call, status write and window event queued behind a stalled engine reply (the previous shell froze the status file for 1,558 ms of a 1,995 ms probe when the new check ran against it); no test built two requests with one id (`tauriTransport.test.ts` held the two S3 cases only); `shell_open_path` handed any existing path to the platform opener and `shell_export_diagnostics` wrote to any absolute directory the webview named (a temp directory when no runtime paths resolved); `tauri.conf.json` had no `csp`; `tauri:smoke` checked the window count and the parity-fixture list only.
Tests added: `engine::tests::request_refuses_duplicate_pending_id` (the duplicate is answered with the code and the method, the original waiter still receives a reply routed by id, a fresh id fails on the write with no engine and leaves no slot); `shell_path_policy_tests::open_path_rejects_outside_app_data` (the roots, files beneath them and an update repository open; a sibling directory, a `..` climb, the temp root, `C:\Windows`, a missing path and an empty root list are refused with their codes), `diagnostics_report_lands_with_a_utc_stamp_and_never_overwrites`, `file_timestamp_is_utc_to_the_millisecond`; `tauriTransport.test.ts` "ids unique within one millisecond" (200 requests in one tick: all distinct, sequence 1…200, one nonce) and "a second transport instance carries its own session nonce"; `tauri:smoke` `verifyContentSecurityPolicy`; lane checks `shell-stays-responsive-during-stalled-request` and `diagnostics-export-defaults-to-app-data-exports`. Tests changed (old → new → why): `tauriTransport.test.ts`'s hoisted `invoke` mock → also records `engine_request` ids and answers an `ok` envelope → the new cases drive `request()`; the two S3 cases are unchanged. The lane's existing `exportShellDiagnostics` step keeps its `directory` and now reaches the shell through `shell_test_bridge_export_diagnostics_to`; its assertion is unchanged.
Lanes: `npm run native:test`, `npm run frontend:test`, `npm run native:engine:build && npm run tauri:build && npm run tauri:smoke`, `npm run tauri:setup-support:qualify`. Done when the status bridge keeps writing during a 1.5 s probe, duplicate ids are impossible, the CSP is present, and `shell_open_path("C:\\Windows")` errors — all four met (307 ms longest gap over a 1,933 ms probe; sequence + nonce ids and the shell-side refusal; policy present, asserted by the smoke and proved on the packaged build; the unit test refuses `C:\Windows`).
Validation (workstation, 2026-09-10): `cargo fmt` applied; `rust:fmt:check` clean; `rust:clippy` clean under `-D warnings`, and `cargo clippy -p sse-exed-tauri-shell --all-targets --features test-bridge -- -D warnings` clean; `native:check` clean; `native:test` 344 passed / 1 ignored (tauri-shell 16 — was 12 — engine 320 + 1 ignored hardware-bound, `contract.rs` 4, `end_to_end.rs` 4; was 340 / 1); `frontend:typecheck` clean (27 fixture scenarios); `frontend:test` — app 62 tests in 10 files, design-system 175 in 24, engine-client 9 in 3 (was 7), tokens 15 in 1; eslint and prettier clean on every changed file; `node scripts/check-operator-copy.mjs` 0 hits; `file:health` passed (`main.rs` 1,643 lines, `engine.rs` 722). `native:engine:build && tauri:build && tauri:smoke` passed (the release shell in 18 s on warm caches; the smoke's CSP assertion included). Packaged CSP proof: a release shell built with `--features test-bridge` (34 s) launched against a fresh app-data with the test-bridge environment reached `lifecycle` `ready` — the bundle executed and the IPC answered under the policy served on `http://tauri.localhost` — reported `runtime.paths.exportsDir` (present at engine start), published commissioning with the override, showed Lighting, Audio and Planning, wrote `exports/diagnostics-2026-09-10T18-16-19-854Z.json`, kept its heartbeat advancing (23 → 27 over one second) and reported `cspViolations` `[]`; the proof runtime held no scenes, so the `data:` thumbnails themselves were not drawn — the policy allows `data:` and the smoke asserts it stays. Then the production `tauri:build` (18 s) and the 12 s no-test-bridge launch of that exe. `tauri:setup-support:qualify` passed all four steps in 42 s with the pre-built debug shell: `shell-stays-responsive-during-stalled-request` (`bridgeIp` 203.0.113.113 — the first candidate stalled — `probeMs` 1933, `heartbeats` 9, `maxGapMs` 307), `diagnostics-export-defaults-to-app-data-exports` (`diagnostics-2026-09-10T18-18-14-224Z.json`) and the nine S1–S3 checks unchanged; run once more against the previous `main.rs` (the sync commands, byte-for-byte from `011fe44`) to prove the guard, the lane failed at the new check — "The shell's status file stopped updating for 1558 ms while a 1995 ms lighting probe was in flight" — and the new file was restored byte-identical and rebuilt before anything else ran. `npm run dev:check` green end to end. TotalMix FX and Companion untouched — Companion's LCD poll reached the proof engines' bridge on 38201 and was refused with 401 as S2 intends (Appendix B item 1 stays open); nothing listened on 4173 between lanes; the lane's temp runtime pointed its sACN target at the unrouted address for the two seconds between the two probes and at loopback afterwards, with no fixtures to render.
Baselines: no operator-visible layout change — the Diagnostics keys keep their labels and slots and only their target folder changed; the recovery display's reference list is data-driven and gains an `exportsDir` row on a live engine, but no fixture carries the key, so no board, ratchet or baseline moves; the visual lanes were not run.
Operator hands: none.
CI (result, 2026-09-10): run 34514042594 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/34514042594), event `push`, sha `f3db990`, created within five seconds of the push; ten and a half minutes end to end on the first attempt. Green on the first attempt: `lint` (0.4 min), `frontend-typecheck` (0.5), `frontend-test` (0.6 — the two new transport cases among them), `format-protocol` (1.0), `tauri-foundation` (1.9 — `tauri:smoke` with `verifyContentSecurityPolicy` on the Linux build), and `qualification` (7.1 — the setup-support lane's four steps under xvfb with the two new checks: `203.0.113.113` stalled on the runner too, the status file kept updating through the stalled probe and the default diagnostics export landed in `exports/`; the workspace lane as in S1–S3). Red on the first attempt, not expected: `rust` (3.1) — 319 passed / 1 failed / 1 ignored in the engine suite: `audio::tests_console_link::console_pull_that_never_goes_quiet_is_incomplete` (audit-remediation Slice 3; a fake TotalMix dump must still be streaming when a 500 ms pull times out, judged by a 150 ms quiet window) reached its `expect_err`, so the dump went quiet for the window on a runner where the whole engine suite took 28.65 s instead of the 8 s of the re-run; S4 touched no audio code, and the shell crate's 16 tests, `contract.rs` and `end_to_end.rs` passed on both attempts. The failed jobs were re-run on the same sha (`gh run rerun --failed`, attempt 2): `rust` green in 2.4 min (rustfmt, clippy, check, the 344 tests with the new shell cases on Linux — engine suite 8.14 s — and `native:acceptance`). Red, expected: `frontend-e2e` — attempt 1: 94 failed / 321 passed in 9.0 min, the same 52 `visual-review` + 39 `storybook` linux snapshot failures as S0–S3 (73 stale + 18 missing), the same `tests/audio-arm-countdown.spec.ts:45` dwell case and `tests/audio.spec.ts:1054` 48 V arm-then-apply case (S13 quarantine list), plus one runner-timing case not seen before, `tests/audio.spec.ts:702` (the simulated-metering movement mark; a fixture-mode spec S4 does not touch); attempt 2: 93 failed / 322 passed in 8.6 min — the 91 snapshot cases and the two listed cases only, `audio.spec.ts:702` green again (noted under Slice 13 as a further quarantine candidate, together with the Rust timing case). S4 changed no rendered front-end source under the fixture transport — the Diagnostics keys keep their labels and no board renders the export path — so no new visual diff exists and the linux refresh stays Appendix B item 6.

## Slice 5 — Engine supervision + single instance

Status: landed (operator verification pending) 2026-09-11 (commit `f1dc897` `Readiness S5: engine exit watcher, automatic restart, single instance` plus the lane follow-up `c4d849f` `Readiness S5 (lane): the second-instance refusal waits for the Linux shell's start-up`, pushed; both CI runs (34572635978 and 34574043349) are recorded under "CI (result)" below, the second by the follow-up commit `Readiness S5 (ledger): CI run on the branch recorded`, as in S0–S4; every S5 lane and `npm run dev:check` green on the workstation; the hardware step is Appendix B item 2, which only the operator can walk). Closes F09 F19. Depends on S4.

Scope (as landed): `T/engine.rs` — `EngineBridge.process` is an `Arc<Mutex<Option<EngineProcess>>>` and the bridge counts launches (`generations`); `EngineProcess` carries `generation`, `pid`, `expected_exit` and an `EventSink` (`Arc<dyn Fn(Value)>` — production wraps `app.emit("engine://event")`, the tests a channel), and its stdin is an `Option` so a stop can close the pipe while the process stays in the slot; `launch(command, binary_path, sink)` does the spawn (`start` resolves the binary and the directories, then calls it) and starts `spawn_exit_watcher(process, pending, generation)`: a named thread that sleeps `ENGINE_EXIT_WATCH_INTERVAL` (250 ms), locks the slot only for `child.try_wait()`, returns silently when the slot is empty or holds a newer launch, and on an exit takes the process out and calls `report_engine_exit` — which reaps the child, drains `pending` answering every waiter `error_response(id, "ENGINE_EXITED", "The hardware link stopped (exit status N) before it answered this request.")`, and emits `{type:"event",event:"engine.exited",payload:{status,graceful,generation,pid}}`; `stop()` = `stop_with_grace(ENGINE_STOP_GRACE)`: under the lock it sets `expected_exit` and drops stdin, then polls `try_wait` every 25 ms under short locks, kills after the grace, and whichever of it and the watcher takes the process out reports it — an exit is reported exactly once, and a confirmed close or a restart reports `graceful: true`; `write_request` answers "Engine is stopping" once stdin is closed; `EngineBootstrapSummary` gains `pid` and `generation` (`engine_start` and `engine_summary` answer them). `T/main.rs` — `tauri_plugin_single_instance::init(|app, _argv, _cwd| focus_main_window(app))` is the first builder call, before `manage` and `setup`; `focus_main_window` unminimises, shows and focuses the main window. `native/tauri-shell/Cargo.toml` — `tauri-plugin-single-instance = "2.4.4"` (crates.io was reachable; `native/Cargo.lock` grew by its tree — zbus for Linux, windows-sys for Windows). Protocol — `native/protocol/v1.contract.json` `events += "engine.exited"`, `EVENT_ENGINE_EXITED` in `native/protocol/rust/src/lib.rs` and `EVENT_NAMES`; `npm run protocol:generate` regenerated `native/protocol/generated/v1.schema.json` and `EC/generated/protocol.ts` (the ts-rs snapshot bindings are unchanged); `native/protocol/v1.md` documents the event and the codes `ENGINE_EXITED` / `ENGINE_ALREADY_RUNNING`. `E/bootstrap.rs` — `STARTUP_CODE_ENGINE_ALREADY_RUNNING`, `INSTANCE_LOCK_FILE_NAME` (`engine.lock`), `acquire_instance_lock(app_data_dir) -> Result<File, …>` (`OpenOptions` read + write + create, `File::try_lock`; `WouldBlock` → `StartupFailure { ENGINE_ALREADY_RUNNING, "Studio Control is already open on this workstation: another copy holds <path>. Close the other copy, then start again." }`; a lock error is a plain bootstrap failure, never a silent pass); `bootstrap_runtime_from_paths` takes the lock right after the directories exist and before `initialize_database`, logs `Instance lock refused (ENGINE_ALREADY_RUNNING): …` on refusal, and keeps the handle in `static INSTANCE_LOCK: Mutex<Option<(PathBuf, File)>>` for the life of the process (`release_instance_lock_for` drops an earlier hold on the same directory so the engine's own tests can bootstrap one directory twice; a real engine bootstraps once); `E/main.rs` needed no change — the failure flows through `startup_failure_code` into `engine.startupFailed {stage:"bootstrap", code:"ENGINE_ALREADY_RUNNING"}` and the engine exits non-zero. `EC/types.ts` — `EngineLaunchInfo {generation, pid}`, `BackgroundFailure {at, context, message}`, `EngineTransport.initialize?(): Promise<void | EngineLaunchInfo>`, `ShellState.backgroundFailures`, `ShellStore.reportBackgroundFailure(error, context?)`; `EC/index.ts` exports the two types. `EC/transports/tauriTransport.ts` — `initialize()` resolves with `{generation, pid}` from the `engine_start` summary (the fixture transport is untouched and resolves with nothing). `EC/store/createShellStore.ts` — `engineGeneration` per bootstrap from the launch info; `engine.exited` is handled before the refresh path and ignored when `graceful`, when its `generation` differs from the current one, or when the engine already reported a start-up failure for this bootstrap (the exit that follows `ENGINE_ALREADY_RUNNING` or `STORAGE_CORRUPT` says nothing new and must not restart an engine that would only refuse again); otherwise `lifecycle: "failed"`, `recovery: "recovery"`, `startupFailure {code:"ENGINE_EXITED", stage:"runtime", message}` — "The hardware link stopped unexpectedly (exit status N)." followed by the restart plan — the ready gate is rejected and the meter frame cleared; `scheduleAutomaticRestart`: at most `AUTOMATIC_RESTART_LIMIT` (3) automatic restarts per `AUTOMATIC_RESTART_WINDOW_MS` (5 min) with `AUTOMATIC_RESTART_BACKOFF_MS` 1 / 2 / 4 s ("Studio Control restarts it on its own in N s (attempt K of 3)."), then "It stopped 3 times within five minutes, so it is not restarted again on its own; use Retry startup once the desk and the rig are ready."; `restartEngine()` (shared by `restart()` and the timer) cancels a pending automatic restart, as do `dispose()` and every bootstrap; `recordBackgroundFailure` keeps the last `BACKGROUND_FAILURE_LIMIT` (20) `{at, context, message}` in `state.backgroundFailures`, which survives a restart; the six `void refreshDomain(..)` / `void refreshAudioSnapshot(..)` sites carry `.catch(inBackground("<context>"))`. `FA/main.tsx` — creates the environment, forwards `window` `error` and `unhandledrejection` to `store.reportBackgroundFailure`, and passes the environment to `OperatorShell` (`FA/app/OperatorShell.tsx` takes an optional `environment` prop and creates its own otherwise, as the stories do). `FA/app/startup/startupHelpers.ts` — `getFailureTitle`: "The hardware link stopped" / "Studio Control is already open"; `formatFailureCode`: "Hardware link stopped" / "Already open"; `formatFailureStage("runtime")` → "running". `FA/app/tauriShellTestBridge.ts` — `testBridge.enginePid` / `engineGeneration` from the summary; the diagnostics report gains `backgroundFailures`. `scripts/tauri-setup-support-qualification.mjs` — steps 5 and 6: `engine-crash` (a fresh runtime; `taskkill /PID <enginePid> /F` on win32, `SIGKILL` elsewhere; `startupFailure.code === "ENGINE_EXITED"` with stage `runtime` and the restart sentence within `ENGINE_CRASH_DETECT_MS` = 2 s; then `lifecycle === "ready"` with a new `enginePid` and `engineGeneration + 1`; evidence `engine-crash-reaches-recovery-and-restarts` with `detectMs`, `restartMs` and both pids) and `second-instance` on the same running shell (the debug shell binary `native/target/debug/sse-exed-tauri-shell[.exe]` launched directly with the same environment and its own status file; either it exits with code 0 within `SECOND_INSTANCE_EXIT_MS` = 5 s — `refusedBy: "shell-single-instance"`, required on win32 and darwin — or, on linux only, it stays up and its status reads `ENGINE_ALREADY_RUNNING` — `refusedBy: "engine-lock"`; the first shell must still be ready with the same engine and a moving heartbeat; evidence `second-instance-is-refused`). Docs: `native/tauri-shell/README.md` (a supervision paragraph), `native/protocol/v1.md`, `docs/OPERATIONS.md` (Restart / recovery bullets; "The app fails before the dashboard" step 4), `docs/DEVELOPMENT.md` (the lane's coverage), a CHANGELOG Added bullet.
**Rescope:** three deviations, none silent. (1) The exit is reported by whichever of the watcher and `stop()` takes the process out of the slot, not by the watcher alone: `stop()` has to close stdin, wait and kill anyway, and a watcher-only report would have added up to one poll interval to every close and restart and needed a fallback for a slot with no watcher; `expected_exit` still goes up before stdin closes, exactly as designed, so the watcher reports `graceful: true` when it wins the race. The audit Slice 11 helper `wait_for_exit` and its test went with it (Tests changed). (2) The store learns the shell's generation from `initialize()` resolving with the `engine_start` summary — the design's first option; the fixture transport is untouched and resolves with nothing, and a store without a generation still handles `engine.exited` (only a known, different generation is ignored). (3) The `second-instance` scenario accepts two refusals — the plugin's (the second shell exits within 5 s, required on Windows and macOS) and the engine lock's (the second shell reaches the recovery surface as `ENGINE_ALREADY_RUNNING`, accepted on Linux only) — because `tauri-plugin-single-instance` 2.4.4 on Linux registers a D-Bus name and, when the session bus cannot be reached, ignores the error and runs unprotected (the `_ => {}` arm of its `platform_impl/linux.rs`); the CI qualification job runs under `xvfb-run` with no session bus of its own, so the engine lock is the guard there — the guard the design named for the double use of the database and the light outputs. The evidence records which refusal happened. The plugin fetch succeeded (crates.io reachable), so the design's lock-file-only rescope was not needed. Files the design did not list: `EC/types.ts`, `EC/index.ts`, `EC/transports/tauriTransport.ts` (+ test), `FA/app/OperatorShell.tsx`, `FA/app/tauriShellTestBridge.ts`, `native/rust-engine/tests/end_to_end.rs`, `native/Cargo.lock`, the docs. (4) After the first CI run: the 5 s bound on the second shell's refusal holds on Windows and macOS only. On the Linux runner the second copy was refused by the plugin — it exited with code 0 — but about 30 s after launch, because a Tauri shell on Linux goes through GTK's start-up before any plugin runs, and under xvfb that start-up waits ~30 s on the AT-SPI bus lookup (the first shell of every lane step shows the same delay); the 5 s bound sent the lane into its engine-lock branch, which then reported the exit as "exited early". The lane follow-up commit makes the Linux bound the lane's wait timeout (`SSE_TAURI_QUALIFICATION_TIMEOUT_MS`, 180 s on CI, 40 s by default) and polls for whichever refusal comes first — the plugin's clean exit or `ENGINE_ALREADY_RUNNING` in the second shell's status file — recording which in the evidence; the Windows and macOS bound stays 5 s, and on those platforms an engine-lock refusal fails the lane. `docs/DEVELOPMENT.md` says so.
Gates before: nothing watched the engine process — a killed engine left every `engine_request` waiting for its ten-second timeout, `engine_summary` reporting `running: true` for a process that was gone, and the front-end painting the dashboard; `engine_start` reported no `pid` for a lane to end; no test or lane started two engines against one directory, and nothing stopped a second shell; the store's background refreshes were `void` promises whose rejections vanished; `initialize()` discarded the `engine_start` result.
Tests added: `engine::tests::exit_watcher_fails_pending_and_emits_event` (a short-lived process; the waiter gets `ENGINE_EXITED`, the event carries status 0 / graceful false / generation 1 / the pid, the slot and the pending map are empty afterwards, the report is single), `engine::tests::stop_reports_a_graceful_exit_and_kills_a_lingering_engine` (a `sort` that exits on stdin EOF is reported graceful well inside a 10 s grace; a `ping` that ignores stdin is killed after a 200 ms grace and reported once); `bootstrap::tests::second_lock_holder_is_refused`, `bootstrap::tests::bootstrap_refuses_a_held_app_data_dir` (refused before the database exists, `ERROR … ENGINE_ALREADY_RUNNING` in the log); `end_to_end::second_engine_on_the_same_app_data_dir_is_refused` (a second engine on the first's directory emits `engine.startupFailed` `ENGINE_ALREADY_RUNNING` / `bootstrap` and exits non-zero; after the first shuts down a third starts on the same directory); `createShellStore.test.ts` "engine.exited → recovery + bounded restart" (fake timers: 1 / 2 / 4 s, four launches, the fourth stop stays failed for a further 60 s, `restart()` by hand still works), "stale generation ignored" (generation 0, a graceful exit and an exit after `ENGINE_ALREADY_RUNNING` change nothing and start nothing), "keeps the last twenty background failures without leaving ready"; `tauriTransport.test.ts` "resolves initialize with the launch the shell reported"; `startupHelpers.test.ts` "formats ENGINE_EXITED", "formats ENGINE_ALREADY_RUNNING"; lane checks `engine-crash-reaches-recovery-and-restarts` and `second-instance-is-refused`; `tests/contract.rs` green with the new event. Tests changed (old → new → why): `engine::tests::wait_for_exit_sees_a_prompt_exit_and_gives_up_on_a_lingering_child` (audit Slice 11) → removed with `wait_for_exit`, its two behaviours asserted through `stop_with_grace` in the new stop test → the stop no longer waits on the child through that helper; `tauriTransport.test.ts`'s `invoke` mock → answers `engine_start` with a summary (`pid` 4242, `generation` 3) → the new case reads it; the S3 and S4 cases are unchanged.
Lanes: `npm run protocol:generate && npm run protocol:check`, `npm run native:test`, `npm run frontend:test`, `npm run tauri:setup-support:qualify`. Done when a killed engine reaches recovery within 2 s and restarts on its own, a second engine exits `ENGINE_ALREADY_RUNNING`, and a second shell focuses the first — all three met (recovery 219 ms after the kill and the engine back in 1,313 ms with a new pid; `second_engine_on_the_same_app_data_dir_is_refused` end to end, plus the lane's `engine.lock` path for Linux; the second shell handed over and exited with code 0 in 113 ms while the first stayed ready).
Validation (workstation, 2026-09-11): `cargo fmt` applied; `rust:fmt:check` clean; `rust:clippy` clean under `-D warnings`, and `cargo clippy -p sse-exed-tauri-shell --all-targets --features test-bridge -- -D warnings` clean; `native:check` clean; `native:test` 348 passed / 1 ignored (tauri-shell 17 — was 16: the `wait_for_exit` case out, the two supervision cases in — engine 322 + 1 ignored hardware-bound — was 320 — `contract.rs` 4, `end_to_end.rs` 5 — was 4; was 344 / 1); `protocol:generate` regenerated the two event lists and nothing else, `protocol:check` clean; `frontend:typecheck` clean (27 fixture scenarios; an earlier pass of the session had failed on `OperatorShell.stories.tsx` while `OperatorShell` carried a `= {}` default parameter, which collapses Storybook's inferred `args` to `never` — the default is gone, every prop is optional); `frontend:test` — app 64 tests in 10 files (was 62), design-system 175 in 24, engine-client 13 in 3 (was 9), tokens 15 in 1; eslint and prettier clean on every changed file; `node scripts/check-operator-copy.mjs` 0 hits; `file:health` passed (`main.rs` 1,666 lines, `engine.rs` 1,004, `createShellStore.ts` 1,365, `bootstrap.rs` 925, the lane script 916). `tauri:setup-support:qualify` passed all six steps in 60 s with the pre-built debug shell (evidence `summary.json`, `githubSha` c126b10): the S1–S4 checks unchanged (`shell-stays-responsive-during-stalled-request` `probeMs` 1931 / `heartbeats` 9 / `maxGapMs` 251), `engine-crash-reaches-recovery-and-restarts` (`taskkill /PID 9268 /F`; `ENGINE_EXITED` on the recovery surface after `detectMs` 219; `ready` again with pid 57052 and launch 2 after `restartMs` 1313), `second-instance-is-refused` (`refusedBy` `shell-single-instance`, exit code 0 after `refusalMs` 113; the first shell still ready with the same engine and a moving heartbeat). The two new checks cannot pass against the previous shell by construction — it reported no `enginePid` to end and raised no `engine.exited` — so no old-shell run was made. After the lane follow-up (the Linux bound and the whichever-comes-first poll) the lane was run again on the workstation: all six steps passed in 48 s, `engine-crash-reaches-recovery-and-restarts` `detectMs` 334 / `restartMs` 1424 (pid 213820 → 219996), `second-instance-is-refused` `refusedBy` `shell-single-instance` after `refusalMs` 102 with exit code 0, `shell-stays-responsive-during-stalled-request` `probeMs` 1864 / `heartbeats` 8 / `maxGapMs` 250; `npm run dev:check` green again before the follow-up commit. `npm run dev:check` green end to end (51 s on warm caches). TotalMix FX and Companion untouched — nothing wrote to 7001–7010; Companion's LCD poll against the lane engines' bridge on 38201 is refused with 401 as S2 intends (Appendix B item 1 stays open); nothing listened on 4173 or 38201 between lanes; the lane runtimes were temp directories, deleted afterwards.
Baselines: no operator-visible layout change on any fixture board — the new recovery titles and codes render only on a live engine, no fixture carries `ENGINE_EXITED` or `ENGINE_ALREADY_RUNNING`, and `OperatorShell` without the new prop builds the same environment it did; no board, ratchet or baseline moves; the visual lanes were not run.
Operator hands: Appendix B item 2 — end the engine process during a session (Task Manager → `studio-control-engine.exe`): the display reads THE HARDWARE LINK STOPPED and the dashboard is back within seconds; then launch a second copy of the app while the first runs: the running window comes to the front and no second window appears.
CI (result, 2026-09-11): run 34572635978 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/34572635978), event `push`, sha `f1dc897`, created within five seconds of the push; eleven and a half minutes end to end. Green on the first attempt: `lint` (0.4 min), `frontend-typecheck` (0.6), `frontend-test` (0.6 — the four new engine-client cases and the two app cases among them), `format-protocol` (1.2 — `protocol:check` with the new event), `rust` (4.2 — rustfmt, clippy, check, the 348 tests on Linux with `second_engine_on_the_same_app_data_dir_is_refused` and the two shell supervision cases, and `native:acceptance`), `tauri-foundation` (4.2 — the shell built with the plugin's zbus tree on Linux, `tauri:smoke` unchanged). Red on the first attempt, not expected: `qualification` (7.0) — the setup-support lane passed steps 1–5 under xvfb (`shell-stays-responsive-during-stalled-request` `probeMs` 2265 / `heartbeats` 10 / `maxGapMs` 269; `engine-crash-reaches-recovery-and-restarts` `detectMs` 412 / `restartMs` 1437, pid 12126 → 12148: the runner's shell reached ready 34 s after its launch, and the recovery and the automatic restart then took under two seconds together) and failed step 6 with "Tauri shell exited early during 'second shell refused by the engine lock' with code 0" — the second copy was refused by the plugin, exit code 0, about 30 s after launch, past the 5 s bound (see **Rescope** (4)); the workspace lane did not run. Fixed by the lane follow-up commit, whose run is recorded below. Red, expected: `frontend-e2e` — 92 failed / 323 passed in 8.6 min: the 91 linux snapshot cases (73 stale + 18 missing) and the constant `tests/audio-arm-countdown.spec.ts:45` dwell case; no runner-timing case this run. S5 changed no rendered front-end source under the fixture transport, so no new visual diff exists and the linux refresh stays Appendix B item 6.
CI (result, 2026-09-11, lane follow-up): run 34574043349 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/34574043349), event `push`, sha `c4d849f`, created within five seconds of the push; ten and three-quarter minutes end to end. Green on the first attempt: `lint` (0.4 min), `frontend-typecheck` (0.4), `frontend-test` (0.5), `format-protocol` (1.5), `rust` (1.8), `tauri-foundation` (2.4) and `qualification` (8.2 — the setup-support lane's six steps under xvfb in 247 s: `engine-crash-reaches-recovery-and-restarts` `detectMs` 209 / `restartMs` 1329, pid 12035 → 12057; `second-instance-is-refused` `refusedBy` `shell-single-instance`, exit code 0 after `refusalMs` 25088 — the second copy's GTK start-up under xvfb, as **Rescope** (4) describes, then the plugin's hand-over, so the runner does reach a D-Bus session bus and the engine-lock branch was not needed; `shell-stays-responsive-during-stalled-request` `probeMs` 2085 / `heartbeats` 9 / `maxGapMs` 272; the workspace lane in 90 s as in S1–S4). Red, expected: `frontend-e2e` — 93 failed / 322 passed in 8.5 min: the 91 linux snapshot cases, the constant `tests/audio-arm-countdown.spec.ts:45` dwell case and `tests/audio.spec.ts:1054` (already on the Slice 13 quarantine list; seen again on this run); no new candidate. The `frontend-e2e` shape is unchanged from S0–S4 and the linux refresh stays Appendix B item 6.

## Slice 6 — OSC ingress

Status: landed (operator verification pending) 2026-09-11 (commit `bee067b` `Readiness S6: loopback bind for the TotalMix meter ports, console-only sources, dropped-datagram log`, pushed; CI run 34593996173 recorded under "CI (result)" below by the follow-up commit `Readiness S6 (ledger): CI run on the branch recorded`, as in S0–S5; every S6 lane and `npm run dev:check` green on the workstation; the hardware step is Appendix B item 3, which only the operator can walk). Closes F05. Depends on S0.

Scope (as landed): `E/rme_totalmix_osc.rs` — `ReceivePolicy { console: IpAddr, bind_host: IpAddr }` with `for_console(console, bind_override)`: the bind host is the override when `SSE_OSC_BIND_HOST` names an IP address, else the loopback address of the console's family when the console address is loopback and the unspecified address (`0.0.0.0` / `::`) otherwise; `resolve_console_address(send_host)` turns the commissioned TotalMix address into that console address (`localhost` → `127.0.0.1`, a literal address → itself, anything else through the resolver the send path already uses; an empty or unresolvable host → `None`); `parse_bind_override(raw)` (unset or empty → no override, an IP address → the override, anything else refused with the sentence the thread logs); `accept_source(source: SocketAddr, expected: IpAddr) -> bool` (the console's own address only; an IPv4 console seen through an IPv4-mapped IPv6 address counts as itself); `DroppedSourceLog` (a per-source `Instant` map, `DROPPED_SOURCE_LOG_INTERVAL` 60 s, at most `MAX_TRACKED_DROP_SOURCES` 256 addresses remembered, one WARN line through `append_log` — "RME TotalMix OSC datagram from <source> dropped on receive port <port>: only the TotalMix address <console> is read; further drops from this source are noted once a minute" — the call site S8 moves to `log_event`); `bind_receive_socket(bind_host, port)` (the one non-blocking bind every slot shares) and `local_port_of`; `BoundRmeSlot` and `GlobalOscSlot` carry `console: IpAddr`; `bind_slots(policy, send_port, receive_port)` and `bind_global_slot(policy, …)` bind the policy's host (a classic bind failure still goes to stderr, now naming the bind host; the former separate non-blocking failure message is folded into it); `read_available_packets(…, drops)` and `read_global_packets(…, drops)` apply `accept_source` to every `recv_from` result before decoding — a foreign datagram is recorded and skipped, and never counts as Global slot activity (`last_rx_at`); `spawn_rme_totalmix_audio_metering(sender, db_path, log_file_path)` reads the override once at thread start (a malformed value is logged WARN and the rule applies), keeps one `DroppedSourceLog`, and at every (send host, ports) rebind resolves the console address — an unresolvable TotalMix address binds nothing and logs a WARN once per key instead of listening on every interface for anyone; `bind_test_global_slot` binds loopback with a loopback console, and the hardware lane's `bind_live_global_slot_for_test` follows the engine's rule (console `127.0.0.1`, the override honoured). `E/main.rs` passes `log_file_path` to the metering thread. `E/commissioning.rs::probe_audio_transport` is unchanged: its send socket stays `0.0.0.0:0`, and the persisted TotalMix address (`app.commissioning.audio.send_host`, `127.0.0.1` on the workstation) is the address the metering thread reads. The module is split by remote generation, as the file-health allowlist reason asked of the next OSC feature: `E/rme_totalmix_osc/classic_eq.rs` (the classic page-2 EQ command path — `send_totalmix_eq_update` and the `totalmix_eq_*` scales; 153 lines), `E/rme_totalmix_osc/global_commands.rs` (the Global OSC channel and output-mix commands, `TotalMixSendReport` and the surface ↔ hardware maps; 236 lines), `E/rme_totalmix_osc/tests.rs` (the module's tests; 1,454 lines), the parent at 1,687 lines re-exporting every public name so no caller changed; `send_osc_messages`, the cfg(test) console-port guard, `validated_command_port` and both metering paths stay in the parent because the two generations share the meter state and the send socket. `scripts/file-health.mjs` — the `rme_totalmix_osc.rs` allowlist entry is gone; the module is held at 2,000 lines like any other file. Docs: `docs/OPERATIONS.md` (the metering checklist names the IP on every remote, and a paragraph on the loopback bind, the dropped-datagram log line and the `netstat` check), `docs/DEVELOPMENT.md` (the bind rule, `SSE_OSC_BIND_HOST`, the ephemeral-port discipline of the tests, the module layout; the hardware lane now binds `127.0.0.1:9004`), a CHANGELOG Changed bullet.
**Rescope:** none in what the slice does — the design's bind rule, `accept_source`, the override and the once-a-minute WARN landed as written, and the title stands. Two things the design did not list, recorded here rather than done silently: (1) the split of `rme_totalmix_osc.rs` (2,892 lines, allowlisted with the reason "split by remote generation when the next OSC feature touches it") — the tests moved out first, and with the ingress code the parent still stood at 2,038 lines, over the guard, so the two command paths moved to `classic_eq.rs` and `global_commands.rs` as pure moves (visibility only: `pub(super)` on the two helpers the tests name, `pub use` / `pub(crate) use` re-exports in the parent); the metering paths of both generations stay together, which is what remains of F26 on this module for S14 to judge — the allowlist entry itself is gone, so `file:health` now holds the module at the cap. (2) The metering thread takes the engine log path as a new parameter so the WARN goes through `append_log` and not an eighth `eprintln!` (the module has six now, one of them the cfg(test) guard's; S8 replaces them).
Gates before: the four receive sockets bound `0.0.0.0` and both readers discarded `recv_from`'s source (`_source`), so any host that could reach the workstation's UDP ports 9001–9004 could feed the meters and — through the Global slot — the console link, which applies what "the desk" reports to the stored audio state; no test bound the real slot set or checked a source address; the hardware-lane helper bound `0.0.0.0` too.
Tests added (`E/rme_totalmix_osc/tests.rs`): `accept_source_rejects_foreign_ip` (loopback, LAN and IPv6 cases, the IPv4-mapped form), `receive_policy_binds_loopback_for_a_loopback_console_and_every_interface_otherwise` (the rule for both families, the override, `resolve_console_address` for `localhost` / a literal / blanks, `parse_bind_override` accepting an address and refusing a name with the variable named), `receive_sockets_bind_loopback_for_loopback_console` (the three classic slots and the Global slot bound through `bind_slots` / `bind_global_slot` on a free four-port base ≥ 7011 taken from a throwaway ephemeral socket — every socket on `127.0.0.1` at base..base+3, send ports 7011–7014 stored and never sent to), `foreign_datagrams_are_dropped_and_logged_once_per_minute` (a Global slot commissioned for a LAN console receives three loopback datagrams: nothing reaches the meter state, `last_rx_at` stays unset, exactly one WARN line naming the source, the receive port and the console; the same datagram with the console set to loopback is applied and logs nothing), `classic_slots_read_the_console_address_only` (the same for `read_available_packets` with a `/1/level1Left` message), `dropped_source_log_notes_a_source_once_a_minute` (per address not per port, a second source, the minute boundary, the no-log-path limiter, the 256-source cap). Tests changed (old → new → why): `send_slot_keepalives_pins_each_slot_to_its_bus_and_bank_start`, `service_console_link_reads_back_over_the_global_slot_and_confirms`, `refresh_global_slot_sends_sendall_and_sendstate_to_the_slot_port` → the slot literals carry `console: LOOPBACK` and the reader call passes a `DroppedSourceLog` → the structs gained the field and the reader the parameter; no assertion changed. The 27 existing module tests moved to `rme_totalmix_osc/tests.rs` unchanged (two of them import the classic helpers from `classic_eq`).
Lanes: `npm run native:test`, `npm run rust:clippy`, then `npm run tauri:setup-support:qualify` and `npm run tauri:workspaces:qualify` (the only lane that receives real TotalMix metering on this PC). Done when `netstat -an | findstr 900` shows `127.0.0.1:9001-9004` while an engine with TotalMix metering runs and a foreign datagram is logged and dropped — both met (below).
Validation (workstation, 2026-09-11): before any change, TotalMix FX 2.1 (pid 30764) held `0.0.0.0:7001` and `0.0.0.0:7004` and was sending from connected UDP sockets to `127.0.0.1:9001` and `127.0.0.1:9004`, and the operator database carried `app.commissioning.audio.send_host = 127.0.0.1` with send port 7001 and receive port 9001 — so the remote IP TotalMix sends to is loopback and the loopback bind is the right one for this workstation (design trap 3, settled from the live sockets; TotalMix's preferences file does not record the remote IP). Baseline before editing: `native:test` 348 passed / 1 ignored, `rust:clippy` clean. After: `cargo fmt` applied; `rust:clippy` clean under `-D warnings` (one `manual_range_contains` in the new test helper fixed on the way); `native:test` 354 passed / 1 ignored (tauri-shell 17, engine 328 + 1 ignored hardware-bound — was 322 — `contract.rs` 4, `end_to_end.rs` 5); the module alone 33 tests green (`cargo test -p studio-control-engine --bin studio-control-engine -- rme_totalmix_osc::`); `file:health` passed with the module's allowlist entry removed (`rme_totalmix_osc.rs` 1,687 lines, `classic_eq.rs` 153, `global_commands.rs` 236, `tests.rs` 1,454, `main.rs` 567). The debug test-bridge shell was pre-built (`cargo build --features test-bridge`), the production app was closed (nothing on 38201, 4173 or 9001–9004; port 80 free for the lighting probe server). `tauri:setup-support:qualify` passed all six steps in 53 s (evidence `summary.json`, `githubSha` eaa3cc5): the thirteen checks as in S5 — `shell-stays-responsive-during-stalled-request` `probeMs` 1869 / `heartbeats` 8 / `maxGapMs` 254, `corrupt-db-reaches-recovery-surface`, `engine-crash-reaches-recovery-and-restarts` `detectMs` 333 / `restartMs` 1424 (pid 156984 → 207204), `second-instance-is-refused` `refusedBy` `shell-single-instance` after `refusalMs` 101. `tauri:workspaces:qualify` passed both steps in 22 s against the live TotalMix: `commissioning-probes-and-publish-complete` with `overrideProbes: false` — the audio probe passed on real meter traffic (`audioSnapshot.verified`), which can only have reached the lane's engine through its loopback-bound slots: the lane commissions a throwaway receive port (64053 this run) and send port 64054 on `127.0.0.1`, so nothing the lane sends reaches TotalMix's 7001–7010 and nothing answers on 64053, and the liveness the probe saw came from TotalMix's packets to `127.0.0.1:9001` / `:9004`, the engine's default slots bound by the new rule at start — then `audio-live-mutations-round-trip`, `lighting-live-mutations-round-trip`, `planning-live-mutations-round-trip` and `restart-preserves-migrated-workspace-state`. A `netstat -an` sampler beside the workspace lane recorded the lane engine holding `UDP 127.0.0.1:9001`, `127.0.0.1:9002`, `127.0.0.1:9003` and `127.0.0.1:9004` (about four seconds into the lane, before its commissioning step rebound the slots to the throwaway ports; no `0.0.0.0:900x` in any sample) while TotalMix's sockets toward `127.0.0.1:9001` and `:9004` stayed up in every sample — the done criterion's `netstat` line. The foreign-datagram criterion is met by the two dropped-and-logged tests (a datagram from an address other than the console's is discarded, the meter state untouched, one WARN line in the log); with the loopback bind a LAN-addressed datagram never reaches the engine at all, so on the workstation the source check is the second layer, for the wildcard and override cases. `npm run dev:check` green end to end (82 s on warm caches: format, lint, `scripts:test`, `file:health`, rustfmt, clippy, `protocol:check` with the contract unchanged, `frontend:typecheck`, `frontend:test` — app 64, design-system 175, engine-client 13, tokens 15 — `native:check`, `native:test`). TotalMix FX and Companion untouched — nothing wrote to 7001–7010 beyond the lane engines' own once-a-second pulls with their default settings (the bus and bank pins, `/sendall` + `/sendstate`), which are the pulls the real app makes; the lane runtimes were temp directories, deleted afterwards.
Baselines: no operator-visible change on any fixture board — the change is engine-side socket binding and logging; no board, ratchet or baseline moves; the visual lanes were not run.
Operator hands: Appendix B item 3 — with the app running, confirm on the Console that live TotalMix meters move and that a fader moved in TotalMix follows in the app (the console echo) with the loopback bind; `netstat -an | findstr 900` in a terminal should show the four ports on `127.0.0.1` and none on `0.0.0.0`.
CI (result, 2026-09-11): run 34593996173 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/34593996173), event `push`, sha `bee067b`, created within five seconds of the push; ten and three-quarter minutes end to end. Green on the first attempt: `lint` (0.4 min), `frontend-typecheck` (0.6), `frontend-test` (0.6), `format-protocol` (1.7 — `file:health` holding `rme_totalmix_osc.rs` at 1,687 lines with no allowlist entry), `tauri-foundation` (2.1), `rust` (2.8 — rustfmt, clippy, check, the 354 tests on Linux with the six ingress cases — the loopback bind of all four slots on an ephemeral base, the dropped-and-logged datagrams on both slot kinds and the rate limit among them — and `native:acceptance`, whose simulated engines bind nothing) and `qualification` (8.6 — the setup-support lane's six steps under xvfb in 250 s: `shell-stays-responsive-during-stalled-request` `probeMs` 2273 / `heartbeats` 10 / `maxGapMs` 316, `corrupt-db-reaches-recovery-surface`, `engine-crash-reaches-recovery-and-restarts` `detectMs` 106 / `restartMs` 1234 (pid 12061 → 12084), `second-instance-is-refused` `refusedBy` `shell-single-instance` after `refusalMs` 25084 as the S5 rescope describes; the workspace lane in 95 s with the audio probe skipped as always on CI — `overrideProbes: true`, `audio-probe-disabled` — so the loopback-bound slots on the runner carried no traffic and the live proof stays the workstation lane above). Red, expected: `frontend-e2e` — 93 failed / 322 passed in 6.9 min: the 91 linux snapshot cases (52 `visual-review` + 39 `storybook`: 73 stale + 18 missing), the constant `tests/audio-arm-countdown.spec.ts:45` dwell case and `tests/audio.spec.ts:1054` (already on the Slice 13 quarantine list); no new candidate, the same shape as the S5 run. S6 changed no front-end source, so no visual diff exists and the linux refresh stays Appendix B item 6.

## Slice 7 — Support backup/restore surface

Status: landed (operator verification pending) 2026-09-11 (commit `9bb1f8d` `Readiness S7: backup verify, database restore through a restart, restore sources inside the backups folder, recovery mode after a storage failure`, pushed, with the win32 baselines in the separate commit `c74ca5f` `Readiness S7 (baselines): Support plate Verify key and recovery-surface copy`; CI run 34602095638 recorded under "CI (result)" below by the follow-up commit `Readiness S7 (ledger): CI run on the branch recorded`, as in S0–S6; every S7 lane and `npm run dev:check` green on the workstation; the hardware step is Appendix B item 4, which only the operator can walk). Closes F20 F29. Depends on S3.

Scope (as landed): `E/support.rs` (1,469 lines; its tests moved to `E/support/tests.rs`, 1,019, before the new code took the file over 2,000 — the S3/S6 precedent) — `SUPPORT_BACKUP_FORMAT_VERSION` 4: the archive gains `settings`, every key under `RESTORE_KEY_PREFIXES` (`shell.` and `app.control_surface.`) verbatim, so the scene thumbnails, the talent marks, the Setup section, the current lighting section, the window mode and the deck's bank, dial mode, selected scene and last event travel with an export and come back with a restore; `clear_support_settings` deletes those prefixes as a whole and `write_support_settings` writes the structured shell fields formats 2 and 3 exported but never restored (window mode, Setup section, section id, thumbnails, marks) and then the verbatim map, which wins; a `formatVersion` above the reader's is refused before anything is written (`SupportCommandError::UnsupportedVersion` → `SUPPORT_RESTORE_UNSUPPORTED_VERSION`); `SupportBackupKind { Archive, Database }` (serialised `archive` / `database`) on every `SupportFileEntry` and `list_backup_files` (replacing `list_json_files`) lists `.json` and `.sqlite3` files and nothing else; `parse_support_restore_request(params, backups_dir)` canonicalises the path and the backups directory (links followed) and refuses anything outside it, a non-file, or an unknown extension — the one root for restore and verify (F29); `verify_support_backup(request) -> SupportBackupVerification { ok, kind, path, formatVersion | schemaVersion, detail }`, never an error: `inspect_archive` parses a JSON file (a support archive of a readable format, or a legacy `db.json` export), `inspect_database_backup(path) -> DatabaseBackupFacts` opens a database backup read-only, runs `PRAGMA integrity_check`, reads `schema_migrations` and the planning and settings counts, and refuses a schema above `STORAGE_SCHEMA_VERSION`; `restore_support_backup` splits into `restore_archive_backup` (parse and check first, then the rollback archive `native-pre-restore-<ts>.json`, then the one transaction; a legacy `db.json` placed in the folder still goes through `import_legacy_db`; refused in recovery mode with the way out) and `restore_database_backup` (the same check as verify, a `PreRestore` database backup of the live file when `storage_ready`, then `stage_database_restore` copies the backup to `<app-data>/restore-pending.sqlite3` and flushes it; the summary carries `requiresRestart: true`, `sourceFormat: "database-backup"`, the counts read from the backup and `rollbackBackupPath: Option` — `None` only from recovery mode); `prune_pre_restore_archives_in` keeps the newest five rollback archives after every archive restore, `prune_exports(dir, now)` removes diagnostics exports older than `EXPORTS_MAX_AGE` (thirty days); `read_support_snapshot` counts both kinds in its summary and its `restoreSummary` no longer says "engine" or "db.json". `E/storage_backups.rs` — `SnapshotReason::Replaced` (`db-<ts>-replaced.sqlite3`, retention 5, in `ALL` so `prune_snapshots` owns it), `reserve_snapshot_path` `pub(crate)` for the bootstrap, `newest_snapshot` skips `replaced` files (never verified, so the recovery sentence never points at one). `E/bootstrap.rs` (1,189) — `apply_pending_restore(paths)` runs after `hold_instance_lock` and before `initialize_database`: the pending file is checked again with `inspect_database_backup` and the schema bound (a file that fails is removed with a WARN and the live database kept), the live file is renamed into the backups directory as `replaced`, its `-wal` / `-shm` are removed, the pending file takes the live name, one INFO line; `prune_exports` runs after the storage init; `recovery_runtime_context(paths)` builds the `RuntimeContext` a recovery-mode engine runs with (`storage_ready` false, no bridge, no token). `E/app.rs` (1,932) — `RECOVERY_METHODS` (`engine.ping`, `support.snapshot`, `support.backup.verify`, `support.backup.restore`) and the guard at the top of `handle_request` that answers everything else `ENGINE_NOT_READY` while `storage_ready` is false; `EngineApp::recovery(paths)`; `support_error_response` shared by the three support arms; the `support.backup.verify` arm; the restore arm emits `support.changed` only (`backup-restore-staged`) when `requires_restart` and the full set otherwise. `E/main.rs` (592) — the request loop is `serve_requests`; on a `STORAGE_CORRUPT` or `STORAGE_MIGRATION_FAILED` bootstrap failure the engine emits `engine.startupFailed` exactly as before and then serves in recovery mode until stdin closes (every other failure still exits). `E/audio/tests_console_link.rs` — the confidence-writer allowlist names `support/tests.rs` instead of `support.rs`. Protocol: `support.backup.verify` in `v1.contract.json`, the generated schema and `protocol.ts`; `v1.md` documents verify, the restore extensions (`kind`, `requiresRestart`, the nullable rollback path, format 4, the backups-folder rule, `SUPPORT_RESTORE_UNSUPPORTED_VERSION`) and recovery mode. Front-end: `EC/types.ts` (`ShellStore.verifySupportBackup`); `EC/store/createShellStore.ts` (1,397) — `restoreSupportBackup` awaits `restartEngine()` when the answer carries `requiresRestart` (the same path as Retry startup; a failure to come back goes to `backgroundFailures`), `verifySupportBackup` is a plain request, and after a start-up failure whose code is in `RECOVERY_MODE_CODES` the store fetches `support.snapshot` so the recovery surface lists the backups folder; `EC/transports/fixtureTransport.ts` (6,711, allowlisted) — `runtime.paths.exportsDir` (the S5 hook), `kind` on the fixture entries, `findFixtureBackup` with the engine's two refusal sentences, a `support.backup.verify` simulation, `requiresRestart` for a `.sqlite3` path, `formatVersion` 4 and the new restore summary; `FA/app/shellData.ts` — `SupportBackupKind`, `describeBackupKind`, `kind` read from the entry or derived from the extension; `FA/app/setup/components/SupportPlate.tsx` (185) — `onVerifyBackup` and the `Verify latest` key (`support-verify-latest`, the same small key beside Export backup and Restore latest), "N backups" as the section detail; `FA/app/setup/SetupSupportPilot.tsx` (1,686) — `verifyBackup` → `performAction("verify-backup")` with the inline feedback "Backup checked: …" / "Backup failed its check: …", a `Verify path` key (`support-verify-path`) beside Restore path, the kind on every backup row and on the plate's Kind readout, the restart sentence after a database restore, and copy that says "backup archive or database backup from the backups folder" instead of "legacy db.json export"; `FA/app/setup/SetupRecoverySurface.tsx` (473) — `engineRequestsAvailable` is true only with no failure or a storage code (the two failures after which the hardware link is still there to answer), the kind on every row, the restart sentence, the folder rule in the hint, the placeholder and the empty state (the protocol-mismatch sentence kept); `FA/app/tauriShellTestBridge.ts` — `verifySupportBackup` command; `FA/vitest.config.ts` — the `__APP_VERSION__` define the pilot's component test needs. Lane: `scripts/tauri-setup-support-qualification.mjs` (1,115) — step 2 gains `database-backup-verifies-and-restores-after-restart` (seed two projects, the test bridge's `restart` — a graceful stop, which writes the `shutdown` database backup — the backup listed with `kind: database`, a third project, verify ok with `schemaVersion`, junk beside it verified `ok: false`, restore → `requiresRestart`, a `-pre-restore.sqlite3` rollback, the shell ready again at generation + 2 with the two projects, the pending file gone, one `-replaced.sqlite3`), step 4 gains `corrupt-db-restores-from-database-backup` (that backup seeded into the corrupt runtime's backups folder; the recovery surface lists it while `failed`, verify ok, restore stages without a rollback copy, the shell restarts into the two projects, and the junk file is kept byte for byte as the replaced backup). Docs: `docs/OPERATIONS.md` (the manual file swap under "The app fails before the dashboard" replaced by the recovery-surface restore; Data Safety names the `replaced` reason, format 4, verify, the folder rule, the caps), `docs/DEVELOPMENT.md` (the lane's new scenarios), a CHANGELOG Added bullet.
**Rescope:** three deviations, none silent, and the title stands because what landed is what it says. (1) Recovery mode — the design's `.sqlite3` restore assumed a running engine, but after `STORAGE_CORRUPT` / `STORAGE_MIGRATION_FAILED` `main.rs` returned an error and the process ended, so the recovery surface's Restore keys (which S3's sentence points the operator at, and which this slice was to make real instead of the manual file swap) could reach nothing. The engine now stays up in recovery mode after those two codes, answering only the four backup requests, and the store fetches the backup list; the manual swap is gone from the operations guide. (2) The F29 exemption "unless the path came from the test bridge" is dropped: the lane restores the engine's own files inside its temp backups folder, so the exemption had no user, and one root with no special case is the rule the engine, the fixture transport and the copy all state. The staged legacy import under `<app-data>/import/db.json` (S1) stays the first-start path and is not a second restore root — a legacy export is restored by placing it in the backups folder, which the placeholder and hints now say. (3) `SnapshotReason::Replaced`: keeping the moved-aside database next to the pre-restore copy needed a name `prune_snapshots` owns (retention 5) and `newest_snapshot` must skip (it was never verified; after a corrupt start it is the junk); the S3 retention values are untouched. Two things recorded rather than done: the fixture transport stays allowlisted — its three support arms lean on eleven module-private helpers of the state machine (`asArray`/`asRecord`/`countControls`/`countPlanningActivity`/`buildSeededPlanningSnapshot`/`updateFixtureCheck`/`synchronizeFixtureState`/`emit` and the rest), so moving the arms alone would first mean exporting those, which is the S14 (F26) restructuring; and `E/app.rs` stands at 1,932 lines, 68 under the cap, for S8 and S9 to mind.
Gates before: the archive carried `ShellSettingsSnapshot` in full, but the restore wrote back only the workspace, width, height and maximized flag, so the scene thumbnails, talent marks, Setup section, current lighting section and window mode were exported and never restored, and nothing under `app.control_surface.` beyond the selected light travelled at all — the round-trip test asserted planning, commissioning, lighting and audio, never the shell or the deck; `parse_support_restore_request` accepted any non-empty string and `restore_support_backup` read whatever file the engine could reach, writing the rollback archive before parsing it, so junk produced a rollback and an error; a `formatVersion` above the reader's was parsed on faith; `list_json_files` hid every `.sqlite3` backup S3 had been writing; no verify existed; the engine exited on a storage failure, so the recovery surface's restore keys (enabled for every code but `PROTOCOL_MISMATCH`) could reach nothing; no test named a path outside the backups directory, a newer format, a `.sqlite3` source or a pending restore.
Tests added: `support::tests` (`E/support/tests.rs`) — `restore_refuses_newer_format` (format 5 refused with `UnsupportedVersion`, no rollback archive, the data untouched, verify says the same), `restore_round_trips_all_prefixes` (ten keys under both prefixes exported, all changed and two added after the export, every one back verbatim and the added ones gone, the map equal to the archive's), `list_includes_sqlite3` (a `daily` database backup and an export listed newest first with their kinds, a stray `.txt` and a `-wal` sidecar not), `verify_flags_junk_file` (junk `.sqlite3` and `.json`, a stranger JSON, a real database backup with `schemaVersion`, a real export with `formatVersion`, a `.txt` refused at parse), `restore_rejects_path_outside_backups` (an archive copied outside the folder, a missing file, the folder itself, a blank path — all refused before anything is read), `database_restore_stages_pending_file_and_keeps_rollback` (the pending file byte-identical to the backup, the `pre-restore` copy a good database, the live database untouched, the log line; in recovery mode no rollback and an archive refused), `pre_restore_archives_are_capped_at_five`, `exports_older_than_thirty_days_are_pruned`; `bootstrap::tests` — `pending_restore_applied_after_snapshot` (the live file becomes `db-…-replaced.sqlite3` beside the `pre-restore` copy, the fake `-wal` / `-shm` go, the restored data reads back, a full `bootstrap_runtime_from_paths` then opens it), `pending_restore_that_fails_its_check_is_refused` (junk pending removed, live file byte-identical, WARN logged); `storage_backups::tests` — `newest_snapshot_skips_replaced_files` (and `prune_keeps_retention` covers the fifth reason through `ALL`); store tests (`createShellStore.test.ts`) — "restore with requiresRestart restarts the link once and spends no restart budget" (launch 2 through the restart path, the graceful exit ignored, an archive restore restarts nothing, the next real stop is attempt 1 of 3), "fetches the backup list after a storage failure at start" (`support.snapshot` once after `STORAGE_CORRUPT`, never after `ENGINE_ALREADY_RUNNING`); `SetupSupportPilot.test.tsx` (new) — "verify shows inline result" (the plate key → "Backup checked: Backup archive, format 4 …" in the feedback banner, tone ok), "verify shows the refusal for a file outside the backups folder" (tone error, the engine's sentence); lane checks `database-backup-verifies-and-restores-after-restart` and `corrupt-db-restores-from-database-backup`. Tests changed (old → new → why): `restore_support_backup_accepts_legacy_json` and `restore_support_backup_accepts_format_v2_archive_without_storage_version` (source file in the test root → inside the backups folder, through `parse_support_restore_request` → F29); `restore_support_backup_round_trips_native_archive` (`SupportRestoreRequest` gains `kind`; `!rollback_backup_path.is_empty()` → `is_some()` plus `!requires_restart` → the rollback path is optional for a recovery-mode database restore); `export_support_backup_writes_archive_and_lists_it` ("1 backup archives" → "1 backup archive", plus the `kind` → the summary counts both kinds and pluralises); `audio::tests_console_link::console_confidence_has_one_writer` (allowlist `support.rs` → `support/tests.rs` → the support tests moved to their own file); the five existing support tests moved otherwise unchanged; the store test double `supervisedTransport` gained `answer(method, value)` and records requests (no assertion changed); the lane's existing checks are unchanged.
Lanes: `npm run protocol:generate && npm run protocol:check`, `npm run native:test`, `npm run rust:clippy`, `npm run frontend:test`, `node scripts/check-operator-copy.mjs`, `npm run build --workspace frontend/app && npm run frontend:playwright:test`, `npm run tauri:setup-support:qualify`. Done when a restore brings back deck bindings, scene thumbnails and talent marks, verify answers `ok: false` on junk, and the archives are capped — all met (below).
Validation (workstation, 2026-09-11): baseline before editing — `native:test` 354 passed / 1 ignored, `frontend:test` 267. After: `cargo fmt` applied; `rust:clippy` clean under `-D warnings`; `cargo test -p studio-control-engine --test contract` 4 passed with the new method; `protocol:check` green (the schema and `protocol.ts` gained the one method; no ts-rs binding changed, `SupportFileEntry` is not a ts-rs type); `native:test` 365 passed / 1 ignored (tauri-shell 17, engine 339 + 1 ignored hardware-bound — was 328, `contract.rs` 4, `end_to_end.rs` 5) — the first full run after the move failed one source-scan test, `console_confidence_has_one_writer`, whose allowlist named `support.rs` for a key the moved tests mention, fixed by naming `support/tests.rs`; `frontend:test` 271 (app 66 — the two pilot cases new, with `ResizeObserver` and `matchMedia` stubbed for jsdom — design-system 175, engine-client 15 with the two store cases new, tokens 15); `frontend:typecheck` clean; `node scripts/check-operator-copy.mjs` 0 hits; prettier and eslint clean on every changed file; `file:health` passed (`support.rs` 1,469, `support/tests.rs` 1,019, `bootstrap.rs` 1,189, `app.rs` 1,932, the fixture transport 6,711 on its existing allowlist entry). `dist` and `storybook-static` rebuilt, then `frontend:playwright:test`: 397 passed, 18 visual diffs and no ui-contract movement (every ratchet held, so Verify stays its own key) — the five `Shell/OperatorShell` stories Setup Required / Ready / Degraded, Protocol Mismatch and Bootstrap Failed, `visual-review` setup-ready at 2560×1440, the studio preview, graphite and bone, setup-degraded at 2560×1440, and protocol-mismatch at all six viewports plus graphite and bone; the diffs are the plate's Backups section (measured in the built app at 2560×1440: three keys of 122 × 28 px on one row, no text overflow, the plate not scrolling) and, on the recovery surface, the empty-state sentence that used to promise "restore a known file directly" and the availability rule; refreshed in the baselines commit and re-run clean (23 passed). The debug test-bridge shell was pre-built (`cargo build --features test-bridge`), the production app was closed (nothing listening on 38201 or 4173), and `tauri:setup-support:qualify` passed all six steps in 62 s on the first run (evidence `summary.json`, `githubSha` 330c3fb): the thirteen checks as in S6 — `shell-stays-responsive-during-stalled-request` `probeMs` 2053 / `heartbeats` 9 / `maxGapMs` 252, `engine-crash-reaches-recovery-and-restarts` `detectMs` 331 / `restartMs` 1414, `second-instance-is-refused` `refusedBy` `shell-single-instance` after `refusalMs` 101 — plus `database-backup-verifies-and-restores-after-restart` (backup `db-2026-09-11T12-56-55-295Z-shutdown.sqlite3` verified at `schemaVersion` 6, rollback `db-…-pre-restore.sqlite3`, replaced `db-2026-09-11T12-56-56-785Z-replaced.sqlite3`) and `corrupt-db-restores-from-database-backup` (generation 2 after the restart, replaced `db-2026-09-11T12-57-13-310Z-replaced.sqlite3` equal to the junk byte for byte). `npm run dev:check` green end to end (62 s on warm caches: format, lint, `scripts:test`, `file:health`, rustfmt, clippy, `protocol:check`, `frontend:typecheck`, `frontend:test`, `native:check`, `native:test`). TotalMix FX and Companion untouched — the lane's engines wrote nothing to 7001–7010 beyond their default once-a-second pulls; the lane runtimes were temp directories, deleted afterwards.
Baselines: win32 refreshed for the eighteen titles above in the separate commit `Readiness S7 (baselines): Support plate Verify key and recovery-surface copy` (every changed PNG inspected: the plate's third key and the two readouts, the recovery surface's sentence); linux stays Appendix B item 6 (the CI `frontend-e2e` count moves by these boards); darwin pending the next macOS host visit.
Operator hands: Appendix B item 4 — with the app running, Setup / Support: Verify latest should answer "Backup checked: Database backup, schema 6, …" for the last shutdown backup; Restore latest then restarts the hardware link on its own and the Console comes back with the fixtures, the scenes and their thumbnails, the talent marks and the deck's bank, dial mode and selections as they were at that backup; `<app-data>/backups` should then hold a new `db-…-pre-restore.sqlite3` and a `db-…-replaced.sqlite3`.
CI (result, 2026-09-11): run 34602095638 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/34602095638), event `push`, sha `c74ca5f` (the baselines commit, pushed together with `9bb1f8d`), created within five seconds of the push; ten and a half minutes end to end. Green on the first attempt: `lint` (0.4 min), `frontend-typecheck` (0.6), `frontend-test` (0.6 — the 271 Vitest cases, the two store cases and the two pilot cases among them), `format-protocol` (1.4 — `protocol:check` with the new method, `file:health` with `support.rs` at 1,469 lines and `app.rs` at 1,932), `rust` (2.0 — rustfmt, clippy, check, the 365 tests on Linux with the eight support cases, the two pending-restore cases and the replaced-reason case among them, plus `native:acceptance`), `tauri-foundation` (2.2) and `qualification` (8.2 — the setup-support lane's six steps under xvfb in 250 s with all fifteen checks: `shell-stays-responsive-during-stalled-request` `probeMs` 2159 / `heartbeats` 9 / `maxGapMs` 325, `database-backup-verifies-and-restores-after-restart` with backup `db-2026-09-11T13-09-06-125Z-shutdown.sqlite3` at `schemaVersion` 6, rollback `db-2026-09-11T13-09-07-757Z-pre-restore.sqlite3` and replaced `db-2026-09-11T13-09-08-447Z-replaced.sqlite3`, `corrupt-db-reaches-recovery-surface` and then `corrupt-db-restores-from-database-backup` at generation 2 with replaced `db-2026-09-11T13-10-21-331Z-replaced.sqlite3`, `engine-crash-reaches-recovery-and-restarts` `detectMs` 105 / `restartMs` 1231 (pid 12206 → 12227), `second-instance-is-refused` `refusedBy` `shell-single-instance` after `refusalMs` 25103 as the S5 rescope describes; the workspace lane in 93 s with the audio probe skipped as always on CI — `overrideProbes: true`, `audio-probe-disabled`). Red, expected: `frontend-e2e` — 93 failed / 322 passed in 8.6 min: the 91 linux snapshot cases (52 `visual-review` + 39 `storybook`; the eighteen S7 boards were already among the stale or missing linux baselines, so the count did not move), the constant `tests/audio-arm-countdown.spec.ts:45` dwell case and `tests/audio-talkback.spec.ts:64` (already on the Slice 13 quarantine list); no new candidate, the same shape as the S5 and S6 runs. The linux refresh stays Appendix B item 6.

## Slice 8 — Engine health + logging

Status: verified 2026-09-17 (commit `b00ecaf` `Readiness S8: engine health registry, rotating engine and shell logs, request log at DEBUG`, pushed; CI run 35201156819 recorded under "CI (result)" below by the follow-up commit `Readiness S8 (ledger): CI run on the branch recorded`, as in S0–S7; every S8 lane and `npm run dev:check` green on the workstation; no hardware exposure — the slice binds nothing new, writes to no console and changes no operator surface, so it ends as `verified` like S3 and S4). Closes F11 (engine half) F14 F16 F27. Depends on S3 S6.

Scope (as landed): `E/diagnostics.rs` (565 lines, was 80) — `LogLevel { Debug, Info, Warn, Error }` parsed from `SSE_ENGINE_LOG_LEVEL` (`LOG_LEVEL_ENV`; unset or empty → `INFO`, an unknown name → `INFO` plus one WARN line saying so); `LogWriter` keeps one `File` open, counts bytes, and rotates `engine.log` → `engine.log.1` … `engine.log.5` (`LOG_ROTATE_BYTES` 5 MiB, `LOG_ROTATE_KEEP` 5) by renaming with the handle dropped first, best effort — a rename a viewer blocks leaves the file in place and the next line tries again; `init_log(path, level)` installs it in a `OnceLock<Mutex<LogWriter>>` (first call wins); `log_event(level, message)` writes through it, dropping lines below the level, and falls back to stderr when there is no writer yet or the write failed; `append_log(path, level, message)` is routed through the writer when `path` is its file (rotation and the level filter apply) and opens and appends any other path per call as before, so every existing test that reads its own temp log still works without `init_log`; `request_log_line(level, method, id, elapsed, ok)` builds the F27 line (`method=… id=… ms=… ok=…`) only at `DEBUG`; `read_log_tail(path, 64 KiB, max_lines)` seeks to the last `LOG_TAIL_MAX_BYTES` of the current file, drops the partial first line and returns the last `max_lines` (the missing-file and empty-file sentences are unchanged; `read_log_excerpt` is gone). New `E/health.rs` (589 lines) — `SubsystemState { Ok, Attention, Warning, Error }` (serialised lower-case), `SubsystemStatus { state, detail, at }` (`at` Unix seconds), `HealthRegistry` (a `BTreeMap<&'static str, SubsystemStatus>` behind a process-wide `OnceLock<Mutex<…>>`) whose `report` returns whether the state changed (a first `ok` is not a change, a first `attention` is); `report` / `report_at` raise `app.changed { reason: "health" }` through the new `engine_events::emit_app_changed` on a change (a no-op before `main.rs` registers the sender, so the bootstrap's reports raise nothing and the first `health.snapshot` carries them); `effective_entries(entries, now)` turns a `backups` entry that is `ok` but older than `BACKUP_WARNING_AGE_SECS` (48 h) into `warning`; `derive_status(entries, storage_ready)` = `error` when `storage_ready` is false or any entry is `error`, else `attention` if any, else `warning` if any, else `ok`; `read_health_snapshot(runtime)` (moved here from `app.rs` with `format_health_summary`) keeps every field the shell reads, sets `status` from the registry (the never-reachable `"starting"` is gone), adds `checks.engine` = the effective entries keyed by subsystem, and reads the excerpt with `read_log_tail(…, 64 KiB, 12)`. Reporters: `control_surface_http::start_control_surface_bridge` → `bridge` `ok` (the serving sentence) or `attention` ("unavailable because the listener could not bind: …" — no fallback port is scanned); the metering thread in `rme_totalmix_osc.rs` → `osc` after every bind cycle (`bind_slots_reporting` returns the bound slots and the failed ports; `report_meter_port_health` says `ok` "Listening for TotalMix on N meter port(s) from <receive port> on <bind host>" when every classic slot and the Global OSC slot bound, `attention` naming each port that did not; the unresolvable-address branch is `attention`; switching TotalMix metering off reports `ok` "metering is off" once, through `bound_key.take()`); `lighting_sacn_output::run_output_loop` → `sacn` `ok` when the UDP socket is allocated, `attention` when it is not; `main.rs::write_database_backup` → `backups` `ok` with the copy's sentence or `warning` with the failure (daily and shutdown alike); `bootstrap_runtime_from_paths` → `storage` `ok` ("Integrity ok, schema vN") after `initialize_database`, and seeds `backups` `ok` at the modified time of `newest_snapshot(backups_dir)` so the 48 h rule holds across restarts; `apply_pending_restore` → `restore` `ok` with the S7 INFO sentence; `EngineApp::recovery` → `storage` `error`. `E/app.rs` (1,874 lines, was 1,932): `handle_request` times the request, runs the old body as `dispatch`, and appends the DEBUG line through `append_log` when `configured_log_level()` is `DEBUG` — the `INFO` "Handling request: …" line on every request is gone; `shutdown` logs the released talkback holds instead of printing them. `E/main.rs` (611): `mod health`; `init_log(&planned_paths.log_file_path, LogLevel::from_env())` right after `resolve_runtime_paths()` succeeds, before `validate_protocol_version` and `EngineApp::bootstrap`, so the protocol mismatch, the bootstrap failure and the recovery mode all log to `engine.log`; the output-writer failure and a malformed request log at ERROR / WARN. `E/audio/talkback.rs` (the three watchdog and shutdown lines → `log_event`), `E/rme_totalmix_osc.rs` (settings-read, bind, receive and console-link-flush failures → `log_event` WARN; a decode failure → DEBUG, since a malformed-datagram flood must not become a log flood; the `cfg(test)` console-port guard → `println!`), `E/audio/tests_console_link.rs` (two test prints → `println!`). `rg "eprintln!" native/rust-engine/src` = 2, both documented here: `main.rs` "Engine bootstrap failed" when `resolve_runtime_paths` fails (no log path exists yet) and the stderr fallback inside `diagnostics::log_event`. Shell: new `T/shell_log.rs` (177) — `ShellLog::open(logs_dir)` → `<logs>/shell.log`, `write_line(label, message)` with the same 5 MiB × 5 rotation; `T/engine.rs` (1,108) — `EngineBridge.shell_log` opened once per shell in `start()` (`shell_log_for`) and passed to `launch(…, Option<SharedShellLog>)`; `spawn_stderr_thread` writes every engine stderr line as `[ts] ENGINE-STDERR <line>` and echoes it only under `cfg(debug_assertions)`; `T/main.rs` `mod shell_log`. Front-end: no source change (D6) — `EC/machines/recoveryMachine.ts` is unchanged and now reachable; `recoveryMachine.test.ts` and `FA/app/setup/setupState.test.ts` pin the mappings. Lane: `scripts/tauri-setup-support-qualification.mjs` step 7/7 `bridge-port-taken-reads-as-health-attention` — a `net.createServer` holds an ephemeral loopback port, the shell is launched with `SSE_CONTROL_SURFACE_PORT` naming it, and the status file must show `healthSnapshot.status` `attention`, `checks.engine.bridge.state` `attention` with a "could not bind" detail, `appSnapshot.runtime.controlSurface.available` false on that port, `recovery` `degraded`, a string `recentLogExcerpt` ≤ 64 KiB and an `engine.log` in the lane's logs dir. Docs: `native/protocol/v1.md` (the `status` vocabulary, `checks.engine`, the bounded string excerpt, the `health` reason of `app.changed` — no contract method or event changed, `protocol:check` untouched), `docs/OPERATIONS.md` (Health status, Logs), `docs/DEVELOPMENT.md` (the writer, `SSE_ENGINE_LOG_LEVEL`, the two stderr sites, `shell.log`, the reporting rule), `CHANGELOG.md`.

**Rescope:** seven deviations, none silent, and the title stands because what landed is what it says. (1) The bridge reports from `E/control_surface_http.rs`, where S2 moved the listener, not from `E/control_surface.rs` as the design's file list says. (2) The registry, the status derivation and `read_health_snapshot` live in a new `E/health.rs` (the design named the tests `health::tests`, so this is where it pointed); `app.rs` could not take them and stay under the 2,000-line cap. (3) The two documented stderr sites are not "the two pre-log-path failures in `main.rs`": `init_log` runs as soon as the runtime paths resolve, so the protocol mismatch logs to `engine.log` like everything after it, and the second documented site is the fallback inside `log_event` for a line the file could not take — nothing is dropped, and the shell keeps it in `shell.log`. The two test-only stderr prints (the `cfg(test)` console-port guard, `tests_console_link`) became `println!` so the count is honest without an allowlist. (4) `warning` covers a failed backup as well as a late one, and the last-backup time is seeded from the newest backup on disk at bootstrap, so a restart does not forget when the last copy was written. (5) A sixth entry, `restore`, records the S7 pending-restore application (the S7 ledger hook). (6) "Support shows Attention on port fallback" lands as the Support surface already renders it: the health sentence in the DEGRADED state (a probe not green) and the Deck lamp; the Setup word is READY while the probes were green and SETUP REQUIRED on the lane's clean runtime, so the lane asserts the snapshot fields and the store's `degraded` recovery state, and `setupState.test.ts` pins the word / tone / sentence mapping; the front-end source is untouched (D6). (7) The OSC decode-failure line is `DEBUG`, not `WARN`, so a malformed-datagram flood cannot become a log flood. Decisions recorded, not deviations: `health.snapshot` stays out of `RECOVERY_METHODS` (it still reads the database for the lighting, audio and SQLite facts; the recovery surface reads `engine.startupFailed`, and the recovery engine's registry says `storage` `error` for any later reader); no port scan (F14's text asks for `attention` on the failure, and a scan would move the deck's port from under Companion); `recentLogExcerpt` stays the string the protocol has always sent (the front-end `RecoverySurface` reads an array — a pre-existing mismatch left for S9, below); a first `ok` report is silent so the bootstrap's reports do not cost the store a full refresh each.

Gates before: `read_health_snapshot.status` was `"ok"` for every running engine (`"starting"` was unreachable), `append_log` opened the file per line, `read_log_excerpt` read the whole file for twelve lines, fifteen `eprintln!` sites wrote to a stream the release shell discards, and every request wrote an INFO line; no test asserted any of it.

Tests added: `diagnostics::tests` — `read_log_tail_bounded` (a ~500 KiB log: twelve lines, the last one last, the first one whole, an early marker absent, the whole window ≤ 64 KiB), `read_log_tail_scans_only_the_tail_of_a_large_log` (opt-in with `SSE_ENGINE_TEST_LARGE_LOG=1`: a 100 MB log, the tail read must take < 50 ms), `append_log_rotates_at_limit` (the one test that installs the process-wide writer, with a 400-byte limit and three files: `.1` … `.3` appear, `.4` never, a DEBUG line through `append_log` is dropped at INFO, a different path still gets the direct append), `log_writer_filters_below_its_level_and_keeps_the_size_across_reopen`, `log_level_parses_the_environment_value`, `request_line_is_off_at_info_and_on_at_debug`; `health::tests` — `status_error_on_integrity_failure`, `status_attention_on_bridge_port_fallback`, `status_warning_when_the_last_backup_failed_or_is_older_than_two_days`, `report_signals_a_transition_only_on_a_state_change`, `health_snapshot_lists_engine_checks_and_a_bounded_log_excerpt` (a runtime over a 400 KB log: twelve lines, `checks.engine` an object; a `storage_ready: false` runtime answers `error`); `app::tests::handle_request_writes_no_request_line_at_the_default_level`; shell — `engine::tests::stderr_lines_reach_shell_log` (a `cmd /C echo … 1>&2` / `sh -c` process through `launch`; the line lands in `shell.log` as `ENGINE-STDERR …`), `shell_log::tests::shell_log_rotates_at_its_limit`, `shell_log_opens_under_the_logs_dir_and_keeps_existing_content`; front-end — `recoveryMachine.test.ts` (`attention → degraded`, `warning → degraded`, `error → recovery`, `ok → healthy`, missing or unknown → `healthy`), `setupState.test.ts` (attention with a probe not green → DEGRADED / attention / the health sentence; error keeps DEGRADED with the error tone; ok and all green → READY; unpublished → SETUP REQUIRED); lane check `bridge-port-taken-reads-as-health-attention`. Tests changed: `diagnostics::tests::read_log_excerpt_returns_missing_file_message` → `read_log_tail_returns_missing_file_message` and `read_log_excerpt_returns_last_lines` → `read_log_tail_returns_last_lines` (the function was renamed and takes the byte window); `app::tests::health_summary_includes_all_native_domains` → `health::tests::health_summary_includes_all_native_domains` (moved with the function); the shell's three existing `launch` calls pass `None` for the shell log.

Lanes: `npm run rust:clippy` (and `cargo clippy -p sse-exed-tauri-shell --all-targets --features test-bridge -- -D warnings`), `npm run native:test`, `npm run frontend:test`, `npm run protocol:check`, `node scripts/check-operator-copy.mjs`, `npm run tauri:setup-support:qualify`, `npm run tauri:workspaces:qualify`. Done when a 100 MB `engine.log` leaves `health.snapshot` under 50 ms, `rg "eprintln!" native/rust-engine/src` has ≤ 2 documented hits, and the Support surface shows the attention posture on the port fallback — all three met (below).

Validation (workstation, 2026-09-15; the unit lanes and `dev:check` again on 2026-09-17, the day of the commit, on the unchanged tree): baseline before editing — `native:test` 365 passed / 1 ignored, `frontend:test` 271. After: `cargo fmt` applied; `rust:clippy` clean under `-D warnings`, and the test-bridge shell clippy clean; `native:test` 380 passed / 1 ignored (tauri-shell 20, engine 351 + 1 ignored hardware-bound, `contract.rs` 4, `end_to_end.rs` 5) — the twelve new engine cases, the moved summary case and the three shell cases among them; the opt-in 100 MB case: the tail of a 100 MB log read in 4.3 ms; `frontend:test` 280 (app 70, design-system 175, engine-client 20, tokens 15); `frontend:typecheck`, `lint`, `format`, `protocol:check` (no contract change), `file:health` (`app.rs` 1,874, nothing new over 2,000), `scripts:test` and `check-operator-copy` (0 hits) green; `rg "eprintln!" native/rust-engine/src` = 2. `tauri:setup-support:qualify` passed in 63.4 s with all sixteen checks over seven steps: `shell-stays-responsive-during-stalled-request` `probeMs` 1872 / `heartbeats` 8 / `maxGapMs` 251, `database-backup-verifies-and-restores-after-restart` at schema 6, `corrupt-db-restores-from-database-backup` at generation 2, `engine-crash-reaches-recovery-and-restarts` `detectMs` 211 / `restartMs` 1305, `second-instance-is-refused` by the plugin in 104 ms, and the new `bridge-port-taken-reads-as-health-attention` — port 57252 held, `healthStatus` `attention`, `recovery` `degraded`, `bridgeDetail` "Native control-surface bridge is unavailable because the listener could not bind: Only one usage of each socket address … (os error 10048)"; not one `engine stderr` echo in the lane's output, where the debug shell used to print the engine's talkback and metering lines. `tauri:workspaces:qualify` passed in 21.5 s with its five checks (audio receive port 62541 — a throwaway; nothing written to 7001–7010); no engine stderr line there either. `npm run dev:check` green on 2026-09-17 before the commit; the two qualification lanes were not repeated that day — no file had changed since they passed (every edit predates their 2026-09-15 run, the last at 12:11 against a lane start of 12:12) and the production app was open on 38201, which a lane must never share.

Baselines: no operator-visible change on any fixture board — no front-end source changed; the visual lanes were not run.

Operator hands: none. Notes for later slices: S9 — `EVENT_DOMAIN_REFRESH` should map `app.changed { reason: "health" }` to the health snapshot alone (today every `app.changed` is a full refresh); `FA/app/startup/RecoverySurface.tsx` reads `recentLogExcerpt` as an array and the fixtures carry one, while the engine sends the string the protocol describes — decide the shape there and align the fixtures; `shellState.recovery` `degraded` is not rendered anywhere. S13 — `read_log_tail_scans_only_the_tail_of_a_large_log` is opt-in (`SSE_ENGINE_TEST_LARGE_LOG=1`), a candidate for the workstation lane.

CI (result, 2026-09-17): run 35201156819 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/35201156819), event `push`, sha `b00ecaf`, created within seconds of the push; twelve and a half minutes end to end. Green on the first attempt: `frontend-typecheck` (0.5 min), `lint` (0.5), `frontend-test` (0.6 — the 280 Vitest cases, the five recovery-machine cases and the four setup-state cases among them), `format-protocol` (1.3 — `protocol:check` with no contract change, `file:health` with `app.rs` at 1,874 lines and the new `health.rs` at 589), `rust` (2.3 — rustfmt, clippy, check, the 380 tests on Linux (tauri-shell 20, engine 351 + 1 ignored, `contract.rs` 4, `end_to_end.rs` 5) with the diagnostics, health and shell-log cases among them, plus `native:acceptance`), `tauri-foundation` (2.8) and `qualification` (9.5 — the setup-support lane's seven steps under xvfb in 295 s with all sixteen checks: `shell-stays-responsive-during-stalled-request` `probeMs` 2361 / `heartbeats` 10 / `maxGapMs` 324, `database-backup-verifies-and-restores-after-restart` with backup `db-2026-09-17T08-50-22-172Z-shutdown.sqlite3` at `schemaVersion` 6, `engine-crash-reaches-recovery-and-restarts` `detectMs` 207 / `restartMs` 1243, `second-instance-is-refused` by the plugin once the Linux shell had started (25.1 s), and the new `bridge-port-taken-reads-as-health-attention` — port 41043 held, `healthStatus` `attention`, `recovery` `degraded`, `bridgeDetail` "Native control-surface bridge is unavailable because the listener could not bind: Address already in use (os error 98)", so the scenario holds on Linux as it does on Windows; the workspace lane in 95 s with its five checks, the audio probe overridden as on every CI run). Red, as expected and not chased: `frontend-e2e` (9.8 min) — 93 failed / 322 passed, the S6 and S7 shape exactly: the 91 linux snapshot cases (52 `visual-review.spec.ts` + 39 `storybook.spec.ts`, stale or missing until Appendix B item 6), the constant `audio-arm-countdown.spec.ts:45` dwell case and one runner-timing case already on the S13 list, `audio.spec.ts:1054` this time. No new candidate — S8 changed no front-end source, so no board moved. Nothing is pending on S8.

## Slice 9 — Front-end resilience

Status: verified 2026-09-17 (commit `96ae4e9` `Readiness S9: error boundaries, domain-scoped snapshot refresh, reply guards, background-failure band` plus the queue follow-up `c87565a` `Readiness S9 (queue): a restart abandons a refresh batch that never settles`, pushed; both CI runs (35208750697 and 35209951483) are recorded under "CI (result)" below by the follow-up commit `Readiness S9 (ledger): CI run on the branch recorded`, as in S5; every S9 lane, the setup-support lane and `npm run dev:check` green on the workstation; no hardware exposure — the slice binds nothing, writes to no console and sends no light output, so it ends as `verified` like S1, S4 and S8). Closes F10 F11 (front-end half) F32. Depends on S5 S8.

Scope (as landed): **Refresh by domain (F11).** New `EC/store/domainRefresh.ts` (145 lines) — `DomainKey` (one per snapshot the store keeps), `DOMAIN_REQUESTS` (`satisfies Record<DomainKey, RequestMethod>`), `ALL_DOMAINS` (ten: the bootstrap's set and what `refresh()` fetches), `CHANGEABLE_DOMAINS` (eight: everything but the fixture catalog and the deck's page model), and the two maps. `EVENT_DOMAIN_REFRESH satisfies Record<EventName, readonly DomainKey[]>` lists all eleven events: `lighting.changed` → lighting + its DMX monitor (2 requests, was 10), `planning.changed` → planning + commissioning, `commissioning.changed` → commissioning, app, health, lighting + DMX monitor, audio, `app.changed` → app + health (S8's `health` reason included), `settings.changed` → app, `support.changed` → support (S7's `backup-restore-staged` included), `audio.changed` → audio for the record only (it keeps its dedicated path with echo suppression and the console-echo rule), and `audio.meters`, `engine.ready`, `engine.startupFailed`, `engine.exited` → nothing. `domainsForEvent` answers the eight changeable domains and `known: false` for an event this build does not know; a development store logs that. `domainsForMethod(method, params)` scopes the refresh that follows a request by method name or prefix (`settings.update` → app; `commissioning.` → the commissioning set, `seedPlanningDemo` adds planning; `lighting.` → lighting + DMX monitor; `planning.` → planning + commissioning; `support.backup.restore` → the eight; `support.` → support; `exports.` → nothing; an unmapped method → the eight), and adds a workspace's own snapshots to the `settings.update` that opens it (Setup: health, commissioning, support, the deck's page model; Lighting: lighting + DMX monitor; Audio: audio; Planning: planning + commissioning). `EC/store/createShellStore.ts` (1,517 lines, was 1,397): `refreshDomain` is gone; `refreshDomains(domains, event)` marks the snapshots wanted and resolves once a batch that went out after the call has fetched them; `runRefresh` keeps one batch in flight and sends whatever was asked for meanwhile as the next, so a burst costs one request per snapshot, not one per event; `fetchDomains` uses `Promise.allSettled`, so a request that fails does not cost the others their answers; `snapshotsToState` writes only what was fetched — `activeWorkspace` only with the app snapshot, `recovery` only with the health snapshot, and never the lifecycle or the start-up failure, which stay the bootstrap's; a batch that comes back after a restart, or after the hardware link stopped, is dropped; `cancelQueuedRefresh` on bootstrap and dispose. The bootstrap keeps the whole fetch (health first, then the rest side by side) through the same `fetchDomains`. The fixture catalog is fetched once per session (`catalogLoaded`), kept across a restart, and fetched again only by `refresh()`. `refreshControlSurfaceSnapshot` goes through the same queue. **Reply guards (F32).** New `EC/store/snapshotGuards.ts` (93 lines) — `snapshotProblem(domain, value)`: the five loosely typed snapshots must be objects; the five typed ones may be absent (`null` — no console, no plan yet) and otherwise must carry their lists (`fixtures` / `groups` / `scenes`, `definitions`, `channels`, `channels` / `mixTargets`, `projects` / `tasks`, the optional ones only when present) with a string `id` on every keyed row; one pass, no allocation, nothing deeper. `SnapshotShapeError` names the request and the field (`lighting.snapshot: fixtures is not a list`). `createShellStore(transport, { development })`: a development store sets `ShellState.snapshotFault` and throws, and `useShellSnapshot` rethrows the fault while rendering so the root boundary shows it by name; a production store records the failure in the ring (`reply refused`), keeps the last good snapshot and never throws. The app passes `import.meta.env.DEV` (`FA/app/createShellEnvironment.ts`). **Boundaries (F10).** New `FA/app/startup/ShellErrorBoundary.tsx` (132) at the root in `FA/main.tsx`: a class component whose page — `THIS SCREEN STOPPED`, the error in the display's code slot, **Reload**, **Export diagnostics** — is the pre-ready state display in a window-filling container (`ShellErrorBoundary.module.css`, existing tokens only), needs no store, provider or layout context, and exports the error, the component stack and, when it can be read, the shell state; a reload hands the screen back the hardware link that is already running (`T/engine.rs::start` returns the running engine's summary). New `FA/app/startup/WorkspaceErrorBoundary.tsx` (65) around the bay's surface in `FA/app/OperatorShell.tsx` (640, was 606), keyed `experience:workspace:reloads`: `LIGHTING STOPPED` on the same state display with **Reload this area**, no element of its own while nothing has failed, and the pre-ready surfaces inside it too, so a recovery surface that fails to draw cannot take the header, the restart dialog and the close dialog with it. New `FA/app/startup/reportUiFailure.ts` (24): both boundaries and the two window listeners in `main.tsx` record through it, and a `WeakSet` keeps each error object once. New `FA/app/shared/BackgroundFailureBand.tsx` (52) + `.module.css`: the design system's `StatusBand` (`warning`) at the foot of the bay while the ring is non-empty and the shell is ready — "Studio Control hit a problem in the background", how many and since when, **Dismiss** until the next failure, no cause named. New `FA/app/startup/WorkspaceCrashProbe.tsx` (18) and the `crash` URL parameter in `createShellEnvironment.ts` (46): fixture transport only, never inside the Tauri runtime; `window.__SSE_TEST_DISARM_CRASH__` exists only when the parameter armed a fault. `FA/app/setup/SetupSupportPilot.tsx` (1,689): the diagnostics export carries `backgroundFailures`, read from `store.getSnapshot()` at the moment of the export. **S8's hook.** `FA/app/startup/RecoverySurface.tsx` `readLogExcerpt` reads the string the hardware link has always sent (split into lines) and still a list; `frontend/packages/test-fixtures/src/fixtures.json` carries strings; `check-fixtures.mjs` requires one. Docs: `CHANGELOG.md`, `docs/OPERATIONS.md` ("When part of the screen stops"), `docs/DEVELOPMENT.md` (the maps, the queue, the development store, the boundaries, the crash hook). No engine, shell or protocol change: `protocol:check` and `native:test` are untouched.

**Rescope:** eight deviations, none silent, and the title stands because what landed is what it says. (1) The refresh after a request stays, scoped by method, beside the event's — the design only names the event map. On the hardware link `settings.update` and `exports.companion.export` answer with no event at all (`E/app.rs` replies with `Self::reply`), so a workspace switch reaches the screen only through that refresh. Dropping it was checked against both transports: the fixture double raises an event for every mutation it implements and would have allowed it, the hardware link does not. The two refreshes share one queue and never run side by side; a lighting request from the UI costs 1 + 2 + 2 requests (was 1 + 10 + 10) — the pair is not merged because the refresh after a request must go out after its reply arrived, and merging it with the event's batch would lean on an ordering between two shell commands that the async command pool (S4) does not promise. (2) Three mappings are wider than the design's examples, each because of what the engine's snapshot builder reads: `commissioning.changed` adds lighting, its DMX monitor and audio (`E/lighting/snapshot.rs` derives `reachable` from the stored probe status and the monitor from the lighting snapshot; `E/audio/helpers.rs` derives the capabilities from the audio probe status; publishing gives the rig its bridge and universe), and `planning.changed` adds commissioning (`read_commissioning_snapshot` carries the planning counts). Both were found by the qualification lanes, not by reading — see Validation. `lighting.changed` is the design's two requests. (3) "Unknown → full refresh" is the eight changeable snapshots, not ten: the fixture catalog is compiled in (`read_lighting_fixture_catalog_snapshot()` takes no argument) and so is the deck's page model (`build_control_surface_snapshot()` takes none; its one live field, `lastEvent`, is polled by the Setup verify step every 500 ms and fetched when Setup opens). (4) Opening a workspace fetches that workspace's snapshots — not in the design. Scoped refreshes no longer sweep up, by accident, the changes that raise no event: the Stream Deck's lighting and planning actions (`E/control_surface*.rs` raises only `audio.changed { control-surface }`), the daily backup appearing in the backups folder, the health sentences that describe lighting and audio state, the commissioning snapshot after `audio.settings.update` reset the audio probe. The moment the operator looks is when they are fetched: two to five requests per switch, was ten. (5) `import.meta.env.DEV` is not read inside `engine-client` — its tsconfig extends the base with no `vite/client` types, so the expression typechecks in the app's program and not in the package's own — but injected as `{ development }`; and "throws, caught by the boundary" is the fault kept in the state and rethrown by `useShellSnapshot` while rendering, because a boundary catches render errors only and a refresh is not one. (6) The band sits at the foot of the bay, against the footer, not inside it: the footer is 40 px of fixed chrome (plan D4) and a 58 px band cannot enter it without moving every board. It is rendered ahead of the surface and placed with `order`, so the surface stays the workspace stack's last child and no existing rule changes. (7) Files beyond the design's list: `domainRefresh.ts`, `reportUiFailure.ts`, `WorkspaceCrashProbe.tsx`, `BackgroundFailureBand.tsx`, the two CSS modules, the recovery surface, the fixtures and their check — the new code went into new files because of the 2,000-line cap (trap 11). (8) The setup-support lane was run although the slice lists only the workspace lane, because the slice changed what that lane exercises (the archive restore's five events, the restart, recovery mode, S8's `degraded`); it found the second coupling. Decisions recorded, not deviations: `shellState.recovery` `degraded` is still rendered nowhere and is left so — the band is for failures, a band for `attention` would put a new state on the three `setup-degraded` boards (D6), and the Support health sentence and the Deck lamp already carry that posture; the pre-ready surfaces sit inside the workspace boundary; the band names no cause (the ring's messages are written for whoever reads the diagnostics file, and the copy gate's words appear in them); `coerceAudioSnapshot` stays for telling a whole audio snapshot from a partial command result.

Gates before: every event and every non-audio request cost ten snapshot requests (twenty for a request whose event also arrived), a refresh that came back after the hardware link stopped wrote `lifecycle: "ready"` and cleared the start-up failure, replies were cast to their types unseen, a render error blanked the window, and the background-failure ring was visible only in a lane's diagnostics export (the Support export did not carry it). No test asserted a request count outside the audio path, and none rendered a component that throws.

Tests added: `EC/store/createShellStore.test.ts` (810 lines, was 372) — "event refreshes only mapped domains" (the measure: one `lighting.changed` → `lighting.snapshot` + `lighting.dmxMonitor.snapshot`; then all eleven events against a table written out in the test and typed `Record<EventName, string[]>`), "a staged restore refreshes support alone and a health transition app and health" (S7's and S8's hooks; `recovery` becomes `degraded`), "unknown event full refresh" (the eight, and the development warning), "catalog once per session" (one request across an event, a request and a restart; `refresh()` asks again and asks for all ten), "a request refreshes only what its method can change" (the four workspace switches, a section change, lighting, selection, planning, a probe, the seed, export, the Stream Deck profile, an applied restore), "events raised together are fetched as one batch" (the restore's five events: each snapshot once, was fifty requests), "events that arrive while a batch is out share the next one" (five events behind a held request → one more `lighting.snapshot`, not five), "a partial refresh writes only what it fetched" (workspace and recovery untouched by an event that names neither; a refresh that returns after `engine.exited` leaves `failed` and `ENGINE_EXITED` alone), "a request that fails does not cost the others their answers", "malformed snapshot rejected in dev" (`refresh()` rejects with the request and field named, the fault is in the state, the last good snapshot stays, the rest of the batch arrives), "a row without an id is refused by name", "a malformed snapshot in production is a failed refresh, never a throw", "every fixture scenario passes the guards" (a development store over every scenario of the fixture double); new `domainRefresh.test.ts` (lighting and its DMX monitor are never fetched apart, over every event, every request method, the four workspace switches and the two fallbacks; the catalog is never refreshed by a mapping and the deck's page model only by opening Setup; a method matches by its own name before its prefix; the workspace-switch rule) and `snapshotGuards.test.ts`; `FA/app/startup/ShellErrorBoundary.test.tsx` (11 cases: the root boundary untouched while nothing fails, the page with no store at all — Reload, Export diagnostics, the fixture file name — the export when the state cannot be read, a malformed reply shown by name in a development build; the workspace boundary adds no element, keeps a render error inside the area and brings the area back on reload; `reportUiFailure` records the same error object once whether the boundary or the window listener sees it first, and contains a sink that throws; the band is absent while nothing failed, says how many and since when, names no cause, returns after Dismiss only for a new failure, and survives an unparseable time; `readLogExcerpt`); Playwright `frontend/app/tests/shell.spec.ts` "workspace crash keeps shell alive" (`audio-populated` with `crash=lighting`: `LIGHTING STOPPED`, no root page, tabs, clock, lamps and the restart dialog alive, the band at one problem, Audio's Solo FX 3/4 toggles, Dismiss, the second failure counted, then the fault disarmed and **Reload this area** brings back the stage and the footer) and "the crash hook is absent unless the fixture URL asks for it". Tests changed: the supervision double in `createShellStore.test.ts` answered `{ protocol: "1" }` for every request → it answers `null` for the five typed snapshots (no console, rig or plan in that double) and the same object for the rest → the guards rightly refuse an object without its lists as a lighting or planning snapshot, and "keeps the last twenty background failures without leaving ready" would otherwise find those refusals in the ring first; the double also gained `hold(method)` and `snapshotRequests()`. Two fixture scenarios' `recentLogExcerpt` list → string.

Lanes: `npm run frontend:typecheck`, `npm run frontend:test`, `npm run build --workspace frontend/app && npm run frontend:storybook:build && npm run frontend:playwright:test`, `npm run tauri:workspaces:qualify`, `npm run tauri:setup-support:qualify`, `node scripts/check-operator-copy.mjs`, `npm run dev:check`. Done when one `lighting.changed` costs 2 requests (was 10) on a recording transport and a Lighting render error leaves Audio usable — both met (below).

Validation (workstation, 2026-09-17): baseline before editing — `frontend:typecheck` green, `frontend:test` 280 (app 70, design-system 175, engine-client 20, tokens 15). After: `frontend:typecheck` green; `frontend:test` 315 (app 81, design-system 175, engine-client 44, tokens 15); `node scripts/check-operator-copy.mjs` 0 hits; `npm run dev:check` green — format, lint, `scripts:test`, `file:health` (`createShellStore.ts` 1,517, `SetupSupportPilot.tsx` 1,689, nothing new over 2,000), rustfmt, clippy, `protocol:check` (no contract change), typecheck, the 315 Vitest cases, `native:check`, `native:test` 380 passed / 1 ignored (tauri-shell 20, engine 351 + 1, `contract.rs` 4, `end_to_end.rs` 5), unchanged. Playwright against the rebuilt `dist` and `storybook-static`: 417 passed in 2.8 min (415 + the two new cases), the 81 UI-contract boards, `visual-review` and `storybook` among them; run twice, the second time on the final maps. Both qualification lanes run the development store (they start `tauri dev` over the Vite dev server), so they are also the proof that every live reply passes the guards. `tauri:workspaces:qualify`: **failed on the first run** — "Expected live lighting DMX monitor snapshot to expose channels": after publish, `setWorkspace("lighting")` no longer dragged a full refresh along, `commissioning.changed` refreshed lighting without its monitor, and the monitor stayed as empty as the clean runtime had left it; `LIGHTING_DOMAINS` now composes every list that names lighting, and `domainRefresh.test.ts` holds the rule. It then passed in 17.7 s with its five checks (audio receive ports 55939 and 52897, throwaways; nothing written to 7001–7010), and again on the final maps. `tauri:setup-support:qualify`: **failed on its first run**, nine checks in, in the database-restore step — "Expected a third planning project after the backup was written": `commissioningSnapshot.planningProjectCount` stayed at 2 because a planning change refreshed planning alone; `PLANNING_DOMAINS` now carries commissioning. It then passed in 49.4 s (63.4 s in S8) with all sixteen checks over seven steps: `shell-stays-responsive-during-stalled-request` `probeMs` 1912 / `heartbeats` 9 / `maxGapMs` 261, `backup-restore-round-trips-native-support-backup`, `database-backup-verifies-and-restores-after-restart` at schema 6, `corrupt-db-restores-from-database-backup` at generation 2, `engine-crash-reaches-recovery-and-restarts` `detectMs` 102 / `restartMs` 1124, `second-instance-is-refused` by the plugin in 100 ms, `bridge-port-taken-reads-as-health-attention` — port 53871 held, `healthStatus` `attention`, `recovery` `degraded` through the scoped `app.changed` map. After the two failures the snapshot builders were read for every input rather than waiting for a third: the app snapshot reads shell, commissioning-stage and planning settings (Planning uses its planning section only to pick a loading skeleton), health reads the two probe-derived checks, the bridge state and the registry, commissioning reads the probe records, the bridge and console addresses and the planning counts, the DMX monitor is a function of the lighting snapshot, support lists the backups folder, the deck's page model and the catalog take no input; the maps above are that table. In the dev build the flow was walked by hand in the Browser pane at 2560 × 1440: the area display and the band beside a live header, the band's text at 5.4:1 or better and 12 px in Studio, Graphite and Bone with a 28 px Dismiss key, the root page rendered from the real component into a scratch root (fills the window, Reload and Export diagnostics answer). **Production app (the standing permission of 2026-09-17):** the packaged app was open on 38201 (shell 65144, engine 63660, both started 09:58:10 from `release\native\windows\…`); the database had not been written since 10:51:06. The shell alone was ended with `taskkill //F`; the engine exited on its own within a second, 38201 was free, TotalMix FX and Companion were not touched. What did not happen: the engine left **no shutdown backup and no log line** for that exit — the newest backup is `db-2026-09-17T08-51-18-418Z-shutdown.sqlite3` (10:51:18, after the last database write, so nothing was lost), and `engine.log` ends on its minute-by-minute "401: a bearer token is required" from Companion (Appendix B item 1 is unsigned, so the deck is refused by that build). The procedure's expectation that a force-ended shell gives the engine its graceful path did not hold for the packaged build; the shell has no job object (`T/engine.rs` sets `CREATE_NO_WINDOW` only), and the cause was not pursued — it is outside this slice. The app stays closed until the operator reopens it.

Queue follow-up (workstation, 2026-09-17; commit `c87565a` `Readiness S9 (queue): a restart abandons a refresh batch that never settles`): found reading the queue again after the slice commit was pushed, not by a lane. `runRefresh` keeps one batch in flight and `refreshRunning` said so until the batch settled — so a request that never settled would have held every later refresh behind it, and `cancelQueuedRefresh` cleared what was queued but not that flag, so **Restart the hardware link**, the operator's remedy for a screen that has stopped updating, would not have freed it. Before this slice a refresh that hung held only itself. It should not happen on the hardware link — `T/engine.rs` gives up on a request after ten seconds and fails what is pending when the engine stops — which is why no lane could find it; the property is cheap to hold anyway. `createShellStore.ts` (1,532 lines): a run counter (`refreshRun`) that every bootstrap and dispose advances; a batch belongs to the run that sent it, an abandoned run drops its answers and leaves the flag to the run that replaced it, and the requests waiting on the abandoned batch are released with the queued ones. Test added: `createShellStore.test.ts` (849 lines) "a restart abandons a batch that never settles" — a `lighting.snapshot` that never answers, a request queued behind it, an event that gets nothing through; after `restart()` the waiting request resolves and the next `planning.changed` fetches its two snapshots. Run against the slice commit's store it fails (`expected [] to deeply equal [ 'commissioning.snapshot', … ]`), so it guards the fix; the double gained `stall(method)`. Validation: `frontend:typecheck` green; `frontend:test` 316 (app 81, design-system 175, engine-client 45, tokens 15); Playwright on the rebuilt `dist` 417 passed in 2.8 min; `tauri:setup-support:qualify` passed in 50.1 s with all sixteen checks (its restarts — the database restore, the corrupt-database restore, the engine crash — are the paths the change touches); `tauri:workspaces:qualify` passed in 18.4 s with its five checks (audio receive port 52006, a throwaway); `npm run dev:check` green.

Baselines: none. No board moved — the band is absent while nothing has failed, the boundary adds no element, and the recovery log excerpt renders only on `RecoverySurface`, which no fixture board reaches (a failed start on a fixture is on the Setup tab and shows `SetupRecoverySurface`); no story was added. The visual lanes ran (above) and no snapshot or ratchet file changed.

Operator hands: none. Notes for later slices: S10 — the Stream Deck's lighting and planning actions raise no event (`E/control_surface*.rs`: only `audio.changed { control-surface }`), so a deck key pressed while Lighting is already open shows only at the next lighting event, request or tab switch; `handle_light_action` is S10's file, and `lighting.changed { reason: "control-surface" }` from there (and `planning.changed` from the planning actions) closes it. S11 — `lighting.output.setArmed` falls under the `lighting.` prefix and its event under `lighting.changed`; a new event fails the typecheck until it is mapped, a new method falls back to the eight until it is; `recentEvents` on the support snapshot is refreshed by `support.changed` and by opening Setup — decide whether recording an action should raise `support.changed`. S13 — the request-count assertions in `createShellStore.test.ts` are the fan-out's regression guard; `audio.settings.update` with a changed console address resets the audio probe engine-side while the audio path refreshes audio alone (older than this slice; opening Setup now fetches the rest). S14 — the fixture double does not implement `lighting.fixture.identify`, `.highlight`, `.identifySequence`, `.identify.clearAll`, `lighting.scene.reorder`, `.pin`, `lighting.group.reorder` and `lighting.fixture.delete` (it answers `{}` and raises nothing); the split is the moment to make it answer as the hardware link does. S15 / operator — the packaged production build predates S5 and S8 and does not shut down as `docs/OPERATIONS.md` describes when its shell is force-ended (above); repackaging from this branch once the Appendix B items are signed is the fix, and the close procedure in the session prompt should say "ends without a shutdown backup on the packaged build" until then.

Correction (2026-09-17, recorded by the Slice 10 session; what Slice 9 wrote above stands as written): the cause that Slice 9's Validation says "was not pursued" is known. The packaged production build under `release\native\windows\SSE ExEd Studio Control Native\` is not merely one that "predates S5 and S8" — it is the S2-era build: `grep -a -c` on its engine finds `control-surface.token` and none of `pre-migration`, `engine.lock`, `ENGINE_ALREADY_RUNNING`, `restore-pending`, `engine.log.1` (engine exe 2026-09-10 16:08, the S2 packaging run; shell exe 2026-09-04). It has no database backup code at all, no integrity check, no single-instance guard and no supervised restart, so ending its shell leaves no shutdown backup and no log line by construction — expected, not a fault of the close, and not a regression of S3 or S5. The `db-*-shutdown.sqlite3` files of that morning can therefore only have been written by a newer build run against the same app-data (this one cannot write them). Nothing was lost on 2026-09-17, as Slice 9 recorded: the database had been idle since 10:51:06, and the 401 lines from Companion in its log are Appendix B item 1, still unsigned. Re-checked on 2026-09-17 by the Slice 10 session with the same `grep` on the same file, with the same result. The note for S15 / the operator above is otherwise right: repackaging from this branch is the fix, and it is the operator's step (Slice 10, Operator hands).

CI (result, 2026-09-17): run 35208750697 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/35208750697), event `push`, sha `96ae4e9`, created within seconds of the push; 11.8 minutes end to end. Green on the first attempt: `lint` (0.4 min), `frontend-test` (0.5 — the 315 Vitest cases: app 81, design-system 175, engine-client 44, tokens 15), `frontend-typecheck` (0.6 — the fixture check with the string excerpt among it), `format-protocol` (1.4 — `protocol:check` with no contract change, `file:health` with `createShellStore.ts` at 1,517 lines), `tauri-foundation` (2.4), `rust` (2.8 — rustfmt, clippy, check, the 380 tests on Linux (tauri-shell 20, engine 351 + 1 ignored, `contract.rs` 4, `end_to_end.rs` 5), unchanged, plus `native:acceptance`) and `qualification` (9.3 — both lanes run the development store under xvfb, so every reply of the Linux engine passed the guards too: the setup-support lane's seven steps in 295.1 s with all sixteen checks — `shell-stays-responsive-during-stalled-request` `probeMs` 2576 / `heartbeats` 10 / `maxGapMs` 513, `backup-restore-round-trips-native-support-backup`, `database-backup-verifies-and-restores-after-restart` at `schemaVersion` 6, `corrupt-db-restores-from-database-backup` at generation 2, `engine-crash-reaches-recovery-and-restarts` `detectMs` 326 / `restartMs` 1579, `second-instance-is-refused` by the plugin once the Linux shell had started (25.1 s), `bridge-port-taken-reads-as-health-attention` — port 35937 held, `healthStatus` `attention`, `recovery` `degraded` through the scoped `app.changed` map; the workspace lane in 89.9 s with its five checks, the audio probe overridden as on every CI run — `overrideProbes: true`, `audio-live-mutations-skipped`). Red, as expected and not chased: `frontend-e2e` (10.2 min) — 94 failed / 323 passed of 417 (415 + the two S9 cases): the 91 linux snapshot cases (52 `visual-review.spec.ts` + 39 `storybook.spec.ts`, stale or missing until Appendix B item 6; S9 added no board, so none is new), the constant `audio-arm-countdown.spec.ts:45` dwell case, and two runner-timing cases from the S13 list on this run instead of one — `audio-talkback.spec.ts:64` and `audio.spec.ts:1054`. Because S9 touched the store's audio refresh, each was compared with its own failure on a run that predates the slice (the S7 run 34602095638 and the S8 run 35201156819) rather than accepted by title: the same locator, the same line (70 and 1093) and the same five-second wait for `aria-pressed` / `data-armed` — unchanged. Both new S9 cases, "workspace crash keeps shell alive" and "the crash hook is absent unless the fixture URL asks for it", passed on Linux: neither title is among the failures and nothing was skipped. No new candidate.

CI (result, 2026-09-17, queue follow-up): run 35209951483 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/35209951483), event `push`, sha `c87565a`, pushed only after the first run had finished so that neither cancelled the other; 11.8 minutes end to end. Green on the first attempt: `lint` (0.5 min), `frontend-test` (0.6 — 316 Vitest cases, engine-client 45 with "a restart abandons a batch that never settles" among them), `frontend-typecheck` (0.6), `format-protocol` (1.2 — `file:health` with `createShellStore.ts` at 1,532 lines), `tauri-foundation` (1.9), `rust` (2.0 — the 380 tests, unchanged) and `qualification` (9.8 — the setup-support lane in 295.2 s with all sixteen checks: `shell-stays-responsive-during-stalled-request` `probeMs` 2583 / `heartbeats` 11 / `maxGapMs` 295, `engine-crash-reaches-recovery-and-restarts` `detectMs` 431 / `restartMs` 1580, `second-instance-is-refused` by the plugin after 25.2 s, `bridge-port-taken-reads-as-health-attention` on port 46205 with `attention` / `degraded` — its three restart paths are what the queue change touches; the workspace lane in 89.8 s with its five checks, the audio probe overridden). Red, as expected: `frontend-e2e` (7.9 min) — 92 failed / 325 passed of 417, the tightest shape on this stack so far: the same 91 linux snapshot titles as the first run (the two lists compared, identical) and the constant `audio-arm-countdown.spec.ts:45` dwell case, with no runner-timing case at all this time — the two that failed on the first run passed here, on a store that differs only in the queue's run counter. Nothing is pending on S9.

## Slice 10 — Lighting concurrency + sACN render path

Status: landed 2026-09-17 (operator verification pending, Appendix B item 5; commit `957acc9` `Readiness S10: lighting state lock, deck keys through the shared preview, sACN settings cache, kept read connections`, pushed; CI run 35230069147 recorded under "CI (result)" below by the follow-up commit `Readiness S10 (ledger): CI run on the branch recorded`, as in S0–S9; every S10 lane, the setup-support lane, the `dev-fixtures` build and `npm run dev:check` green on the workstation; the slice has hardware exposure — the sACN render path and the Stream Deck's lighting keys — so it ends as `landed` and waits on Appendix B item 5, which is walked together with S11 and does not block it). Closes F12 F18. Depends on S2 S3.

Scope (as landed): **The lock, the preview, the generation (F12).** New `E/lighting/state_lock.rs` (103 lines): `static LIGHTING_STATE_LOCK: Mutex<()>` with the lock-order rule in the comment beside it (the lighting state lock first, then the shared preview; a reader takes the preview alone; never held together with `AUDIO_STATE_LOCK`; not re-entrant); `with_lighting_state(|| …)` takes the lock, runs the whole mutation — its load, its change, its persist — and advances `LIGHTING_RENDER_GENERATION` while still holding it, whether the mutation succeeded or was refused (a guard's `Drop`, so a panic advances it too); `with_lighting_state_and_preview(|preview| …)` is the same with the shared preview taken second and handed in; `shared_lighting_preview()` (a `OnceLock<Mutex<LightingPreviewRuntimeState>>`, as `shared_console_link()` is), `lock_shared_lighting_preview()` for readers, `lighting_render_generation()`, `bump_lighting_render_generation()`; under `cfg(test)` `reset_shared_preview()` and `shared_preview_test_guard()`. `E/app.rs` (1,712 lines, was 1,874): `EngineApp.lighting_preview` and its accessor are gone; `dispatch_lighting_preview_mutate` runs its handler inside `with_lighting_state_and_preview`, `dispatch_lighting_mutate` inside `with_lighting_state`; `read_lighting_snapshot` takes the shared preview and then reads the settings, so the pair is from one side of any preview-aware mutation; the archive restore (`support.backup.restore`) and the dev parity fixture run inside `with_lighting_state`; the commissioning check advances the generation after its write. **Every writer of what the lighting state or the wire depends on, and who holds the lock:** (a) the lighting mutations in `E/lighting/` (about sixty load → persist sites) — reached only through the two dispatchers (IPC thread) and the deck's lighting handler (four bridge workers), all three under the lock; the functions themselves take none, so `*_with_preview` can fall through to the plain function and a recall can record its own failure; (b) the Stream Deck's lighting keys — under the lock, selection keys included (a read-then-write of the selected id); (c) the archive restore, which deletes and rewrites every `app.lighting.*` key in one transaction — under the lock, so a deck key cannot write its older copy back over it (it touches audio settings too and takes no audio lock, so the two locks are never held together); (d) the dev parity fixture (`dev-fixtures` builds only) — under the lock; (e) `commissioning.check.run`, which stores the bridge address and the universe a lighting probe used (`app.commissioning.lighting.*`) and the probe's status — single upserts, never a read-modify-write of the lighting state, so no lock; the generation is advanced after it, because the address and the universe are part of what the wire carries; `commissioning.update` writes the stage keys only, which no lighting read uses — no lock, nothing advanced; (f) the schema migrations, the default seeding and the staged database restore — before any thread exists; (g) `storage.importLegacyDb` — planning rows, planning settings, the workspace and the commissioning stage, no lighting key; (h) `record_lighting_action_failure` — three upserts from inside a lighting function, already under the caller's lock; (i) the deck's `last_event` stamp — one upsert of its own key. `save_lighting_editor_state`, the whole-state writer the deck used, is `cfg(test)` now: outside the tests the state can only be written by a lighting function. **The deck through the screen's functions (F12).** `E/control_surface.rs` (1,202 lines, was 1,451): `handle_light_action` answers `switchToDeckMode` as before (a planning setting) and runs every other key inside `with_lighting_state_and_preview`; `locked_light_action` builds each request with the `parse_lighting_*_request` functions and calls `update_lighting_fixture_with_preview` (toggle, intensity ±5, colour temperature ±200 K, the two resets), `set_lighting_all_power_with_preview`, `recall_lighting_scene_with_preview` — and nothing after it —, `create_lighting_scene_with_preview` and `delete_lighting_scene`. The relative keys start from new `lighting::read_lighting_fixture_levels(settings, preview, fixture_id)` (`E/lighting/preview.rs`, 601 lines): the preview buffer while previewing, otherwise the stored value with a running fade sampled now, never the identify / highlight / solo overlays. Replies of the value, all-power and recall keys carry `preview`; a value key's reply carries what was stored. The `light_intensity` and `light_cct` LCD texts read the same values under the preview guard and end in a third line `PREVIEW` while previewing; the LCD no longer renders a whole lighting snapshot it never used on every poll. **Events from the deck (S9's hook).** `E/engine_events.rs` (69): `emit_lighting_changed`, `emit_planning_changed`; `handle_control_surface_http_action` raises `lighting.changed { reason: "control-surface" }` after a successful lighting key and `planning.changed { reason: "control-surface", projectId: null, taskId: null }` after a planning key or `switchToDeckMode` (`deck_change_event`; `openDetail` and the audio route raise nothing new). **The render path (F18).** `E/lighting_sacn_output.rs` (556, was 332): `RenderSettingsCache::settings_for_tick(generation, now, read)` reads the database when the generation has moved since the last read or `SETTINGS_SAFETY_REREAD` (2 s) has passed, keeps the last good settings through a failed read and retries on the next tick; the loop renders every 40 ms tick from the kept settings — settings, never frames, so fades and identify bursts still move with the clock; `send_frame`, `terminate_universes`, the 800 ms keep-alive and the packet layout are untouched. The S8 health report was already there. **Kept read connections (F18).** `E/storage.rs` (994, was 889): `enable_thread_read_connection()` opts a thread in; `list_settings_by_prefix` then reads through `with_read_connection` — one connection per thread, opened read-write and made `query_only` with the 5 s busy timeout, the journal mode and `synchronous` left as the file has them, replaced when the path differs, dropped after a failed read, no statement held between calls; `checkpoint_database()` (`PRAGMA wal_checkpoint(TRUNCATE)`, an error when a reader blocked it). Opted in: the sACN thread, the metering thread (`E/rme_totalmix_osc.rs`, the reader switch) and the engine's bridge workers (`E/control_surface_http.rs` 1,375: `BridgeContext::keeping_read_connections`, set by `start_control_surface_bridge` only; the worker loop moved into `serve_queued_connections`). **The write-ahead log.** Threads that keep a connection never close it, so SQLite's checkpoint-on-last-close no longer happens: `E/main.rs` (622) checkpoints after the shutdown backup, and `bootstrap::apply_pending_restore` (`E/bootstrap.rs` 1,316) checkpoints the database before it moves it aside and deletes its `-wal` file, best effort (a database that cannot be opened is kept as it is, with a WARN line). Test modules moved out under the 2,000-line guard, paths unchanged: `E/app/tests.rs` (350), `E/control_surface/tests.rs` (621); new `E/lighting/tests_state_lock.rs` (238). Docs: `native/protocol/v1.md` (the lighting keys, the `preview` field, the two `control-surface` reasons — no contract change, both events exist), `docs/OPERATIONS.md` (Lighting Output, Data Safety), `docs/DEVELOPMENT.md` (the lock order, the generation, the kept connections, the checkpoint rule, the test guard), `CHANGELOG.md`. No front-end source changed: the store already maps both events (S9).

**Rescope:** ten deviations, none silent, and the title stands because what landed is what it says. (1) `with_lighting_state` wraps the entry point's whole mutation; it does not take `db_path` and does not load or persist itself. The design's sentence reads both ways; the other reading rewrites about sixty load → persist sites into closures for the same serialisation, because every one of those sites is reached only through the two dispatchers and the deck's handler. The writer list above is the argument, and `save_lighting_editor_state` going `cfg(test)` is what keeps it true. (2) Files: the lock, the generation and the shared preview live in a new `lighting/state_lock.rs` (the design put them in `helpers.rs` and `preview.rs`) so the order rule sits beside everything it governs; `lighting/helpers.rs` and `lighting/palettes.rs` from the design's list did not change; `engine_events.rs`, `control_surface_http.rs`, `bootstrap.rs`, `main.rs`, `lighting/editor_state.rs` and the three test files are beyond it. (3) The deck: `saveScene` and `deleteScene` go through the screen's functions too, which the design does not list. The deck keeps its scene name (`Scene N`); the id comes from `next_custom_scene_id` (highest + 1) — the deck's own rule counted the scenes and, after a delete, gave a new scene the id of a live one. `allOn` / `allOff` with no fixture is refused (409) where it answered ok and did nothing. While previewing, the LCD shows the preview buffer with a `PREVIEW` line rather than the live value: a key whose display does not move reads as a dead key, and a staged number without the word would read as the rig's. (4) The generation advances on every exit from `with_lighting_state`, refusals included, and by hand after the commissioning check (the one writer outside the lighting functions that changes what the wire carries: the bridge address and the universe); a restore and the parity fixture advance it through the lock; anything else is the 2 s safety read's. "≤ 1 read per state change" is therefore: one read per generation change, plus one per two seconds, plus the first — which is what the counting test asserts, with the generation and the clock handed in. (5) The kept connection is a per-thread opt-in inside `list_settings_by_prefix`, not a second function called from every settings read the bridge workers reach across `control_surface.rs` and `control_surface_audio.rs`; a thread that did not opt in — the IPC loop, every test thread, a test bridge's workers — behaves exactly as before. (6) The two checkpoints are not in the design. They follow from trap 7: without them a graceful stop leaves the newest commits in `-wal`, and a restore after an engine that was ended rather than stopped kept a `replaced` copy without them — that second gap exists at `96939dc` too (the new bootstrap test fails without the call), the kept connections would only have made it the normal case. (7) The events from the deck are S9's hook, not the design's. (8) The concurrency test is 2 × 20 operations through the real entry points in `app::tests`, not 2 × 200 in `lighting::tests`. A commit waits for the disk (S3) — about 35 ms here, more when a per-call connection is the last one and its close checkpoints — and the first version (two hundred group creations against two hundred scene saves) passed in 303 s. Twenty group creations against twenty presses of the intensity key (0 % → 20 × 5 = 100 %) pass in 2.6 s and fail every time without the lock, because each side reads while the other commits. (9) Lanes beyond the design's list: `tauri:setup-support:qualify` (the backup, restore and restart paths the kept connections could disturb), the `dev-fixtures` build (the parity dispatcher compiles only there), and the packaging lane run with the production folder moved aside (below). (10) The test modules of `app.rs` and `control_surface.rs` moved to their own files: `app.rs` was 126 lines under the cap with S11 still to come, `control_surface.rs` reached 1,825 with the new tests. Decisions recorded, not deviations: a restore does not clear the preview (the buffer is keyed by fixture id and a missing id is ignored); the deck's `deleteScene` with nothing selected still acts on the first scene, which is the scene its LCD shows as selected; `SACN_PORT` stays a constant — the loop's read and render decisions are tested as values and no test sends a frame, so S11's `held_outputs_send_nothing` is the slice that makes the destination injectable.

Gates before: no test pressed a lighting key on the deck at all (`control_surface.rs` tested LCD text, audio and the verify echo), none ran two lighting mutations at once, none counted the sACN thread's reads, and `pending_restore_applied_after_snapshot` staged empty `-wal` / `-shm` files, so nothing noticed that the log is deleted with whatever it holds. The deck loaded the whole lighting state, changed it and saved the whole of it back with no lock, no preview and no event; its recall saved its pre-recall copy over what the recall had just written; the sACN thread opened the database twenty-five times a second.

Tests added (16; engine 351 → 367): `app::tests` — `deck_and_ipc_mutations_serialize_without_lost_updates` (twenty `lighting.group.create` requests through `handle_request` against twenty `intensityUp` keys through the bridge's action handler, a third thread reading `lighting.snapshot` throughout: twenty more groups, every id once, the fixture at 100 %), `ipc_preview_mode_is_the_preview_the_deck_edits` (`lighting.editor.previewMode` over IPC, a deck `toggleLight`: `previewFixtures` shows it, `fixtures` and the DMX monitor do not move; after `previewDiscard` the same key moves the rig); `control_surface::tests` — `deck_preview_action_updates_preview_runtime` (intensity, toggle, colour temperature, all-on and recall while previewing: the shared buffer holds each change, the stored fixture and every one of the 512 slots the wire would carry are unchanged, no recall is recorded, the LCD reads `INTENSITY\n100%\nPREVIEW`; out of preview the same key moves the stored value and the frame), `deck_relative_keys_store_through_the_fixture_update` (97 % + 5 = 100, the reply and the LCD carry the stored colour temperature, which the fixture's own range caps below the deck's 6500 K), `deck_recall_writes_what_a_recall_writes_and_nothing_after_it` (a 10 s fade from the screen, then the deck's recall: no fade left, every fixture at the scene's values), `deck_save_scene_never_reuses_a_live_scene_id` (save, save, delete the first, save: every id once, `scene_order` kept, the deck selects what it saved), `deck_light_and_planning_actions_raise_their_events` (the one test that installs the process-wide event sender: a refused key raises nothing, a lighting key `lighting.changed`, the deck mode and a planning key `planning.changed`, all `control-surface`; plus the `deck_change_event` table); `lighting::tests_state_lock` — `with_lighting_state_advances_the_render_generation` (success, refusal, the preview variant, the bare bump — asserted as advanced, never as a number), `fixture_levels_are_the_stored_values_with_a_finished_fade_applied` (a fade that ran out twenty seconds ago: the stored value is still the origin, the levels are the end values), `fixture_levels_follow_the_preview_buffer_while_previewing`, `writers_and_readers_of_the_shared_preview_never_wait_on_each_other_forever` (two writers × 200 and a reader × 200; a lock-order mistake hangs it); `lighting_sacn_output::tests` — `rerenders_settings_only_on_generation_change` (a second of ticks, one read; one change, one read; the safety read at two seconds and not a millisecond before; a failed read keeps the last good settings and is retried; nothing read yet → nothing to render), `render_frames_identical_before_and_after_cache` (frames from a fresh read, from the first tick and from ten ticks later are byte-identical; 40 % is DMX 102, an unlit fixture 0 with its colour temperature slot still set; the reader panics if an unchanged generation reads); `storage::tests` — `thread_local_read_connection_reused` (no connection kept without the opt-in; twenty-five reads, one open; another path replaces it; opting out closes it and the directories can be removed), `kept_read_connection_sees_later_writes_and_blocks_neither_backup_nor_checkpoint` (a later commit is visible, a write through it is refused, a failed read opens a fresh one, a backup copy and a checkpoint go through while it is open and the `-wal` file is empty afterwards); `bootstrap::tests::pending_restore_folds_the_write_ahead_log_into_the_replaced_copy` (the file and the log of a killed engine, copied as they are on disk; the premise — the file alone lacks the commit — is asserted; the replaced copy carries it and the restored database is the backup). **Each guard proved against the code without its fix:** the concurrency test with the lock taken out of `with_lighting_state` failed 3 of 3 runs (the fixture at 75 %, then 21 of 22 groups twice); the recall and save-scene assertions, run against an export of `native/` at `96939dc`, both failed (the fade still there; `scene-custom-5` handed out twice); the bootstrap test without the checkpoint read `checkpointed` where `only-in-the-log` was due; the event test without the emit failed on `lighting.changed`. Tests changed: none in substance — the three `app.rs` tests and the eleven `control_surface.rs` tests moved with their modules; `app_for` builds `EngineApp { runtime }`.

Lanes: `npm run rust:clippy`, `npm run native:test`, `cargo clippy` and `cargo test -p studio-control-engine --features dev-fixtures`, `npm run protocol:check`, `npm run tauri:workspaces:qualify`, `npm run tauri:setup-support:qualify`, `npm run native:engine:build && npm run tauri:build`, then `npm run native:package:win:local && npm run native:bridge:win:verify` with the production folder moved aside, `npm run dev:check`. Done when the sACN thread performs ≤ 1 database read per state change (the counting reader; see Rescope 4 for what the sentence has to mean), a deck action while previewing edits the preview buffer and not live output, and the concurrency test is green — all three met (below).

Validation (workstation, 2026-09-17): baseline before editing — `rust:clippy` clean, `native:test` 380 passed / 1 ignored (tauri-shell 20, engine 351 + 1, `contract.rs` 4, `end_to_end.rs` 5); 38201, 4173 and 6007 free, the production app closed since the S9 session. After: `cargo fmt` applied; `rust:clippy` clean under `-D warnings`; `native:test` 396 passed / 1 ignored (tauri-shell 20, engine 367 + 1, `contract.rs` 4, `end_to_end.rs` 5); the engine's unit suite five more times in a row, 367 each time (the preview, the generation and the event sender are process-wide); it takes 7–9 s now, was 3.7 s, because the new deck tests commit to disk; the `dev-fixtures` build: clippy clean, 369 + 1 / 4 / 5; `protocol:check` green with no contract change. The qualification and packaging lanes ran twice: a first time, green with the same checks, before the last two edits (the deck's LCD reading the settings under the preview guard, with the `control_surface.rs` test module moved out; and `commissioning.update` going back to its `96939dc` shape once it was clear that it writes nothing the wire depends on), and again on the final tree after the unit lanes had been repeated on it — the figures here are the second run's. `tauri:workspaces:qualify` passed in 20.6 s with its five checks (audio receive port 54939, a throwaway; `overrideProbes: false`). `tauri:setup-support:qualify` passed in 47.5 s with all sixteen checks over seven steps: `shell-stays-responsive-during-stalled-request` `probeMs` 1860 / `heartbeats` 9 / `maxGapMs` 258, `backup-restore-round-trips-native-support-backup` (the archive restore, now under the lighting lock), `database-backup-verifies-and-restores-after-restart` at schema 6 with its `replaced` and `pre-restore` files (the staged restore, with the checkpoint in front of it), `corrupt-db-restores-from-database-backup` at generation 2 (the restore whose replaced file is junk, which by the code takes the checkpoint's best-effort branch — the lane keeps no engine log to show the WARN line), `engine-crash-reaches-recovery-and-restarts` `detectMs` 203 / `restartMs` 1221, `second-instance-is-refused` by the plugin in 101 ms, `bridge-port-taken-reads-as-health-attention` on port 54055 with `attention` / `degraded`. Both lanes run the development store, and both ran the engine with its three kept read connections. Hardware: the lanes commission their rig at loopback (the setup-support lane's stalled probe goes to `203.0.113.113`, a documentation address no route leads to), so no sACN frame left for the studio bridge; nothing wrote to 7001–7010 beyond the lane engines' own once-a-second pulls with their default settings, which are the pulls the real app makes, as in S6 and S7; TotalMix FX and Companion untouched. **Packaging lane (the session prompt's procedure):** `native:engine:build` (debug engine 2026-09-17 15:50:33, 9,195,008 bytes) and `tauri:build` (release shell 2026-09-17 15:41:28, 10,358,784 bytes; it was the 2026-09-10 20:17 build, older than S5 and S8) first, so the lane verified this slice's code; then, with the app closed, `release\native\windows` renamed to `release\native\windows.production-keep`; `native:package:win:local` exit 0 (the new folder carried the two exes just built); `native:bridge:win:verify` exit 0 — the packaged engine on the reserved port 59635 with a temporary app-data root, its four steps passed (bind, LCD and action routes with the token, the refusals and the percent-decoding, the exported profile), its bridge workers keeping their read connections; then the new `release\native\windows` deleted and the kept folder renamed back (the same on both runs). Production is what it was, to the byte and the second: shell `sse-exed-tauri-shell.exe` 9,823,232 bytes 2026-09-04 16:44:46, engine `studio-control-engine.exe` 8,801,792 bytes 2026-09-10 16:08:06; no `windows.production-keep` left; 38201 free and no engine process afterwards. `npm run dev:check` green before the commit.

Baselines: none. No front-end source changed and no board moved; the visual lanes were not run.

Operator hands: Appendix B item 5, walked together with S11 (it needs `SSE_SAFE_START`): with the rig in a state that may be lit, switch Preview on in Lighting and press the deck's intensity, toggle and recall keys — the rig must not move, the deck's displays must read `PREVIEW` on a third line, and the page's preview must show each press at once; switch Preview off and press again — the rig follows, and so does the page. Also the operator's, and not done here: **promoting a build from this branch to production.** The packaged app under `release\native\windows` is still the S2-era build (the correction under Slice 9); `npm run native:engine:build && npm run tauri:build && npm run native:package:win:local` replaces it, after which Appendix B items 1–5 are walked on that build; until then the running app has none of S3–S10. Notes for later slices: S11 — take the lighting lock around `lighting.output.setArmed` like any lighting mutation (its dispatcher arm does it) and let the armed flag ride the generation, so the hold reaches the wire within a tick; `held_outputs_send_nothing` needs the sACN destination injectable (`SACN_PORT` is a constant and `send_frame` builds the address itself) — inject the port, keep the packet path byte-identical; `record_event` from a deck key belongs inside `locked_light_action`, where the preview and the outcome are known (a staged change is not a change of the rig — decide whether it is recorded, and with which source); `app.rs` is 1,712 lines and `control_surface.rs` 1,202. S13 — the new deck tests cost the unit suite four to five seconds of disk commits on the workstation; if the suite's time starts to matter, a `cfg(test)` relaxation of `synchronous` is the lever, not fewer assertions. S15 — `docs/OPERATIONS.md` now says what a copy of the database by hand needs after a close that was not graceful.

CI (result, 2026-09-17): run 35230069147 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/35230069147), event `push`, sha `957acc9`, created within seconds of the push; 11.5 minutes end to end. Green on the first attempt: `lint` (0.5 min), `frontend-test` (0.5 — the 316 Vitest cases, unchanged: app 81, design-system 175, engine-client 45, tokens 15), `frontend-typecheck` (0.5), `format-protocol` (1.2 — `protocol:check` with no contract change, `file:health` with `app.rs` at 1,712 lines, `control_surface.rs` at 1,202 and nothing new over 2,000), `tauri-foundation` (2.1), `rust` (2.7, inside its usual 2.0–2.8 — rustfmt, clippy, check, the 396 tests on Linux (tauri-shell 20, engine 367 + 1 ignored, `contract.rs` 4, `end_to_end.rs` 5) with all sixteen new cases among them, plus `native:acceptance`; nothing hung, so the lock order holds on Linux as it does on Windows; the engine's unit suite took 16.2 s there, the new deck tests' disk commits again) and `qualification` (9.3 — both lanes under xvfb with the engine's three kept read connections: the setup-support lane's seven steps in 291.2 s with all sixteen checks — `shell-stays-responsive-during-stalled-request` `probeMs` 1933 / `heartbeats` 9 / `maxGapMs` 263, `backup-restore-round-trips-native-support-backup` (the archive restore under the lighting lock), `database-backup-verifies-and-restores-after-restart` at `schemaVersion` 6 with its `replaced` and `pre-restore` files (the checkpoint in front of the move), `corrupt-db-restores-from-database-backup` at generation 2, `engine-crash-reaches-recovery-and-restarts` `detectMs` 312 / `restartMs` 1436, `second-instance-is-refused` by the plugin once the Linux shell had started (25.2 s), `bridge-port-taken-reads-as-health-attention` on port 36747 with `attention` / `degraded` and "Address already in use (os error 98)"; the workspace lane in 88.1 s with its five checks, the audio probe overridden as on every CI run — `overrideProbes: true`, `audio-live-mutations-skipped`). Red, as expected and not chased: `frontend-e2e` (10.0 min) — 93 failed / 324 passed of 417, and 417 did not move: the 91 linux snapshot cases (52 `visual-review.spec.ts` + 39 `storybook.spec.ts`; the title set compared with the S9 run on `c87565a`, identical — S10 changed no front-end source, so no board could move), the constant `audio-arm-countdown.spec.ts:45` dwell case, and one runner-timing case from the S13 list, `audio.spec.ts:1054`. It was not accepted by title: its error block was compared with its failure on the S8 run 35201156819 — the same locator (`audio-inspector-hardware-mini` › the 48 V button), the same line 1093, the same five-second wait for `data-armed` — unchanged. No new title. Nothing is pending on S10 but Appendix B item 5.

## Slice 11 — Audit trail + safe start

Status: landed 2026-09-17 (operator verification pending, Appendix B item 5; commit `9c45d36` `Readiness S11: action log with sources, held light outputs and safe start, schema 7`, pushed with the baseline refresh `4b89191` `Readiness S11 (baselines): Support plate with the Light outputs switch and Recent actions`; CI run 35261830154, on the baselines commit, recorded under "CI (result)" below by the follow-up commit `Readiness S11 (ledger): CI run on the branch recorded`, as in S0–S10; every S11 lane, the `dev-fixtures` build, the packaging lane with the production folder moved aside and `npm run dev:check` green on the workstation; the slice has hardware exposure — the hold on the light output, the Stream Deck's handlers — so it ends as `landed` and waits on Appendix B item 5, which covers S10 and S11 together and does not block S12). Closes F30 F31. Depends on S3 S10.

Scope (as landed): **Schema 7 (F30).** `E/storage.rs` (1,036 lines, was 994): `STORAGE_SCHEMA_VERSION = 7`; a new `if schema_version < 7` step creates `event_log(id INTEGER PRIMARY KEY, at, source, domain, action, target, detail)` and `event_log_at_idx` on `at DESC`; the v5 → v6 step is pinned to its own literal (`if schema_version < 6`, inserts 6, sets 6) — it was keyed to the constant, so with the constant at 7 a v5 database would have recorded 7 and never created the table, and a v6 database would have run the v6 palette seed a second time, which reads an empty palette list as unseeded and would have given an operator who deleted every palette the defaults back; `set_settings_owned_and(db, settings, |transaction| …)` lets a writer that already commits add rows to the same transaction. S3's pre-migration copy is taken as before (whenever 0 < version < the constant). **The action log (F30).** New `E/action_log.rs` (882; tests in `E/action_log/tests.rs`, 596): `ActionSource { Ui, Deck, Console, Watchdog, Launch }`, `ActionRecord`, `RecordedAction`, `insert_actions(transaction, rows)` (the insert and the prune in one transaction; the prune is by id — `DELETE … WHERE id <= MAX(id) - 5000` — because ids only ever leave from the bottom), `record_actions` / `record_actions_or_log` (a transaction of their own; a row that cannot be written is a `WARN` line, never a failed action), `list_recent_actions(db, limit)` (by id, newest first: the order the rows were written, which a clock step cannot disturb; the index on `at` is the design's and serves time-window reads), `ui_actions(method, params, result, staged)` and `deck_actions(path, action, reply)` — the two tables, pure functions — and `ui_method_class`, which puts every method of the contract in `RECORDED_UI_METHODS` (24) or `NOT_AN_ACTION_UI_METHODS` (57). **What a row is:** a discrete action that changed what a device receives — lights, groups and all lights on / off, a scene recall, a palette, identify / highlight / light solo, a fixture added, removed or repatched, lighting switched on / off, the bridge address (from the settings request or a probe that was given one), arming / holding the light outputs; every channel and output switch (mute, solo, 48 V, phase, pad, instrument, AutoSet, dim, mono, talkback), the EQ, low-cut, EQ-band and dynamics switches, the send switches, a Console mix recall, clear-all-solo, TotalMix control on / off and its address; an applied backup archive. **What is not:** a ride (a fader, a gain, an intensity, a colour temperature, the grand master, a Stream Deck dial detent), a change staged in the lighting preview (it never touched the rig — the engine has no "apply preview", a staged look reaches the rig only through a later recall, which is a row), a refused action (nothing reached a device; the last-action status and `engine.log` carry it), a selection, editor bookkeeping, planning. **Who knows the source — the entry points, not the functions they share:** `EngineApp::handle_request` (`E/app.rs` 1,752, was 1,712 — one hook after the dispatch, source `ui`; it keeps the params only for recorded methods, skips a refused reply and a recovery-mode engine, and reads the shared preview for the five preview-aware methods: only the IPC thread switches the preview, so what it reads is what the mutation saw), `handle_control_surface_http_action` (`E/control_surface.rs` 1,222, was 1,202 — source `deck`, from the route, the key and the reply, which already says `preview`; two replies grew for it: the lighting value keys' `light` carries `name`, and `talkOn` / `talkOff` carry `changed` (`E/control_surface_audio.rs` 1,331), because Companion repeats `talkOn` while the key is held), `audio::apply_console_activity` (`E/audio/console_link.rs` 491 — source `console`; `console_update_action` names the switch as the screen names it, `… at TotalMix: <name>`), the talkback watchdog (`E/audio/talkback.rs` 567 — `release_expired_hold` is what the loop calls per expired hold; source `watchdog`) and the bootstrap (source `launch`). **What a row costs:** the deck's row rides the transaction that stamps its last event and the console's rows ride the flush's own write (`persist_audio_state_with_actions`), so neither waits for the disk a second time — the flush runs on the metering thread; a screen action pays a second commit on the IPC thread; a row is on disk before the reply says ok, so nothing is lost at a hard kill beyond the moment between the action's commit and the row's. No writer thread: it would have put a process-wide thread into a 387-test binary, made the lanes wait, and traded the on-disk-before-ok property for a saving the measurement (below) does not need. **`recentEvents`.** `E/support.rs` (1,506, was 1,469): `SupportSnapshot.recent_events` — the newest fifty, newest first; empty with no query when `storage_ready` is false (`support.snapshot` is a recovery request and there is no database); a list that cannot be read is a `WARN` line and an empty list, never a Support surface that cannot list its backups. Recording raises no event. **Held light outputs (F31, D1).** New `E/lighting/output_arming.rs` (82): `app.lighting.output_armed` — `"false"` holds, anything else (above all a database that never had the key) is armed, so a default launch is what it was; `lighting_output_armed(settings)`, `set_lighting_output_armed` and its request; `LightingSnapshot.outputArmed` (ts-rs; `status` does not change — a hold is not a fault). New method `lighting.output.setArmed { armed }` through `dispatch_lighting_mutate`, so the lock and the render generation come with it; it raises `lighting.changed { reason: "output-armed-changed" }` (no new event). `E/lighting_sacn_output.rs` (852, was 556): the loop body is `SacnOutput { socket, port, active_bridge, universes, held }` with `tick(settings, log)`; the hold is applied there and not in the renderer — `read_lighting_sacn_output_state` and the DMX monitor keep answering what would be sent; held from the start of a launch, nothing is sent at all; armed → held while streaming takes the existing ineligible path (three stream-terminated packets per active universe, then silence — what a receiver expects); the destination port is a field (`SACN_PORT` for the engine) and nothing else about the packet path moved — `build_e131_data_packet`, the sequence numbers, the 800 ms keep-alive and the terminate path are byte for byte what they were. **Health.** `SubsystemState` has no "held" and gets none: the `sacn` entry stays `ok` with the detail `Light outputs held: …` — `attention` would turn the store's recovery state to `degraded` and put the Setup word and the lamps into their fault posture for something that works as asked. The registry announces changes of state only, so the output thread raises `app.changed { reason: "health" }` itself when its detail moves, and the bootstrap writes the held entry before the ready event goes out (found by the lane, below). **Safe start.** `E/bootstrap.rs` (1,641, was 1,316): `RuntimePaths.safe_start`, read with the other variables through the injected reader in `resolve_runtime_paths_from` (`SSE_SAFE_START`: empty, `0`, `false`, `off`, `no` mean no, anything else holds — an operator who wrote `yes` wanted a hold, and a stream is the one answer a safe start must not give); `hold_or_carry_light_outputs` runs right after `initialize_database`, before the bridge and the sACN thread exist, writes the flag and its rows in one transaction, and fails the start if it cannot (a safe start that could not hold must not go on to stream). The flag is persisted: the hold outlives the launch that made it, and only the switch arms. **A restore never arms a held rig.** The key sits under `app.lighting.`, which the archive restore deletes and rewrites as a whole: the export leaves it out, the delete spares it, the write skips one an archive names. A database restore replaces the whole file: `apply_pending_restore_carrying` reads the flag of the database being replaced before it moves it aside and the bootstrap writes it into the restored one (both directions — a restore never changes the switch); when the old database cannot be read (the recovery surface after a failed check) the restored database's value stands, and `SSE_SAFE_START=1` is the answer there — `docs/OPERATIONS.md` says so. The start that applied a restore leaves a `launch` row, because the restored file brought its own, older log. **Front end.** `EC/store/createShellStore.ts` (1,535) + `EC/types.ts`: `setLightingOutputArmed`; `EC/store/domainRefresh.ts` (150): `lighting.output.setArmed` → lighting + monitor + support, ahead of the `lighting.` prefix — the switch is a row in the list beside it; no other lighting request fetches support; `EC/transports/fixtureTransport.ts` (6,746, allowlisted): the double implements the method as the hardware link does — the flag, the row, `lighting.changed` — and its default lighting snapshot says `outputArmed`; `fixtures.json`: `outputArmed` beside every `reachable` (six), `recentEvents` on the three Setup fixtures (none, nine, three — all five sources across them); `check-fixtures.mjs` checks both. `FA/app/setup/components/SupportPlate.tsx` (237, was 185): **Light outputs: Armed / Held** in Workstation, a `Segmented` like Theme, with a readout that says what is sent — `the rig follows the app` / `nothing is sent to the rig` (amber) — and never what the room looks like. New `FA/app/setup/components/RecentActions.tsx` (102) + `.module.css`: a `Section` on the plate after About — the sentence, under it the local time, at the row's end who, as a `StatusBadge` (Screen / Stream Deck / Console neutral, Watchdog attention, Start-up info); the newest eight of the fifty (the plate has about 520 px between About and the red key at 2560×1440; below that the plate is in the drawer), the rest travel in the diagnostics export, which already carries the support snapshot; rows it cannot read are skipped. `FA/app/setup/SetupSupportPilot.tsx` (1,713, was 1,689): the wiring and the feedback sentence (`Light outputs held: nothing is sent to the rig until they are armed. The rig keeps its last look, or does what the bridge does without a source.`). `FA/app/shellData.ts` (765): `deriveLightingWorkspaceTone` — no bridge, then held, then unsaved — and `winsTies` on the workspace tone: the lamp took a workspace's word only when it was strictly worse than the health check's tone, so a bridge that has not been probed (attention) hid `held` (attention); held means nothing reaches the rig at all and is a state the operator has to remember to undo, so it wins the tie, and `unsaved` still does not. `FA/app/OperatorShell.tsx` uses it and hands the pilot `lightOutputsArmed`; `FA/app/tauriShellTestBridge.ts`: `setLightingOutputArmed`. Protocol: `native/protocol/v1.contract.json` (methods += `lighting.output.setArmed`; 81 methods), generated schema, `EC/generated/protocol.ts`, `LightingSnapshot.ts`; `native/protocol/v1.md`. Docs: `docs/OPERATIONS.md` (Holding the light outputs, Recent actions, Lights stop responding, the one-way upgrade under Data Safety, the held health entry), `docs/DEVELOPMENT.md` (the action log's rules, held outputs, the migration rule and the database hazard), `CHANGELOG.md`.

**Rescope:** twelve deviations, none silent, and the title stands because what landed is what it says. (1) The module is `action_log.rs`, not `events.rs`: `engine_events.rs` beside it is the event sender, and a second "events" file would be misread; the table keeps the design's `event_log` and the snapshot field its `recentEvents`. The design's `events::tests::record_list_prune` is `action_log::tests::record_list_prune`. (2) No `record_event` call threaded through the lighting and audio functions, and most of the design's `E/lighting/*` and `E/audio/*` did not change: those functions are shared by the screen and the deck, so the source is known only at the entry points — four hooks and two tables instead of an `ActionSource` parameter through some thirty signatures. (3) A fifth source, `launch` (the operator's word: Start-up). A safe start and an applied database restore change what the rig gets and nobody at a surface did them at that moment; a row with a wrong source is worse than none, and no row leaves the list with an "armed" that has no "held" before it. (4) What is recorded is wider than the design's list (above: identify and highlight, patch changes, addresses, the processing switches, an applied archive) and bounded by a rule rather than a list — rides, staged changes and refusals are out — with `every_contract_method_is_classified` as the guard that "every hardware-affecting action" stays true when a method is added. (5) `safe_start_env_holds` lives in `bootstrap::tests`, not `app::tests`: the variable is read and written there, through the injected reader; nothing calls `std::env::set_var`. (6) Held is a detail of an `ok` entry, not a registry state, and it is applied in the output loop rather than by returning `None` from the renderer — so the DMX monitor and the wire differ on purpose while held, and the snapshot, the lamp and OPERATIONS say so. (7) The restore rules (the archive leaves the key out; the database restore carries it) are not in the design; they follow from the key's prefix and from "a hold outlives the launch". (8) The v5 → v6 migration step pinned to its literal is a fix to older code the design did not list; without it this slice would have damaged every existing database's palette list on upgrade. (9) UI: the plate shows eight of the fifty, not fifty — fifty rows do not fit a surface where nothing scrolls, and the export carries them; the held state has no board of its own (81 boards, ratchets and baselines per platform unchanged in number) — it is measured by a UI-contract case that presses Held on the `setup-ready` board and runs the same census against that board's ratchet in all three themes; the lamp needed `winsTies`. (10) The engine's sentence for an audio recall is `Console mix recalled: <name>`: the source copy gate allows "snapshot" in a sentence that names the Console, but the UI contract's in-page scan allows the word inside the Console's own DOM only, and the Setup boards caught it (`copyHits: 1`); the Console's own word for what it saves is the mix (`Save current mix into …`), and `sentences_avoid_the_words_the_operator_never_reads` now forbids the word outright on the engine's side. (11) The output thread's own `app.changed`, and the bootstrap's synchronous held entry, are not in the design; the second was found by the setup-support lane (Validation). (12) Lanes beyond the design's list: `rust:clippy`, the `dev-fixtures` build, `frontend:typecheck`, the copy gate, the Storybook build, `tauri:visual:review`, the packaging lane with the production folder moved aside, `dev:check`; the safe-start scenario is two launches, the second without the variable, because the persistence is the part an operator would trip over. Decisions recorded, not deviations: a row's `at` is stamped by SQLite inside the insert; recording raises no `support.changed` (it would cost every action one more request) — the list moves when Setup is opened and with the switch, and OPERATIONS says it does not tick; `resetIntensity` / `resetCct` on the deck are rows (a press, a jump), the relative keys are not; `soloClearAll` with nothing soloed is not a row; a staged database restore is not a row (it is written into a file about to be replaced — the start that applies it writes one); at first contact the console reports every switch that differs from the saved state, and those are `console` rows (the lanes show them on the workstation, where their engines hear the real TotalMix).

Gates before: nothing recorded who did anything — the deck's last event was one overwritten key, the planning activity log is planning's; nothing could stop the light output short of disabling lighting or clearing the bridge; no test sent an sACN packet anywhere but the e131 round-trip, none ran the output loop's body at all; no test upgraded a v6 database, and `migration_snapshots_before_upgrading_v5` compared against the constant, so the step keyed to it could not be seen.

Tests added (engine 20, 367 → 387; front end 9 Vitest, 5 Playwright): `storage::tests::migrate_v6_to_v7_after_snapshot` (the v6 road with an explicitly empty palette list — versions 1…7 each with a row, the list still empty, the pre-migration copy at schema 6 without the table, a second start that changes nothing; the v5 road — 6 and 7 both recorded, seeded once; the fresh road; table and index there on all three); `action_log::tests` — `record_list_prune` (sources, newest first, the caller's limit, 5,007 rows in one transaction leave exactly the newest 5,000), `ui_requests_leave_rows_for_discrete_changes_only`, `deck_keys_leave_rows_for_discrete_changes_only`, `every_contract_method_is_classified`, `sentences_avoid_the_words_the_operator_never_reads`; `control_surface::tests::deck_all_off_records_source_deck` (the design's guard: one `deck` row beside the last-event stamp; rides and selections move the stamp and not the log; the fixture named as the screen names it; staged keys and a refused key leave nothing) and `deck_audio_keys_record_source_deck` (the mute; the press and the release of TALK, not the repeats); `app::tests` — `ui_requests_record_source_ui` (through `handle_request`, read back through `support.snapshot`; a ride, a read, a selection, a refusal and two staged requests leave nothing), `set_armed_holds_and_arms_the_light_outputs` (`outputArmed`, the generation advanced, `lighting.changed`, `status` unchanged, the DMX monitor unchanged, the renderer still answering, both rows), `recovery_mode_support_snapshot_has_no_recent_events_and_opens_no_database`; `audio::tests_console_link::console_changes_record_source_console` (two switches are rows, the gain, the fader and the volume are not; the same values again leave nothing); `audio::talkback::tests::watchdog_release_records_source_watchdog`; `lighting_sacn_output::tests` — `held_outputs_send_nothing` (a listener on a loopback port the OS chose, never 5568: fifty held ticks from the start and a marker datagram is the next thing read; arming sends sequence 0, byte for byte what the packet builder makes; a hold sends three stream-terminated packets with the last look and then nothing; a database without the key streams) and `a_hold_reaches_the_wire_on_the_next_tick` (the switch under the lock, the generation moved, the next 40 ms tick on the injected clock reads and terminates); `bootstrap::tests` — `safe_start_env_holds` (the variable's readings; a default launch writes nothing at all; a safe start holds, leaves one `launch` row and a log line; the next launch without the variable is still held and adds no row) and `database_restore_carries_the_armed_flag` (held survives a backup made while armed, armed survives one made while held, a safe start wins over both); `support::tests` — `restore_never_changes_whether_the_light_outputs_are_armed` (the export, the restore, a forged archive, both directions) and `support_snapshot_carries_the_newest_fifty_actions`; `health::tests::held_light_outputs_are_not_a_fault`. Front end: `SetupSupportPilot.test.tsx` (the pilot now fed from the store, as the shell mounts it) — `arm switch calls setArmed` (the design's; the lit key asks nothing, the readout, the feedback without "dark" or "blackout", the row with `Screen`), the newest eight with each source's word, the empty state with unreadable rows skipped; new `shellData.test.ts` (five: only an explicit `false` is a hold; the order; held over a healthy bridge; held wins the tie an unsaved scene loses; an error stays the health check's); `setupState.test.ts` (held leaves a green runtime READY); `domainRefresh.test.ts` and `createShellStore.test.ts` (the switch's mapping and its three snapshot requests — the fan-out guard, changed on purpose). Playwright: `setup.spec.ts` — the switch in the built app (the readout, the lamp's `held` on Setup and on Lighting, the row, no promise of darkness, arming again) and the list's fit (eight of nine, the plate does not scroll, the list ends above the red key); `ui-contract.spec.ts` — `setup-ready @ <theme> holds its ratchet with the light outputs held` × 3. Lanes: setup-support step 8 `safe-start` (`safe-start-holds-the-light-outputs-until-armed`), workspaces `ui-actions-appear-in-recent-actions`. **Each guard proved against the code without its fix** — thirteen neutralisations from a script that keeps a copy, runs the one test, restores and checks the line is back: the migration step keyed to the constant again (no table on the v6 road), the deck hook, the screen hook, the console flush, the watchdog's source, the loop ignoring the hold (both sACN tests; the first reads a data packet where the marker was due), the bootstrap not holding, the restore not carrying, the archive delete, the archive write, the archive export, the recovery-mode query — every one fails. Tests changed: `SetupSupportPilot.test.tsx`'s two S7 cases render through the new store-fed harness and pass `lightOutputsArmed` (same assertions); `bootstrap::tests::runtime_paths_for` gains `safe_start: false`; `apply_pending_restore` is a `cfg(test)` wrapper over the carrying form (same return, same four call sites).

Lanes: `npm run protocol:generate && npm run protocol:check`, `npm run rust:clippy`, `npm run native:test`, `cargo clippy` and `cargo test -p studio-control-engine --features dev-fixtures`, `npm run frontend:typecheck`, `npm run frontend:test`, `node scripts/check-operator-copy.mjs`, `npm run frontend:playwright:test` (it builds `dist` and `storybook-static` itself), `npm run tauri:setup-support:qualify`, `npm run tauri:workspaces:qualify`, `npm run tauri:visual:review`, `npm run native:engine:build && npm run tauri:build`, then `npm run native:package:win:local && npm run native:bridge:win:verify` with the production folder moved aside, `npm run dev:check`. Done when every hardware-affecting action has an event with a source (the rule above, held by the classification test), `SSE_SAFE_START=1` → zero sACN packets until armed (`held_outputs_send_nothing`; the lane's scenario), and the default launch is unchanged (no key written, no row, `outputArmed: true`, the same packets) — all three met.

Validation (workstation, 2026-09-17): baseline before editing — `rust:clippy` clean, `native:test` 396 passed / 1 ignored (tauri-shell 20, engine 367 + 1, `contract.rs` 4, `end_to_end.rs` 5; the engine suite 7.05 s), `frontend:test` 316 (app 81, design-system 175, engine-client 45, tokens 15); 38201, 4173 and 6007 free, no shell and no engine process — the production app closed since the S9 session. **What a row costs, measured** through the real entry points (a temporary ignored test in `app::tests`, twenty rounds each, removed again by the script that added it — the file compared byte for byte; before = `04b68d7`, after = three runs on this tree): a screen `lighting.power.all` 27.7 ms → 29.6 / 30.0 / 30.1 ms and a screen mute 8.0 → 10.2 ms (the second commit: about 2.3 ms here, far under the ~35 ms a cold commit costs — the file is hot and the row is small); a screen intensity ride 30.7 → 29.8–30.2 ms (no row); the deck's all-on / all-off 51.7 → 50.2–50.6 ms (the row rides the stamp) and a dial detent 76.2 → 74.5–75.3 ms (no row); `support.snapshot` 0.1 → 1.3 ms (the fifty rows). After: `cargo fmt` applied; `rust:clippy` clean under `-D warnings`; `native:test` 416 passed / 1 ignored (tauri-shell 20, engine 387 + 1, `contract.rs` 4 with the new arm, `end_to_end.rs` 5), the engine suite 7.6–8.0 s; the `dev-fixtures` build: clippy clean, 389 + 1 / 4 / 5, and `native:engine:build` again afterwards (the plain engine, 2026-09-17 20:38:22, no bundled fixture payloads); `protocol:generate` + `protocol:check` green (81 methods, no new event); `frontend:typecheck` green; `frontend:test` 325 (app 90, design-system 175, engine-client 45, tokens 15); the copy gate at 0; `file:health` green (`app.rs` 1,752, `SetupSupportPilot.tsx` 1,713, nothing new over 2,000). `frontend:playwright:test`: first 414 passed / 8 failed of 422 (417 + 5 new cases, all passing) — the eight are the Setup surfaces the plate explains (`visual-review.spec.ts` × 5: `setup-ready` at 2560, its studio preview, graphite, bone, and `setup-degraded` at 2560; `storybook.spec.ts` × 3: Setup Required / Ready / Degraded); after the win32 refresh of exactly those eight, **422 passed** (2.8 min). On the way the UI-contract boards caught two things: `copyHits: 1` on six Setup boards for `Console snapshot recalled` (Rescope 10), and — in the new held-state case only — `gradientsOff: 1`, which is not this slice's: the pilot's feedback band (`SetupSupportPilot.module.css .feedbackBanner`, shared with `.utilityRow`), shown after every plate action since before S7 and on no board, still carries a pre-A gradient the light census counts as off policy; the case allows it by the band's test id, exactly once, and enforces every other measure, and the restyle is noted for S14 below. No ratchet moved: the nine Setup boards hold `sizeCount` 7 / 7 / 6, 0 contrast fails, 0 small targets, no scroll, with the switch and the list on them. `tauri:visual:review` 54 passed. `tauri:setup-support:qualify` passed in 60.3 s with seventeen checks over eight steps (the figures are the last run's; see below): `shell-stays-responsive-during-stalled-request` `probeMs` 1911 / `heartbeats` 9 / `maxGapMs` 261, `database-backup-verifies-and-restores-after-restart` at schema 7 with its `replaced` and `pre-restore` files, `corrupt-db-restores-from-database-backup` at generation 2, `engine-crash-reaches-recovery-and-restarts` `detectMs` 211 / `restartMs` 1231, `second-instance-is-refused` in 100 ms, `bridge-port-taken-reads-as-health-attention` on port 52221, and the new `safe-start-holds-the-light-outputs-until-armed` — held with the `sacn` entry `ok` and saying so, the `launch` row, still held in a second launch without the variable and no second row, armed by the switch with the `ui` row and the entry back to `socket ready`. **The lane found a fault the unit tests could not:** on its first run the second launch had `outputArmed: false` and a `sacn` entry that read `socket ready` — the shell's first `health.snapshot` follows the ready event at once, the output thread that owns the entry starts after it, and the thread's own announcement arrived while the shell was still starting; the bootstrap now writes the held entry before the ready event and the thread starts from the saved flag. The lane ran three times: that first run, a second, green, and — after an ESLint finding in the lane script itself (`let safeEvidence = null`, `no-useless-assignment`, caught by `dev:check`) — a third on the final tree. `tauri:workspaces:qualify` passed in 17.9 s with six checks (audio receive port 54816, a throwaway; `overrideProbes: false`), the new one reading `ui: Scene recalled: Prep` out of eight rows, the fixture's intensity ride among none of them. Both lanes run the development store. Hardware: the lanes commission their rig at loopback and the stalled probe goes to `203.0.113.113`, so no sACN frame left for the studio bridge; the new sACN tests send to `127.0.0.1` on ports the OS handed their own listeners, never to 5568; nothing wrote to 7001–7010 beyond the lane engines' once-a-second pulls with their default settings, as in S6–S10 — which is also why the lane runtimes' logs show `console` rows on this workstation (Rescope, decisions); TotalMix FX and Companion untouched. **The operator's database:** no engine from this tree was started against the default app-data directory — the unit lanes, both qualification lanes and the bridge lane use temporary directories, and the Browser-pane preview runs the fixture double with no engine. **Packaging lane (the session prompt's procedure, from a script that refuses to start if `windows.production-keep` exists and restores on any exit):** `native:engine:build` (debug engine 2026-09-17 20:38:22, 9,340,928 bytes) and `tauri:build` (release shell 2026-09-17 20:46:45, 10,360,320 bytes) first; app closed, ports free; `release\native\windows` renamed to `windows.production-keep`; `native:package:win:local` exit 0 (the new folder carried those two exes); `native:bridge:win:verify` exit 0 — the packaged engine on the reserved port 61539 with a temporary app-data root, its four steps passed with the deck's hook in place; the new folder deleted and the kept folder renamed back. Production is what it was, to the byte and the second: shell 9,823,232 bytes 2026-09-04 16:44:46, engine 8,801,792 bytes 2026-09-10 16:08:06; no `windows.production-keep` left; 38201 free and no engine process afterwards. `npm run dev:check` green before the commit (its first run failed at `lint` on the line above; fixed, the lane re-run, `dev:check` run again).

Baselines: eight win32 files, refreshed after the lanes and committed on their own as `Readiness S11 (baselines): Support plate with the Light outputs switch and Recent actions` — `visual-review.spec.ts` `setup-ready-2560x1440`, `setup-ready-studio-preview-1512x982`, `setup-ready-graphite-2560x1440`, `setup-ready-bone-2560x1440`, `setup-degraded-2560x1440`; `storybook.spec.ts` `shell-operatorshell--setup-required` / `-ready` / `-degraded`. Every one inspected before staging (the plate in Studio, Graphite and Bone, the three-row degraded plate with the Start-up badge, the empty state on Setup Required, the scaled studio preview); the diffs were copied out of `test-results` first. No new snapshot title, no new board, no ratchet re-seed. linux: the same titles were already stale there (S7 moved this plate); they stay in Appendix B item 6's refresh. darwin pending, as before.

Operator hands: Appendix B item 5, walked for S10 and S11 together — start with `SSE_SAFE_START=1` (`docs/OPERATIONS.md`, "Holding the light outputs", says how for the installed app): the header's Lighting lamp must read `held`, the rig must not move whatever is recalled, the DMX monitor must show what would be sent; arm from Setup / Support › Workstation and the rig follows within a tick; hold again while lit — the rig keeps its last look (the bridge's behaviour without a source; note what it actually does); then S10's Preview steps; then press a deck lighting key, a deck mute, throw a mute at TotalMix, hold TALK until the watchdog releases it, and read Recent actions: Stream Deck, Stream Deck, Console, Watchdog, and Screen for what was done on screen. **Promoting a build from this branch — the operator's, and with a consequence that is new with this slice:** the packaged app under `release\native\windows` is still the S2-era build. The first start of a build from this branch against the real app-data directory upgrades the operator's database to schema 7 (S3's `db-*-pre-migration.sqlite3` copy is written first). The S2-era build refuses a schema-7 database ("newer than this engine binary") and has no way to restore a database backup, so from that moment the way back is by hand — the older build, plus that pre-migration copy put in place of `studio-control.sqlite3` with the app closed (`-wal` and `-shm` beside it deleted) — and it loses everything recorded after the promotion. Promote when ready to stay; until then no engine from this branch may be started against the default app-data directory (`npm run tauri:dev`, a release exe or `studio-control-engine.exe` by hand need `SSE_APP_DATA_DIR` pointing at a scratch directory — `docs/DEVELOPMENT.md` says so). Notes for later slices: S12 — nothing from this slice. S13 — `held_outputs_send_nothing` asserts silence with a marker datagram rather than a timeout; if a runner ever reorders loopback datagrams from two sockets it shows there; the unit suite grew by about a second of disk commits. S14 — the pilot's feedback band needs the A restyle (the design system's `StatusBand`, as `BackgroundFailureBand.tsx` uses it), after which the allowance in `ui-contract.spec.ts`'s held-state case goes (`.utilityRow` shares the rule: check whether a board shows it before changing it); `SetupSupportPilot.tsx` is 1,713 lines and its split should take `RecentActions` and the switch's wiring into the Workstation section; the fixture double records a row for `lighting.output.setArmed` only — the other mutations it implements leave none, which the split is the moment to decide. S15 — OPERATIONS now documents safe start, Recent actions and the one-way upgrade; `README.md` / `SECURITY.md` need nothing from this slice.

CI (result, 2026-09-17): run 35261830154 (https://github.com/Fikarn/sse-exed-studio-control/actions/runs/35261830154), event `push`, sha `4b89191` — the baselines commit, pushed together with the slice commit `9c45d36`, so one run covers both — created within seconds of the push; 14.1 minutes end to end. Green on the first attempt: `lint` (0.4 min), `frontend-test` (0.4 — the 325 Vitest cases: app 90, design-system 175, engine-client 45, tokens 15), `frontend-typecheck` (0.5), `format-protocol` (1.4 — `protocol:check` with the new method, `file:health` with `app.rs` at 1,752 lines, `SetupSupportPilot.tsx` at 1,713 and nothing new over 2,000), `rust` (2.2, inside its usual 2.0–2.8 — rustfmt, clippy, check, the 416 tests on Linux (tauri-shell 20, engine 387 + 1 ignored, `contract.rs` 4, `end_to_end.rs` 5) with all twenty new cases among them, plus `native:acceptance`; nothing hung, and the two sACN cases that listen on a loopback port pass there, marker datagram included; the engine's unit suite took 17.4 s), `tauri-foundation` (2.9) and `qualification` (11.1, was 9.3 — both lanes under xvfb: the setup-support lane's eight steps in 381.2 s with all seventeen checks — `shell-stays-responsive-during-stalled-request` `probeMs` 2547 / `heartbeats` 11 / `maxGapMs` 321, `database-backup-verifies-and-restores-after-restart` at `schemaVersion` 7, `corrupt-db-restores-from-database-backup` at generation 2, `engine-crash-reaches-recovery-and-restarts` `detectMs` 430 / `restartMs` 1567, `second-instance-is-refused` by the plugin (25.2 s, the Linux shell's start), `bridge-port-taken-reads-as-health-attention` on port 45815, and the new `safe-start-holds-the-light-outputs-until-armed`: held with the `sacn` entry `ok` and saying so, the `launch` row, still held in the second launch without the variable, armed by the switch with its `ui` row — the scenario's two launches are what the job's ninety more seconds are; the workspace lane in 93.1 s with its six checks, `ui-actions-appear-in-recent-actions` reading `ui: Scene recalled: Prep` out of three rows, the audio probe overridden as on every CI run — `overrideProbes: true`, `audio-live-mutations-skipped`). Red, as expected and not chased: `frontend-e2e` (10.1 min) — 93 failed / 329 passed of 422. The total moved from 417 by this slice's five new cases, and all five pass on Linux (324 → 329). The failed titles were not counted but diffed against the S10 run's list (35230069147): the two sets of 93 are identical, nothing only in one of them — the 91 linux snapshot cases (52 `visual-review.spec.ts` + 39 `storybook.spec.ts`; the eight Setup titles this slice refreshed on win32 were already among them, stale since S7 moved the same plate, so the slice added no failing title), the constant `audio-arm-countdown.spec.ts:45` dwell case, and one runner-timing case from the S13 list, `audio.spec.ts:1054`. Because this slice touches the store and the Setup surface it was not accepted by title: its error block was extracted from both logs and compared — the same locator (`audio-inspector-hardware-mini` › the 48 V button), the same line 1093, `data-armed` expected `true` and received `false` after the same five-second wait — identical. No new title. Nothing is pending on S11 but Appendix B item 5.

## Slice 12 — Supply chain + release CI

Status: landed 2026-09-18 (commit `Readiness S12: supply-chain job on every push, release evidence on a tag, SBOMs, dependency fixes`, pushed; every workstation lane and `npm run dev:check` green; the slice's last lane — the `v0.0.0-evidence` tag run of `release-evidence.yml` — and the CI run on the branch can only happen after the push, and are recorded under "Release evidence (result)" and "CI (result)" below by the follow-up commit `Readiness S12 (ledger): CI run on the branch recorded`, which moves the slice to `verified`; no hardware exposure, and no operator item blocks it). Closes F17, F03 (release). Depends on S0.

Scope (as landed): **The npm gate (F17).** New `scripts/check-npm-audit.mjs` (295 lines) + `scripts/npm-audit-allowlist.json` + the shared date rule `scripts/supply-chain-expiry.mjs` (54): `npm audit --json` for every lockfile in the repository, each scope with its own bar — `prod` (`--omit=dev`, what ships inside the app) fails from `moderate`, `all` (what builds, lints and tests it) from `high`, `tools` (the SBOM generator's own tree, `tools/sbom/`) from `high`. An advisory at or above its bar fails unless the allowlist names it — GitHub advisory id **and** package — with a reason of at least twenty characters and an `expires` day that is a real calendar day, has not passed, and lies at most 90 days out (`MAX_EXCEPTION_DAYS`); an entry with a problem fails by itself and covers nothing; a severity word the script does not know ranks above `critical`; a report that is not an `auditReportVersion: 2` report (offline, registry error) is exit 2, "could not run", never a pass; `--report=FILE --scope=…` reads a recorded report, which is how the tests stay off the network. **The allowlist starts empty, because the findings were fixed and not listed:** `npm update brace-expansion browserslist` moved seven packages, all of them only under `eslint` → `minimatch` and `eslint-plugin-react-hooks` → `@babel/core` (`npm ls`): `brace-expansion` 5.0.7 → 5.0.12, `browserslist` 4.28.2 → 4.29.0 and its data packages (`baseline-browser-mapping`, `caniuse-lite`, `electron-to-chromium`, `node-releases`, `update-browserslist-db`) — four high advisories and one moderate gone; what is left in the full tree is below the bar (4 moderate: `vitest` and `@vitest/mocker` GHSA-82fw-gwwq-j7x9, `qs` × 2; 2 low: `@babel/core`, `esbuild`) and the shipped tree is clean (0 of 29). **The Rust gate (F17).** New `native/deny.toml` (75): `[graph]` limited to the three triples that are built anywhere (`x86_64-pc-windows-msvc`, `aarch64-apple-darwin`, `x86_64-unknown-linux-gnu`) with `all-features`; advisories — a vulnerability or an `unsound` crate anywhere fails, `yanked` fails, an `unmaintained` notice fails for a direct dependency only (`unmaintained = "workspace"`: the six today are `proc-macro-error` under GTK and five `unic-*` crates under Tauri's `urlpattern`, none with an upgrade); licences — `MIT`, `Apache-2.0` and five more, each with the crates that need it named in the file (`Apache-2.0 WITH LLVM-exception`: target-lexicon; `BSD-3-Clause`: brotli, alloc-no-stdlib, alloc-stdlib; `MPL-2.0`: cssparser, cssparser-macros, selectors, dtoa-short, option-ext; `Unicode-3.0`: the eighteen ICU4X crates and unicode-ident; `Zlib`: foldhash) — written from `cargo deny list` (twelve identifiers) and cut to what the check needs, so the LGPL arm of `r-efi` and the 0BSD / CC0 / MIT-0 / Unlicense arms of dual-licensed crates are deliberately absent, and cargo-deny reports no unused allowance; bans — `wildcards = "deny"`, duplicates reported (Tauri's tree carries 21); sources — crates.io only, no git dependency. One ignored advisory: RUSTSEC-2024-0429 (glib 0.18 `VariantStrIter`, unsound; the fix is glib 0.20, which Tauri 2's GTK3 stack cannot take; Linux runners only, never in a bundle), written with `review by 2026-12-15`. New `scripts/check-deny-ignores.mjs` (122): cargo-deny keeps no time, so every `[advisories] ignore` entry must be one line `{ id = "…", reason = "… review by YYYY-MM-DD" }` under the same date rule; a line of the list it cannot read is a failure, never a skipped entry. **Fixes on the Rust side:** `cargo update -p plist` (plist 1.9.0 → 1.10.1, `quick-xml` 0.39.2 → 0.42.0 — RUSTSEC-2026-0194 and -0195 — and `base64` 0.23.1 added; `native/Cargo.lock` now has 500 packages) and `cargo update -p anyhow` (1.0.102 → 1.0.104, RUSTSEC-2026-0190, unsound). `native/Cargo.toml` `[workspace.package]` gains `license = "MIT"` (the repository's `LICENSE`) and `publish = false`, inherited by the three crates: without them the licence check reports all three as unlicensed and the wildcard ban refuses the version-less path dependency on `studio-control-protocol`. **CI.** `.github/workflows/dev-checks.yml` (298): ninth job `supply-chain` — `npm run supply-chain:npm`, `node scripts/check-deny-ignores.mjs`, then `cargo deny check` through `EmbarkStudios/cargo-deny-action` pinned to the commit behind v2.1.1 (`3c6349835b2b7b196a839186cb8b78e02f7b5f25`; Dependabot's github-actions updates move the pin) with `rust-version: 1.95.0`; each step reports even when another failed; nothing is installed or built, the three read lockfiles. **Release evidence (F03 release, F17, D2).** New `.github/workflows/release-evidence.yml` (206): `push.tags: ["v*"]` + `workflow_dispatch`, `permissions: contents: read`, matrix `windows-latest` / `macos-14`, no build cache and no third-party action (the toolchain comes from `native/rust-toolchain.toml` through rustup): `npm ci`, `npm run release:check` for a real tag only, `native:engine:build`, `tauri:build`, `native:package:<win|mac>:local`, the signing posture and the signing step, `native:checksums:<win|mac>:staged-write`, the manifest checked against the archive (`sha256sum -c` / `shasum -a 256 -c`), the SBOMs, a run summary (commit, toolchain, digest, signed or not), one artifact per runner — the bundle archive, `release/checksums/<target>/`, `release/sbom/<target>/` — kept 30 days. It creates no GitHub Release and uploads nowhere else. **SBOMs.** New `scripts/write-release-sboms.mjs` (200): three CycloneDX 1.5 files under `release/sbom/<target>/` — `…-frontend.cdx.json` (`cyclonedx-npm --omit dev`: 22 components), `…-engine.cdx.json` (38) and `…-shell.cdx.json` (239; `cargo cyclonedx --describe binaries --target <triple>`, so no GTK in a Windows SBOM); cargo-cyclonedx writes beside each crate's manifest and has no output directory, so its files are moved and never left in the tree (`.gitignore` names them as well); each file is read back and refused unless it is CycloneDX 1.5, describes the right component at `package.json`'s version and lists the one dependency that component cannot exist without (`react`, `rusqlite`, `tauri`). New `tools/sbom/` (`package.json` + its own `package-lock.json`, 176 packages, `@cyclonedx/cyclonedx-npm` 6.0.1 exact): installed with `npm ci --prefix tools/sbom --ignore-scripts` by the release workflow only. **Signing, wired and dormant (D2).** `scripts/native-sign-windows.mjs` (204) and `scripts/native-sign-macos.mjs` (145) gain `--bundle-only` — the shell, the engine and the bundle archive, and no installer; without the flag nothing changes; the Windows skip line now names its two variables. The workflow's signing step runs on every run with the secrets in that one step's environment: with none set the script prints why it skipped and exits 0, and the posture step writes `Signing: SKIPPED — … this bundle is UNSIGNED` into the log and the summary; with them set it signs, after a keychain import on macOS (`SSE_MACOS_SIGN_CERT_BASE64` / `_PASSWORD` — a runner has no keychain to find `SSE_MACOS_CODESIGN_IDENTITY` in) and a `signtool` lookup on Windows. `package.json`: `supply-chain:npm`, `supply-chain:cargo`, `supply-chain:check`, `native:sign:<mac|win>:bundle`, `native:sbom:tools:install`, `native:sbom:<mac|win>:write`. `.github/dependabot.yml`: an npm entry for `/tools/sbom`. Docs: `docs/RELEASE.md` (Release Evidence (CI), Supply Chain Checks, the runner's secrets under Optional Signing), `docs/DEVELOPMENT.md` (§4 Supply chain: what the gates are, take the fix first, never `npm audit fix`; the nine-job paragraph), `AGENTS.md` (command map, CI table), `docs/HANDOFF.md` (nine jobs), `scripts/README.md`, `CHANGELOG.md`.

**Rescope:** twelve deviations, none silent, and the title stands because what landed is what it says. (1) The npm gate reads every lockfile with its own bar instead of `npm audit --omit=dev` failing on high / critical alone. On inspection the shipped tree is clean and **all six of GitHub's open Dependabot alerts are development-scope** (`gh api …/dependabot/alerts`): the designed gate would have said "0" beside a high alert; the build tools run on the studio workstation, which is the production machine, and on the runner that builds the bundles; and with the full tree gated at high, a second look at the shipped tree is only worth having at a stricter bar, hence `moderate` there. (2) The allowlist starts empty, where the session prompt expected it non-empty: both highs had an upgrade inside their ranges, and an upgrade beats a listed exception. `npm audit fix` was **not** used: its dry run proposed 25 changes, among them `esbuild` 0.27.7 → 0.27.2 — a downgrade of the bundler that produces the shipped `dist`, to step out of an advisory range, against the `allowScripts` pin. The targeted update is proved harmless rather than argued: `frontend/app/dist` rebuilt after it is identical to the build from before it, 22 files by name and SHA-256. (3) The 90-day cap, the minimum reason and the id-and-package match are not in the design, which says only that entries carry an expiry date: without a cap, `2099-01-01` is a permanent mute; without the package, an entry for one package would cover the same advisory on another. (4) `@cyclonedx/cyclonedx-npm` is a locked dependency of `tools/sbom/`, not a root devDependency: as one it added 163 packages — one with a native install script, `libxmljs2` — to every `npm ci` on every job and on the workstation, to serve one step of a workflow that runs on tags. It keeps an integrity-pinned lockfile (an `npx` fetch would not), is audited and has its Dependabot entry. (5) `cargo deny check` runs all four checks (the design names three; `sources` is the one that refuses a git dependency), on three named triples, with `unmaintained` scoped to direct dependencies and `unsound` widened to the whole tree. (6) `scripts/check-deny-ignores.mjs` and the `review by` convention are not in the design; they give the Rust side's exceptions the date the design gives the npm side's. (7) `native/Cargo.lock` and the three crates' manifests are not in the design's file list: the two advisory fixes and the licence / publish metadata the checks need. (8) The release workflow restores no cache and uses no third-party action — a from-scratch build is the cleaner evidence and tags are rare — and it runs `release:check` for a real tag only: `resolveReleaseTag` reads `GITHUB_REF_NAME`, and `v0.0.0-evidence` is a valid tag by its pattern, so the rehearsal would have failed against `package.json` 2.2.1; a tag ending in `-evidence` is a rehearsal. The manifest self-check and the run summary are additions. (9) `--bundle-only`, the keychain import with its two further secrets, and the `signtool` lookup are not in the design. They exist because `native:sign:*:release` as written rebuilds and signs the QtIFW installer, which a runner cannot have: wired as designed, the step would have failed on the day the secrets arrived. **The signing path has never run — no certificate exists**; what is verified is the dormant path, on the workstation and on both runners, and that half a configuration fails instead of skipping. (10) `workflow_dispatch` is in the workflow, but GitHub starts a manual run only from a workflow file on the default branch, so until this branch is merged the tag is the only trigger; the session prompt asked for the dispatch so the evidence run would not depend on the tag, and on this branch it still does. (11) The SBOM script validates what it wrote and refuses an empty or foreign SBOM; three SBOMs, one per thing inside the bundle; CycloneDX 1.5 on both sides because cargo-cyclonedx 0.5.9 writes nothing newer. (12) Lanes beyond the design's list: `tauri:foundation`, the packaging lane with the production folder moved aside (package, dormant signing, checksums, manifest check, SBOMs, bridge), both qualification lanes (the shell's dependency tree moved), the `dist` hash comparison, fifteen neutralisations and the policy run against an export of HEAD, `dev:check`. Decisions recorded, not deviations: the supply-chain lanes are **not** part of `dev:check` — they need the network and they judge today's advisory databases, so they would change `dev:check`'s answer overnight; for the same reason no test judges an exception's date against today's, so a date can turn the `supply-chain` job red and never `scripts:test`; the below-the-bar findings were left alone (their fixes were part of the refused `npm audit fix`); `bans` stays light — no deny-list was invented; QtIFW installers stay workstation-built, as the design says.

Gates before: nothing looked at a dependency — no `npm audit`, no advisory, licence or source check for the 499 packages of `native/Cargo.lock`, Dependabot's alerts visible on GitHub only; a `v*` tag ran no workflow at all (`dev-checks.yml` triggers on branches); the two signing scripts had no test (`scripts/README.md`: accepted gap); nothing described what is inside a bundle.

Tests added (`scripts:test` 85 → 114; 110 pass, the 4 skipped are the ones skipped before): `scripts/check-npm-audit.test.mjs` (13; on the two `npm audit --json` reports recorded on the workstation on 2026-09-17 before any fix, `scripts/fixtures/npm-audit/recorded-2026-09-17-{all,prod}.json` — it never calls the registry): the eleven distinct advisories of the nine vulnerable packages, worst first; an unlisted high fails and the same report passes with all four listed, one left out is named; the two bars; an expired entry fails by itself, covers nothing, and fails even with nothing left to cover, the expiry day itself still counts; the date rule (day 90 yes, day 91 no, 2099 no, `2026-02-31` and six other non-days); what an entry needs; an entry covers its own package only; a report that is not one is `AuditReportError`; an unknown severity fails at every bar; the command line on recorded reports — exit 0, exit 1 naming the advisories, exit 1 on a 2020 expiry, exit 2 for a non-report, a missing allowlist and a bad flag; the committed allowlist's shape. `scripts/check-deny-ignores.test.mjs` (7): entries read with reason and date, both spellings of empty, a passed date and one too far out, an unreadable line is a failure (four forms), only `[advisories]` is read, the committed `deny.toml`, the command's exit codes. `scripts/write-release-sboms.test.mjs` (3): the plan (names, folder, the dependency each must list), the refusal rule (empty, nested, wrong version, wrong component, wrong spec, not CycloneDX), unknown targets. `scripts/native-sign.test.mjs` (6): both scripts, with and without `--bundle-only`, skip with exit 0 and name their variable; a blank value is no configuration; half a configuration fails and never prints "Skipping". **Each guard proved against the code without it** — twelve neutralisations from a script that keeps a copy, replaces exactly one needle, runs the test files, restores and asserts the needle is back (an expired date accepted; the cap gone; a non-report read as no advisories; an unknown severity below the bar; an entry covering any package; a broken entry still covering; an unreadable ignore line skipped; an ignore without a date; an SBOM without its must-list component; an SBOM of another component; Windows half-configuration skipping; the dormant macOS path exiting 1) — every one fails, and the restored tree passes. The policy itself the same way, through `cargo deny --config` on neutralised copies: without the glib ignore `advisories FAILED` (exit 1), without `MPL-2.0` `licenses FAILED` (5 rejected, exit 4), without the registry `sources FAILED` (380 crates, exit 8); and the finished policy against the tree as it was (`git archive HEAD native` into a scratch directory, the new `deny.toml` beside it): exit 7, `advisories FAILED, bans FAILED, licenses FAILED, sources ok` — the two `quick-xml` vulnerabilities, `anyhow`, the two wildcard bans, the three unlicensed crates. The npm gate against the tree as it was: exit 1, four `FAIL high` lines. Tests changed: none.

Lanes: `npm run supply-chain:check` (`cargo install cargo-deny --locked` once: 0.20.2, 78 s), `npm audit --omit=dev`, `npm run scripts:test`, `npm run lint`, `npm run rust:clippy`, `npm run native:test`, `npm run frontend:test`, `npm run tauri:foundation`, the packaging lane with the production folder moved aside, `npm run tauri:setup-support:qualify`, `npm run tauri:workspaces:qualify`, `npm run dev:check`; then the push, tag `v0.0.0-evidence` on the branch head, both runs watched, the tag deleted locally and on origin. Done when both jobs are green — the `supply-chain` job and the two `release-evidence` jobs — and the tag produced bundles + SHA256 manifests + SBOMs as artifacts.

Validation (workstation; the session ran across midnight, so each lane carries its day): **2026-09-17, 23:37–23:59** — baseline before editing: `rust:clippy` clean, `native:test` 416 passed / 1 ignored (tauri-shell 20, engine 387 + 1, `contract.rs` 4, `end_to_end.rs` 5; the engine suite 7.98 s), `frontend:test` 325 (app 90, design-system 175, engine-client 45, tokens 15), `scripts:test` 85; 38201, 4173 and 6007 free, no shell and no engine process, the production app closed as the S11 session left it, TotalMix FX and Companion running and untouched. Network up. `npm audit --omit=dev`: 0 of 29; `npm audit`: 9 packages (2 high, 5 moderate, 2 low). `cargo deny` with its defaults: 2 vulnerabilities and 6 unmaintained notices, bans and sources ok; with the finished policy on an export of HEAD's tree: exit 7, as listed under Tests (run 2026-09-18 00:16). After the two `cargo update`s and the manifest metadata: `cargo deny check` exit 0 — `advisories ok, bans ok, licenses ok, sources ok`, 21 duplicate warnings, no unused licence allowance; `native:test` on the new lockfile 416 / 1 (`plist` 1.10.1, `quick-xml` 0.42.0, `anyhow` 1.0.104, `base64` 0.23.1 compiled). The npm gate before the fix: exit 1, four `FAIL high`; after `npm update brace-expansion browserslist`: exit 0 (`package-lock.json` +31 −28, seven packages, none added), `npm run lint` clean. `@cyclonedx/cyclonedx-npm` installed at the root (+163 packages), measured, and uninstalled again — the root lockfile's diff against HEAD is the seven packages and nothing else — then locked under `tools/sbom/` (176 packages, `found 0 vulnerabilities`); `cargo install cargo-cyclonedx --locked`: 0.5.9. `node scripts/write-release-sboms.mjs --target=windows`: 22 / 38 / 239 components; the shell's SBOM carries `quick-xml` 0.42.0 and no GTK; our three crates read `MIT`; nothing left beside the manifests. **2026-09-18, 00:05–00:12** — the fifteen neutralisations; `cargo deny check` on the real file again exit 0. `frontend/app/dist` hashed (the S11 session's build, same front-end source — `git diff HEAD -- frontend/` is empty), `npm run tauri:foundation` exit 0 (`protocol:generate` left no diff, debug engine 9,340,928 bytes — the size S11 built, its tree did not move — release shell 10,356,736 bytes, was 10,360,320; `tauri:smoke` passed against a temporary app-data directory), `dist` hashed again: **identical, 22 files by name and SHA-256**, so neither Playwright nor the shell can render anything the lockfile update changed, and the Playwright lane was not run (no front-end source changed; CI runs it). **Packaging lane (the session prompt's procedure, from a script that refuses to start if `windows.production-keep` exists, if a port or a process is in the way, or if production is not the expected build, and restores on any exit):** `release\native\windows` renamed to `windows.production-keep`; `native:package:win:local` exit 0 (the new folder carried the two exes above; the archive 6,579,401 bytes); `native:sign:win:bundle` exit 0 with `Skipping native Windows signing because no signing certificate is configured (SSE_WINDOWS_SIGN_CERT_PATH or SSE_WINDOWS_SIGN_CERT_BASE64).`; `native:checksums:win:staged-write` exit 0 and `sha256sum -c` on its manifest `OK`; `native:sbom:win:write` exit 0 (22 / 38 / 239); `native:bridge:win:verify` exit 0 — the packaged engine on the reserved port 53247 with a temporary app-data root, its four steps passed; the new folder deleted, the kept one renamed back, `release/checksums` and `release/sbom` removed again so `release/` is as it was found. Production is what it was, to the byte and the second: shell 9,823,232 bytes 2026-09-04 16:44:46, engine 8,801,792 bytes 2026-09-10 16:08:06; no `windows.production-keep` left; ports free and no engine process afterwards. `tauri:setup-support:qualify` passed in 71.0 s with its seventeen checks and `tauri:workspaces:qualify` in 17.9 s with its six, both on the shell built from the new lockfile (pre-built with `--features test-bridge`). `scripts:test` 114 (110 pass, 4 skipped, 0 fail). Hardware: nothing in this slice opens a socket toward a device; the two qualification lanes behaved as in S6–S11 (their rig at loopback, the stalled probe to `203.0.113.113`, the lane engines' once-a-second pulls toward the local TotalMix with default settings); nothing wrote to 7001–7010 beyond that and no sACN frame left for the studio bridge. **The operator's database:** no engine from this tree was started against the default app-data directory — every lane that starts one uses a temporary directory. `npm run dev:check` green before the commit.

Baselines: none — no front-end source changed and `dist` is byte-identical; no snapshot title can move.

Operator hands: Appendix B item 7 (certificates, D2) — it blocks nothing. When certificates exist, add as repository secrets: Windows — `SSE_WINDOWS_SIGN_CERT_BASE64` (the `.pfx`, base64), `SSE_WINDOWS_SIGN_CERT_PASSWORD`, optionally `SSE_WINDOWS_SIGN_TIMESTAMP_URL`; macOS — `SSE_MACOS_CODESIGN_IDENTITY`, `SSE_MACOS_SIGN_CERT_BASE64` (the Developer ID `.p12`, base64), `SSE_MACOS_SIGN_CERT_PASSWORD`, optionally `SSE_MACOS_NOTARY_APPLE_ID` / `_PASSWORD` / `_TEAM_ID`. Then push a rehearsal tag (`v0.0.0-evidence`) before any real one: that run is the signing path's first test, on both runners, and should be expected to need an iteration. Also the operator's: merging, after which `gh workflow run release-evidence.yml` works without a tag and GitHub closes the Dependabot alerts the lockfile updates answer (the `browserslist` high among them). Notes for later slices: S13 — `supply-chain` is already in the required-checks list the design gives S13; say beside it that the job reads today's advisory databases and can turn red on a push that changed nothing (the answer is an upgrade or a dated exception, the same day), and that the "green on 5 consecutive runs" rule is about the e2e project. The first dates to come due are `review by 2026-12-15` in `native/deny.toml` and whatever entries the allowlist holds by then. S14 — nothing from this slice. S15 / operator — (a) the packaged bundle carries the **debug-profile engine**: `scripts/native-package.mjs` copies `native/target/debug/studio-control-engine.exe`, as it always has, so the evidence bundle does too; whether the engine should ship from the release profile is a release decision this slice did not take. (b) `npm run clean` and `clean:local` delete `release/` whole (`scripts/clean.mjs` lists it as a target), and on this workstation `release\native\windows` **is** the production app; `docs/DEVELOPMENT.md` §4a recommends the command without saying so. Not run in this session, not S12's to change; a task chip was raised 2026-09-18 and the session prompt should carry the warning until it is fixed. (c) `README.md` and `SECURITY.md` say nothing yet about the supply-chain job or the evidence workflow; `docs/RELEASE.md`, `docs/DEVELOPMENT.md`, `AGENTS.md` and `docs/HANDOFF.md` do.

## Slice 13 — Test posture

Status: planned. Closes F25, F03 (e2e blocking). Depends on S2 S10 S12.

Scope: `frontend/app/vitest.config.ts` (+ packages' vitest configs), `frontend/app/playwright.config.ts`, `package.json`, `.github/workflows/dev-checks.yml`, `native/rust-engine/Cargo.toml` (dev-dep `proptest`), `E/control_surface.rs` + `E/rme_totalmix_osc.rs` fuzz modules, this ledger. Vitest coverage (v8) with floors = measured − 2 points; `frontend:test:coverage` script; CI job `rust-coverage` (`cargo llvm-cov --workspace --fail-under-lines <measured − 2>`); Playwright project `quarantine` (`workers: 1`, `retries: 2`) holding `audio-render-budget.spec.ts`, the flaky `shell.spec.ts` case and the `audio.spec.ts:1054` case (S2's CI run 34487837771 added `lighting.spec.ts:721`, the rotate-handle drag, and S3's run 34507456992 `audio-talkback.spec.ts:64`, the `T` hold never read as pressed, as further candidates — each seen on one run and green on the next; S4's run 34514042594 added `audio.spec.ts:702`, the simulated-metering movement mark, and — on the Rust side, where no quarantine project exists yet — `audio::tests_console_link::console_pull_that_never_goes_quiet_is_incomplete`, which failed once on a loaded runner (engine suite 28.65 s instead of 8 s; the fake dump went quiet for its 150 ms window) and passed on the re-run) — the default project becomes blocking in `frontend-e2e`, quarantine advisory with an exit date recorded here; proptest `read_http_request_never_panics`, `query_parameter_never_panics`, `osc_decode_never_panics`; the required-checks list (`format-protocol`, `lint`, `frontend-typecheck`, `frontend-test`, `frontend-e2e`, `rust`, `tauri-foundation`, `qualification`, `supply-chain`) written here for the operator (D4).
Gates before: —. Tests added (planned): the proptest suites; coverage floors. Tests changed: —.
Lanes: `npm run frontend:test:coverage`, `cd native && cargo llvm-cov --workspace`, `npm run build --workspace frontend/app && npm run frontend:playwright:test`, CI. Done when floors are enforced and the default e2e project is blocking and green on 5 consecutive runs. Network is needed for `proptest` and `cargo-llvm-cov`; an offline session must **Rescope:**.
Validation: —. Baselines: —. Operator hands: Appendix B item 6 (required checks).

## Slice 14 — Front-end structure

Status: planned. Closes F26. Depends on S9 S11 (D3: last implementation slice).

Scope: `FA/app/lighting/LightingWorkspace.tsx` → orchestrator + `lighting/clusters/*` + `useLightingEditor.ts`; `FA/app/planning/PlanningWorkspace.tsx` → `planning/bays/*`; `FA/app/setup/SetupSupportPilot.tsx` → `setup/support/{WorkstationSection,BackupsSection,DiagnosticsSection}.tsx`; `scripts/file-health.mjs` (the three allowlist entries removed); `FA/app/OperatorShell.tsx` (`React.lazy` + `Suspense` per workspace after `ready`, idle prefetch of the others); `frontend/app/vite.config.ts` `manualChunks` per workspace. Rule: pure moves — no DOM, class or CSS order changes; zero visual diffs expected.
Gates before: —. Tests added (planned): `shell.spec.ts` `lazy workspace loads`; existing suites. Tests changed: —.
Lanes: `npm run file:health`, `npm run frontend:typecheck`, `npm run frontend:test`, `npm run build --workspace frontend/app && npm run frontend:storybook:build && npm run frontend:playwright:test`, `npm run tauri:workspaces:qualify`, `npm run tauri:visual:review`. Done when no source file exceeds 2,000 lines, the allowlist entries are gone, visual diffs are zero, and `dist` carries per-workspace chunks.
Validation: —. Baselines: —. Operator hands: none.

## Slice 15 — Docs + closeout

Status: planned. Depends on all.

Scope: `docs/OPERATIONS.md` (bridge token + profile re-import, backups, safe start, recovery codes, health states), `docs/DEVELOPMENT.md` (new lanes, feature flags, coverage, quarantine rule), `SECURITY.md` (bridge auth model, OSC binding), `README.md`, `AGENTS.md` (command map additions), `CHANGELOG.md` (consolidate `[Unreleased]`), this ledger (Appendix A complete, Appendix B enumerated, traceability commits filled), `docs/HANDOFF.md` (blocker 9 updated; this program recorded), memory note update.
Lanes: `npm run dev:check`; `npm run tauri:cutover:candidate` (sequential; nothing else on 4173). End-to-end verification: `dev:check` green on a clean checkout; `tauri:cutover:candidate` green including the new scenarios `corrupt-db`, `engine-crash`, `second-instance`, `safe-start`; `native:package:win:local && native:bridge:win:verify` green with the negative checks; CI every job green on the branch push, `supply-chain` green, a `v0.0.0-evidence` tag produced bundles, checksums and SBOMs; manual on the workstation: unauthenticated `curl -X POST http://127.0.0.1:38201/api/deck/action -d '{"action":"deleteProject"}'` → 401, a corrupted copy of the DB → recovery surface with backups listed, a killed engine → recovery and restart within seconds, `netstat` shows loopback OSC binds, a 100 MB log does not slow `health.snapshot`. Done when every F-row has commit + status + guard and Appendix B lists every operator item with a blank signature line.
Validation: —. Baselines: —. Operator hands: Appendix B item 8.

## Appendix A — Gate honesty map (finding → guard → lane that runs it)

Filled as slices land; the guard names are the plan's and become real only when the traceability row says so.

| Finding                                        | Guard                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Lane that runs it                                                                                                                                                                            |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F01 bridge unauthenticated                     | `authorize_rejects_missing_bearer`, `authorize_rejects_wrong_token`, `authorize_rejects_origin`, `authorize_rejects_foreign_host`, `authorization_precedes_body_parsing`, `bridge_refuses_unauthenticated_requests_and_serves_authenticated_ones`, `companion_export_carries_the_bridge_token_on_every_request`; qualification refusals 401 / 403 / 400 and the exported-profile check                                                                                                                                                                                                                                                                                                                                                                                                                                              | `native:test` (CI `rust`); `native:bridge:win:verify` (workstation)                                                                                                                          |
| F02 no backups, integrity ignored              | `initialize_refuses_corrupt_database`, `snapshot_writes_verified_copy`, `prune_keeps_retention`, `corrupt_db_yields_storage_corrupt`; `formats STORAGE_CORRUPT`; `is listening for engine events before it starts the engine`, `keeps the engine-reported failure instead of a ready timeout`; scenario `corrupt-db`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `native:test` (CI `rust`); `frontend:test` (CI `frontend-test`); `tauri:setup-support:qualify` (CI `qualification`)                                                                          |
| F03 CI never ran / e2e advisory                | the run on every branch push (S0); `release-evidence.yml` — a `v*` tag builds the bundle on clean runners, checks its SHA256 manifest against the archive and refuses an empty or foreign SBOM (S12); blocking default e2e project (S13)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | GitHub Actions (`dev-checks.yml` on every push, `release-evidence.yml` on a tag); the operator's branch protection (D4)                                                                      |
| F04 dev method in prod                         | `parity_fixture_unavailable_without_feature` (release build); `parity_fixture_loads_with_feature`, `load_defaults_to_merge` (feature build)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `native:test` (CI `rust`) + `cargo test -p studio-control-engine --features dev-fixtures` (workstation, S1 lane)                                                                             |
| F05 OSC 0.0.0.0 ingress                        | `accept_source_rejects_foreign_ip`, `receive_policy_binds_loopback_for_a_loopback_console_and_every_interface_otherwise`, `receive_sockets_bind_loopback_for_loopback_console`, `foreign_datagrams_are_dropped_and_logged_once_per_minute`, `classic_slots_read_the_console_address_only`, `dropped_source_log_notes_a_source_once_a_minute` (`rme_totalmix_osc/tests.rs`); the workspace lane's audio probe against the live TotalMix (`overrideProbes: false`, `audioSnapshot.verified`)                                                                                                                                                                                                                                                                                                                                          | `native:test` (CI `rust`); `tauri:workspaces:qualify` (workstation only — CI skips the audio probe)                                                                                          |
| F06 bridge DoS                                 | `read_http_request_rejects_body_over_cap`, `read_http_request_rejects_oversized_headers`, `read_http_request_rejects_post_without_content_length`, `read_http_request_rejects_chunked_transfer_encoding`, `read_http_request_times_out_on_short_body`, `worker_pool_returns_503_when_saturated`; qualification 413 / 413 / 408 checks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `native:test` (CI `rust`); `native:bridge:win:verify` (workstation)                                                                                                                          |
| F07 sync commands block UI                     | `shell-stays-responsive-during-stalled-request` — the status-file heartbeat keeps landing while the engine sits in a stalled lighting probe (fails on the sync-command shell: 1,558 ms frozen)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `tauri:setup-support:qualify` (CI `qualification`)                                                                                                                                           |
| F08 request-id collision                       | `ids unique within one millisecond`, `a second transport instance carries its own session nonce`; `request_refuses_duplicate_pending_id`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `frontend:test` (CI `frontend-test`); `native:test` (CI `rust`)                                                                                                                              |
| F09 silent engine death                        | `exit_watcher_fails_pending_and_emits_event`, `stop_reports_a_graceful_exit_and_kills_a_lingering_engine`; store tests "engine.exited → recovery + bounded restart", "stale generation ignored", "keeps the last twenty background failures without leaving ready"; "resolves initialize with the launch the shell reported"; "formats ENGINE_EXITED"; lane `engine-crash`                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `native:test`; `frontend:test`; `tauri:setup-support:qualify`                                                                                                                                |
| F10 no error boundary                          | `ShellErrorBoundary.test.tsx` (root page with no store, development-build malformed reply by name, workspace boundary, `reportUiFailure` once, `BackgroundFailureBand`, `readLogExcerpt`); `shell.spec.ts` "workspace crash keeps shell alive", "the crash hook is absent unless the fixture URL asks for it"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `frontend:test`; `frontend:playwright:test` (CI `frontend-e2e`)                                                                                                                              |
| F11 fan-out + whole-log read                   | `diagnostics::tests::read_log_tail_bounded` and `health::tests::health_snapshot_lists_engine_checks_and_a_bounded_log_excerpt` (engine, S8); `createShellStore.test.ts` "event refreshes only mapped domains" and the cases beside it, `domainRefresh.test.ts` (store, S9); lane checks `commissioning-probes-and-publish-complete` → the DMX monitor assertion, `database-backup-verifies-and-restores-after-restart` → the planning count assertion                                                                                                                                                                                                                                                                                                                                                                               | `native:test`; `frontend:test`; `tauri:workspaces:qualify`, `tauri:setup-support:qualify` (CI `qualification`)                                                                               |
| F12 lighting blob race / deck bypasses preview | `deck_and_ipc_mutations_serialize_without_lost_updates`, `ipc_preview_mode_is_the_preview_the_deck_edits` (`app/tests.rs`); `deck_preview_action_updates_preview_runtime`, `deck_recall_writes_what_a_recall_writes_and_nothing_after_it`, `deck_save_scene_never_reuses_a_live_scene_id`, `deck_relative_keys_store_through_the_fixture_update`, `deck_light_and_planning_actions_raise_their_events` (`control_surface/tests.rs`); `lighting/tests_state_lock.rs`                                                                                                                                                                                                                                                                                                                                                                 | `native:test` (CI `rust`); the deck's keys on the rig while previewing: Appendix B item 5 (operator)                                                                                         |
| F13 in-place migrations                        | `migration_snapshots_before_upgrading_v5`, `blob_migration_failure_rolls_back`, `migrate_schema_refuses_malformed_editor_state_json_at_the_blob_step`, `migration_failure_maps_to_its_own_code`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `native:test` (CI `rust`)                                                                                                                                                                    |
| F14 health never degrades                      | `health::tests::status_error_on_integrity_failure`, `status_attention_on_bridge_port_fallback`, `status_warning_when_the_last_backup_failed_or_is_older_than_two_days`, `report_signals_a_transition_only_on_a_state_change`; lane check `bridge-port-taken-reads-as-health-attention`; `recoveryMachine.test.ts`, `setupState.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `native:test`; setup-support lane step 7 (pre-bound `SSE_CONTROL_SURFACE_PORT`); `frontend:test`                                                                                             |
| F15 no CSP / open paths                        | `open_path_rejects_outside_app_data`, `diagnostics_report_lands_with_a_utc_stamp_and_never_overwrites`; `verifyContentSecurityPolicy` in `tauri:smoke`; `diagnostics-export-defaults-to-app-data-exports`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `native:test` (CI `rust`); `tauri:smoke` (CI `tauri-foundation`); `tauri:setup-support:qualify` (CI `qualification`)                                                                         |
| F16 stderr discarded                           | `engine::tests::stderr_lines_reach_shell_log`, `shell_log::tests::shell_log_rotates_at_its_limit`, `diagnostics::tests::append_log_rotates_at_limit`; documented `rg "eprintln!" native/rust-engine/src` count = 2 (the Slice 8 ledger entry names both)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `native:test`; the S8 ledger entry                                                                                                                                                           |
| F17 no scanning / unsigned                     | `supply-chain` job: the npm audit gate for every lockfile against the expiring allowlist, the review dates on `native/deny.toml`'s ignored advisories, `cargo deny check` (advisories, bans, licenses, sources); `check-npm-audit.test.mjs`, `check-deny-ignores.test.mjs`, `write-release-sboms.test.mjs`, `native-sign.test.mjs`; `release-evidence.yml` (bundle + SHA256 manifest + three CycloneDX SBOMs; signing dormant, D2)                                                                                                                                                                                                                                                                                                                                                                                                  | CI `supply-chain` (every push; reads today's advisory databases); `scripts:test` (CI `format-protocol`); CI `release-evidence` (a `v*` tag); `npm run supply-chain:check` on the workstation |
| F18 DB as bus                                  | `rerenders_settings_only_on_generation_change`, `render_frames_identical_before_and_after_cache` (`lighting_sacn_output.rs`); `thread_local_read_connection_reused`, `kept_read_connection_sees_later_writes_and_blocks_neither_backup_nor_checkpoint` (`storage/tests.rs`); `pending_restore_folds_the_write_ahead_log_into_the_replaced_copy` (`bootstrap.rs`); the restore, restart and recovery checks of the setup-support lane with the kept connections open                                                                                                                                                                                                                                                                                                                                                                 | `native:test` (CI `rust`); `tauri:setup-support:qualify`, `tauri:workspaces:qualify` (CI `qualification`); `native:bridge:win:verify` (workstation)                                          |
| F19 no single instance                         | `second_lock_holder_is_refused`, `bootstrap_refuses_a_held_app_data_dir`, `second_engine_on_the_same_app_data_dir_is_refused` (end_to_end); "formats ENGINE_ALREADY_RUNNING"; lane `second-instance` (the plugin's refusal on Windows and macOS, the engine lock's on Linux)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `native:test`; `frontend:test`; `tauri:setup-support:qualify`                                                                                                                                |
| F20 restore gaps                               | `restore_refuses_newer_format`, `restore_round_trips_all_prefixes`, `list_includes_sqlite3`, `verify_flags_junk_file`, `database_restore_stages_pending_file_and_keeps_rollback`, `pre_restore_archives_are_capped_at_five`, `exports_older_than_thirty_days_are_pruned` (`support/tests.rs`); `pending_restore_applied_after_snapshot`, `pending_restore_that_fails_its_check_is_refused` (`bootstrap.rs`); `newest_snapshot_skips_replaced_files` (`storage_backups.rs`); "restore with requiresRestart restarts the link once and spends no restart budget", "fetches the backup list after a storage failure at start" (`createShellStore.test.ts`); "verify shows inline result" (`SetupSupportPilot.test.tsx`); lane checks `database-backup-verifies-and-restores-after-restart`, `corrupt-db-restores-from-database-backup` | `native:test` (CI `rust`); `frontend:test`; `tauri:setup-support:qualify` (CI `qualification`)                                                                                               |
| F21 synchronous NORMAL                         | `connection_uses_synchronous_full`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `native:test` (CI `rust`)                                                                                                                                                                    |
| F22 relative app-data default                  | `default_app_data_dir_is_absolute_platform_dir`, `runtime_paths_refuse_to_guess_without_a_platform_base`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `native:test` (CI `rust`)                                                                                                                                                                    |
| F23 CWD auto-import                            | `auto_import_ignores_cwd` + `auto_import_reads_the_staged_app_data_file` (`tests/end_to_end.rs`); `legacy_import_source_*`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `native:test` (CI `rust`)                                                                                                                                                                    |
| F24 vite host 0.0.0.0                          | `scripts/frontend/dev-server-host.test.mjs` — Vite host assertion + `0.0.0.0` scan of the server surfaces and `scripts/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `scripts:test` (CI `format-protocol`; runs on the workstation since S1)                                                                                                                      |
| F25 test posture                               | coverage floors; quarantine project; proptest suites                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `frontend:test:coverage`; CI `rust-coverage`; CI `frontend-e2e`                                                                                                                              |
| F26 oversized modules                          | `file:health` with the three allowlist entries removed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `file:health` (CI `format-protocol`)                                                                                                                                                         |
| F27 request logging                            | `diagnostics::tests::request_line_is_off_at_info_and_on_at_debug`, `app::tests::handle_request_writes_no_request_line_at_the_default_level`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `native:test`                                                                                                                                                                                |
| F28 percent decoding                           | `query_parameter_decodes_percent`; the loopback bridge test's `%2F` / `%5F` cases; qualification `%2F` / `%5F` cases                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `native:test` (CI `rust`); `native:bridge:win:verify` (workstation)                                                                                                                          |
| F29 arbitrary restore paths                    | `restore_rejects_path_outside_backups`, `verify_flags_junk_file` (`support/tests.rs`); "verify shows the refusal for a file outside the backups folder" (`SetupSupportPilot.test.tsx`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `native:test`; `frontend:test`                                                                                                                                                               |
| F30 no hardware audit trail                    | `deck_all_off_records_source_deck`, `deck_audio_keys_record_source_deck` (`control_surface/tests.rs`); `ui_requests_record_source_ui`, `recovery_mode_support_snapshot_has_no_recent_events_and_opens_no_database` (`app/tests.rs`); `console_changes_record_source_console` (`audio/tests_console_link.rs`); `watchdog_release_records_source_watchdog` (`audio/talkback.rs`); `action_log/tests.rs`; `migrate_v6_to_v7_after_snapshot` (`storage/tests.rs`); `support_snapshot_carries_the_newest_fifty_actions` (`support/tests.rs`); `SetupSupportPilot.test.tsx`; `setup.spec.ts` (Recent actions); workspaces lane `ui-actions-appear-in-recent-actions`                                                                                                                                                                      | `native:test`; `frontend:test`; `frontend:playwright:test`; `tauri:workspaces:qualify`                                                                                                       |
| F31 no safe start                              | `held_outputs_send_nothing`, `a_hold_reaches_the_wire_on_the_next_tick` (`lighting_sacn_output.rs`); `safe_start_env_holds`, `database_restore_carries_the_armed_flag` (`bootstrap.rs`); `restore_never_changes_whether_the_light_outputs_are_armed` (`support/tests.rs`); `set_armed_holds_and_arms_the_light_outputs` (`app/tests.rs`); `held_light_outputs_are_not_a_fault` (`health.rs`); `arm switch calls setArmed` (`SetupSupportPilot.test.tsx`), `shellData.test.ts`, `setupState.test.ts`; `setup.spec.ts` (Light outputs: Armed / Held), `ui-contract.spec.ts` (the held state against the `setup-ready` ratchets); scenario `safe-start`                                                                                                                                                                                | `native:test`; `frontend:test`; `frontend:playwright:test`; `tauri:setup-support:qualify`                                                                                                    |
| F32 unvalidated snapshots                      | `createShellStore.test.ts` "malformed snapshot rejected in dev", "a row without an id is refused by name", "a malformed snapshot in production is a failed refresh, never a throw", "every fixture scenario passes the guards"; `snapshotGuards.test.ts`; `check-fixtures.mjs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `frontend:test`; `frontend:typecheck` (the fixture check); both qualification lanes (development store on live replies)                                                                      |

## Appendix B — Operator hardware checklist (signed by date)

Sign-off state (2026-09-10): none signed; items 1–5 become walkable as their slices land. Only the operator can do these. Sign each with the date next to the item; the matching slice's `Status:` then moves from `landed (operator verification pending)` to `verified`.

1. After S2: Companion → Full Reset & Import of the regenerated Stream Deck profile; press one deck key and watch one LCD refresh. Signed: \_\_\_\_
2. After S5: kill the engine process during a session; confirm recovery + automatic restart; try launching a second copy of the app. Signed: \_\_\_\_
3. After S6: confirm live TotalMix meters and console echo still arrive (loopback bind). Signed: \_\_\_\_
4. After S7: verify and restore a database backup; confirm fixtures, scenes, deck bindings return. Signed: \_\_\_\_
5. After S10 / S11: start with `SSE_SAFE_START=1` (`docs/OPERATIONS.md`, "Holding the light outputs") — the header's Lighting lamp reads `held`, nothing reaches the rig whatever is recalled, the DMX monitor shows what would be sent; arm from Setup / Support › Workstation and the rig follows; hold again while lit and note what the bridge does without a source (the rig should keep its last look — held is not a blackout). Deck lighting keys while previewing a scene — the rig must not move, the deck's intensity and colour temperature displays read `PREVIEW` on a third line, the Lighting page shows each press at once; with Preview off the same keys move the rig and the page follows. Then a deck lighting key, a deck mute, a mute thrown at TotalMix and a TALK key held until the watchdog releases it: Recent actions shows Stream Deck, Stream Deck, Console, Watchdog — and Screen for what was done on screen. Signed: \_\_\_\_
6. Open the pull request for `production-readiness-2026-09` (after the stack below it) and set the required checks listed in the S13 entry (D4); trigger the linux baseline refresh from the run's `playwright-test-results` artifact. Signed: \_\_\_\_
7. Decide when to buy signing certificates; add the secrets that flip the dormant signing steps (D2) — Windows: `SSE_WINDOWS_SIGN_CERT_BASE64`, `SSE_WINDOWS_SIGN_CERT_PASSWORD`, optionally `SSE_WINDOWS_SIGN_TIMESTAMP_URL`; macOS: `SSE_MACOS_CODESIGN_IDENTITY`, `SSE_MACOS_SIGN_CERT_BASE64`, `SSE_MACOS_SIGN_CERT_PASSWORD`, optionally the `SSE_MACOS_NOTARY_*` trio (`docs/RELEASE.md`, Optional Signing). The signing path has never run: push a rehearsal tag (`v0.0.0-evidence`) first and read both `release-evidence` jobs before trusting it with a release. Signed: \_\_\_\_
8. Sign each item above by date; slices move from `landed (operator verification pending)` to `verified`. Signed: \_\_\_\_

## Baseline refresh procedure

Refresh only after a slice lands and only for diffs the slice explains; the refresh is its own commit `Readiness S<N> (baselines): …`. Build first: `npm run build --workspace frontend/app && npm run frontend:storybook:build`. win32 (this workstation): copy the `*-diff.png` files out of `frontend/app/test-results` before the update run, then `cd frontend/app && npm exec playwright test visual-review.spec.ts storybook.spec.ts -- --update-snapshots` (narrow with `-g "<title words>"`; titles are joined by spaces, not `›`), inspect every changed PNG before `git add`. linux: from the CI `playwright-test-results` artifact of the branch push run (since S0 a push produces it; `frontend/app/tests/__visual__/README.md`), copy each `*-actual.png` over its `*-linux.png` baseline; the pull request itself is the operator's (D4). darwin: pending the next macOS host visit (listed in `docs/HANDOFF.md`). Never run Playwright and a qualification lane at the same time — both bind 4173.
