{
  "name": "sse-exed-sbom-tools",
  "version": "0.0.0",
  "private": true,
  "description": "The CycloneDX generator behind `npm run native:sbom:win:write` (installed by `npm run native:sbom:tools:install`). A package of its own, outside the root workspaces, so its 160-odd packages are installed only where an SBOM is written (the release-evidence workflow) and never by the everyday `npm ci`.",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@cyclonedx/cyclonedx-npm": "6.0.1"
  }
}
