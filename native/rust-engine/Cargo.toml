[package]
name = "studio-control-engine"
version = "2.2.1"
edition.workspace = true
rust-version.workspace = true

[features]
default = []
# Enables ts-rs `#[derive(TS)]` on snapshot types and triggers TypeScript
# binding export when `cargo test --features ts-rs` runs. Production
# builds must NOT enable this feature; it adds a dev-only dependency.
ts-rs = ["dep:ts-rs"]
# Compiles the dev-only `dev.parityFixture.load` method together with its
# bundled fixture payloads (`parity_fixtures.rs`). Release engines are built
# without it and answer METHOD_UNAVAILABLE; the method stays in the protocol
# contract (2026-09 production readiness, Slice 1 — finding F04).
dev-fixtures = []

[dependencies]
rusqlite.workspace = true
rosc.workspace = true
getrandom.workspace = true
serde.workspace = true
serde_json.workspace = true
studio-control-protocol.workspace = true
ts-rs = { workspace = true, optional = true }

[lints]
workspace = true
